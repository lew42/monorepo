# css — improvements

Any agent may append. One line each: `YYYY-MM-DD · what should change · why (the evidence)`.
A recurring line is a rule waiting to be written; the owner promotes.
2026-09-06 (omnibox-search) · say that a utility class on a box you also want to HIDE is a trap, not just an override trap · `div.c("omnibox-chips flex v gap")` plus `.omnibox:not(.open) .omnibox-chips { display: none }` left the chips on screen permanently: `.flex` is `@layer util` and a module's `@layer theme` cannot reach it at any specificity. The skill states the direction ("a module's own @layer theme rule cannot override it at ANY specificity") but only for sizing — `display` is the case where the failure is invisible rather than merely wrong, and the fix is the same: drop the utility from the markup and declare the three lines yourself.
2026-09-06 (ui-theme) · caveats.md names the `:first-child` half of framework.css's util margin rule; add the `:last-child` half, and say it reaches a COMPONENT'S CHILDREN, not just page blocks · `:last-child { margin-bottom: 0 }` cancelled `ext/tabs`'s `.tab { margin-bottom: -1px }` on the last tab of every strip, and because a `.tab-bar` stretches its items that one low tab made every sibling a pixel taller — a set of "equal" tabs measured 37.09 and 36.09px with both rules looking correct in devtools. The fix has to be restated in `util`, since `util` is the layer that took it away.
