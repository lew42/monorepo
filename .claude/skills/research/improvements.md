# research — improvements

Any agent may append. One line each: `YYYY-MM-DD · what should change · why (the evidence)`.
A recurring line is a rule waiting to be written; the owner promotes.

- 2026-08-30 · written · four minions dug in parallel with the schema in a prompt rather than a skill; all 175 lines happened to validate, but nothing except that prompt would have made the next run grade its credence at all.
