# documentation — improvements

Any agent may append. One line each: `YYYY-MM-DD · what should change · why (the evidence)`.
A recurring line is a rule waiting to be written; the owner promotes.
- 2026-08-19 (ext/Panel sizing): the pre-landing check says "the page loads clean at 1600" but not what URL a `doc/*.md` answers at. `/module/doc/<name>/` **404s**; the shape the rail links is `/module/doc/<name>.md` and `/module/doc/file/<file>.md` (extension included). Cost a probe cycle of false 404s that looked like a broken doc.
