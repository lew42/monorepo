# mastermind — improvements

Any agent may append. One line each: `YYYY-MM-DD · what should change · why (the evidence)`.
A recurring line is a rule waiting to be written; the owner promotes.
2026-08-31 - briefs should say: search with Glob/rg scoped to the repo, never `find /` - second orphaned root-scan in two days burning a core for hours (feeds minion 3673 cpu-sec 08-30, sweep minion 887 cpu-sec 08-31, both reaped by the mastermind)
