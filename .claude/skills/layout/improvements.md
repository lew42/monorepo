# layout — improvements

Any agent may append. One line each: `YYYY-MM-DD · what should change · why (the evidence)`.
A recurring line is a rule waiting to be written; the owner promotes.

2026-08-17 · check `width-used` at 3440 explicitly in question 2 — it is the prime objective as a number · it read 6–10% on 17 of 18 pages until fixed (now 0.92 at 1280 vs 0.58 at 3440) and is 28.1% of the generator's quality gap (ai/2026-08-17/tier-calibration/, ai/2026-08-17/loss-budget/) — declined 2026-08-17: Q2 already names the widths; belongs in ext/DesignTool/knowledge/

2026-08-18 · question 1 should name `catalog()`: on a page with `initialize(){ this.catalog(); }`, `content()` is moved into a child `intro` page inside `.page-catalog-pages`, so `.wide` claims the DETAIL column, not the region · measured on /framework/styles/elements/code/ — the block claiming `wide` got 591px at 1280 (651px inner page) and 2428px only at 3440, so a 6-column table wraps at the two widths most readers use, and the answer to Q1 written before looking was wrong by 2x

2026-08-18 · question 3 should name what the flex vocabulary CANNOT do: every flexible word sets `flex-grow: 1` (`.flex.auto`, `.all-1`, `.three`, `.flex-1`), so free space is always split evenly and there is no word for a fluid track twice its neighbour · two of eight Figma wireframes needed a 2:1 seam and both ship an inline `flex: 2 1 30em`; the zero-CSS workaround (per-child `--column` under `flex auto`) decays 2.00 → 1.45 → 1.30 → 1.17 at 400/1280/1920/3440, measured (styles/layouts/wire/doc/bento.md)

2026-08-18 · "look at it, then cycle" should warn how a wrapped row is COUNTED: distinct `Math.round(rect.top)` is the obvious metric and it is wrong under `v-center` — centred children of different heights get different tops on the SAME line · it reported the homepage topbar as 3 lines at 1920 (it is 1) and 6 other seams likewise, and the fix is to call two children one line when their vertical ranges OVERLAP (ai/2026-08-18/figma-home/)
- 2026-08-18 (figma/home): verifying `scrollWidth === clientWidth` proves only that nothing overflows HORIZONTALLY. A page can pass it while its content region is collapsed — the homepage had 4549px of bands inside a 284px box in a `page full fill` shell, and read as nav->hero->footer with seven bands invisible. Add the vertical twin to the skill's 'look at it' step: assert the CONTENT region's `scrollHeight <= clientHeight`, or that the document is what scrolls. Also: test an odd width (1440 found it; 400/1280/1920/3440 all missed it). ⚠ Scope the check to regions INSIDE the layout — `div.pages` is the SPA's own scroller with an explicit `overflow-y: scroll` and reads 24x at 400 legitimately. An unscoped sweep false-positives on it, twice tonight. The real defect looks different: a `flex-1` collapsed *within* the layout while a sibling footer held its full height.

2026-08-18 · "look at it, then cycle" needs a VERTICAL check beside the horizontal one: `scrollWidth === clientWidth` is blind to a region that collapses and scrolls its content internally, and a `fill` page reports the INNER scroller's scrollHeight as "page height", so both numbers read healthy while seven of ten bands were unreachable · found at 1440 on styles/layouts/home/ — 4549px of content in a 284px `.flex-1` (16.0x) with document scrollHeight === clientHeight === 1000; assert `inner.scrollHeight <= inner.clientHeight + 50` on every `overflow-y: auto` box inside the layout, and test 1440, which sits between the two widths everyone tests (ai/2026-08-18/figma-home/)
