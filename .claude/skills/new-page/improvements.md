# new-page — improvements

Any agent may append. One line each: `YYYY-MM-DD · what should change · why (the evidence)`.
A recurring line is a rule waiting to be written; the owner promotes.


- **A page `icon:` is unverified and fails silently.** The site loads **Material Icons**, not Material Symbols; a name only the newer set has (`developer_guide`) renders as its own literal ligature — 291px of text in a 219px card label, breaking the title into two lines on the parent's wall. Nothing throws and the page itself looks fine. Probe a new name's `offsetWidth` in the live page: a real glyph is ~19px, a miss is 100px+. (2026-08-18, figma-apidoc)
