# new-task — improvements

Any agent may append. One line each: `YYYY-MM-DD · what should change · why (the evidence)`.
A recurring line is a rule waiting to be written; the owner promotes.
2026-09-04 · the PostToolUse edit hook attributed a table-equal minion's requirements.md edit to the local-dev-harness minion's task.jsonl — with several minions in one session the hook picks the wrong task; evidence in ai/2026-09-04/local-dev-harness/task.jsonl. Worth saying in section 3: check your own log's action lines at landing and strike a sibling's.
2026-09-04 · the shared-scratchpad overwrite bit the MASTERMIND too: a reviewer minion replaced the mastermind's platform-probe.mjs at 18:13 (different port, different url) and the next harvest probe failed with a connection refused that looked like a dead server. The rule 'name scripts after your task' is in section 1 but agents skip it; the fix that holds is a per-agent SUBDIRECTORY of the scratchpad (scratchpad/<slug>/), stated as the default in the skill.
2026-09-05 · section 2 says refresh public/framework/ai/usage.json every ~15 min, but a fenced multi-agent brief (eighteen UX reviewers, 2026-09-05) explicitly scopes writes to the realm dir + task dir + one day.jsonl line and says nothing about usage.json — following section 2 literally means writing outside your fence on a file seventeen siblings also touch. Worth a line: skip the usage refresh when your brief names a narrower write scope and does not mention it.
2026-09-05 · confirming the line above: bit a second reviewer (ux-blogx) the same night. Section 2's "refresh every ~15 min" ran ahead of reading the brief's own fences and appended to public/framework/ai/usage.jsonl before noticing the brief scoped writes to the realm dir + task dir + one day.jsonl line; caught after landing and the one appended line was removed. Two agents in one night is a rule waiting to be written: section 2 should say "unless your brief names a narrower write scope," not assume it is always in-fence.
- 2026-09-05 (spacing-components): the escapes warning covers `$`, `<`, `>` and backticks but not the one that bit me - a `\"` inside a python string inside an UNQUOTED bash heredoc (`<<PY`). Bash eats one backslash, python eats the other, and the quote lands raw: the line appended fine, looked fine in the terminal, and broke `json.loads` at column 154. Say it: quote the heredoc (`<<'PY'`) whenever the payload contains a quote, and re-parse EVERY line of the jsonl after any append, not just the one you wrote.
