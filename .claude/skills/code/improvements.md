# code — improvements

Any agent may append. One line each: `YYYY-MM-DD · what should change · why (the evidence)`.
A recurring line is a rule waiting to be written; the owner promotes.

Applied and cleared 2026-09-21 — all 20 entries (21 traps) folded into SKILL.md section 7 by
`traps-consolidate`; the reasoning is that task's log, the exact text its `skill-patch.md`.
