/* THE WALL'S CONTENT — every addon in `framework/ext/`, read off the modules
   themselves: `icon` and `description` from each one's `page.js`, `about` from
   the first line of its `readme.md`. Not one sentence here was written for this
   demo, which is the point — a layout judged on invented content is not judged.

   ⚠ TWENTY-FOUR IS THE COUNT, AND IT WAS CHOSEN. 24 divides by 8, 6, 4, 3, 2 and
     1, so every column count this wall can reach fills its last row exactly and
     the wall never shows a hole. Re-generated by
     `scratchpad/practice-gen-modules.mjs`; if `ext/` gains a module, pick the
     new count for its divisors, not for its tidiness. */

export default [
	{"name":"markdown","icon":"article","say":"Markdown as a View addon — md(), view.md(), md.file() — plus a fence that can name its file.","about":"`md()` for prose and `md.file()` for a whole `.md`, as a View addon; already re-exported from `/app.js` for every page"},
	{"name":"demo","icon":"play_circle","say":"Show the code, then run it — from one source. Four doors.","about":"show the code and run it from one source; the site's one example mechanism, for any page that shows a thing"},
	{"name":"highlight","icon":"code","say":"Syntax highlighting bolted onto the code element — code.js(), code.fn(), and every markdown fence.","about":"syntax highlighting on the `code` factory, for every page that shows code"},
	{"name":"files","icon":"folder_open","say":"A tree of real files, and the one you clicked — as panels you can resize, split and rearrange.","about":"a tree of real files on disk, fetched, beside the one you clicked; for pages that teach a directory"},
	{"name":"toc","icon":"toc","say":"This page's own headings, as a nav, with the current one marked.","about":"this page's own headings as a right-hand nav, current one marked; for any plain reading page"},
	{"name":"Doc","icon":"menu_book","say":"A module as a page: its files, its members, its notes — each one a real url backed by a .md you wrote.","about":"a module documented as a page (Overview · API · Docs · Files), each member a real url backed by a `.md` beside it; for anyone writing a module's `page.js`"},
	{"name":"tabs","icon":"tab","say":"A bar of links and the panel they fill — one ext, patched onto every Page.","about":"a bar of links and the panel its children mount into; `this.tabs()` on every `Page`, for pages that flip *between* children"},
	{"name":"catalog","icon":"view_sidebar","say":"previews() as a persistent rail beside the region the children mount in — the mechanism behind every Doc Overview tab.","about":"two arrangements of `previews()` patched onto `Page`, for any index page: `catalog()` pins a rail beside the routed child (read in order); `browse()` lays a filterable wall of bands (choose from the set). Same cards either way (RULE#7)."},
	{"name":"layout","icon":"tune","say":"A toolbar over anything, and a drawer that pushes the page over to make room.","about":"a toolbar over anything, and a right-hand drawer that *pushes* the page over; for demo and guide pages that let the reader arrange things"},
	{"name":"drawer","icon":"view_sidebar","say":"The right rail — one per document, opened by anything, shut only by its own ✕.","about":"the right rail, for anything that needs a place beside the page: one per document, opened by any caller, shut only by its own ✕; it pushes the page, never covers it."},
	{"name":"grip","icon":"drag_handle","say":"A rail's resize edge — a strip inside the edge it drags and a pill that rides your pointer. Shared by the drawer, the dev rail and the Playground's two columns.","about":"a rail's resize edge: a strip just inside the edge it drags, and a pill that rides your pointer. No permanent handle. Shared by `ext/drawer` and `dev/DevBar`"},
	{"name":"Dropdown","icon":"view_agenda","say":"One choice out of a list — a trigger showing its picture and its name, and a list that opens in the top layer, so nothing can clip it.","about":"one choice out of a list: a trigger showing its picture and its name, and a list that opens in the top layer, so nothing can clip it"},
	{"name":"Omnibox","icon":"search","say":"Moved into core on 2026-09-06 — the search box is core/Search, mounted once for the whole site.","about":"moved into core on 2026-09-06; this directory is a pointer"},
	{"name":"depth","icon":"layers","say":"A page becomes a 3D scene; layers drift against each other as you scroll.","about":"a page becomes a 3D scene; `.depth(n)` layers drift as you scroll and lean with the pointer"},
	{"name":"DesignTool","icon":"straighten","say":"Measures a layout and scores it. Ratios, not eyeballs — and no AI at runtime.","about":"measures a layout numerically: what is BROKEN, what is OFF, what is GOOD. For pages, the layout generator, and the site audit — no AI at runtime."},
	{"name":"Saver","icon":"save","say":"Where a document goes. Three methods, one write queue, four backends.","about":"where a document goes: `save` / `load` / `delete` over one write queue, for anything that persists JSON"},
	{"name":"Draggable","icon":"drag_indicator","say":"Grab a node and drop it somewhere else — reorder, cross-list and nest on one code path.","about":"grab-and-move for any View; `Sortable` reorders, crosses lists and nests on one code path"},
	{"name":"editor","icon":"design_services","say":"A drag-and-drop builder on this stack and nothing else — the prototype the whole thing points at.","about":"a drag-and-drop block builder assembled from Item, Sortable, Saver, ext/layout and Panel; a page you visit, not a module you import"},
	{"name":"Panel","icon":"dashboard_customize","say":"Chrome for arranging: divide, drag, align and fill any region — and it survives a reload.","about":"chrome for arranging: divide, drag, align, fill and persist any region; for wireframing pages, not shipping them"},
	{"name":"Timeline","icon":"view_timeline","say":"A general-purpose h/v timeline — CSS-variable positioning and zoom, greedy lane packing, window bands.","about":"a general h/v timeline (CSS-var positioning, greedy lanes, window bands) for anyone plotting dated items"},
	{"name":"AITask","icon":"smart_toy","say":"A task's task.jsonl (or legacy session.json), rendered — request, checklist, spend, agents — with a live feed and a chat panel onto the raw transcript.","about":"the AI working log, rendered: one task's page, a day's dashboard, the highlights wall the front opens with, and the board over every task"},
	{"name":"JSONL","icon":"table_rows","say":"Append-only .jsonl logs, assembled back into object state by replaying one verb per line.","about":"append-only `.jsonl` logs the AI blindly appends to, replayed back into object state; the task and day logs under `ai/`, for the dashboards that read them"},
	{"name":"Research","icon":"biotech","say":"A research topic as an append-only log — minions write it while they dig, the page renders it live.","about":"a question dug by several minions into append-only files, rendered live: conclusions first, every claim a card that opens forever, and how sure anyone is never left to the reader to guess"},
	{"name":"Ask","icon":"forum","say":"Chat with a Claude Code session from the browser — one socket message, one headless CLI turn.","about":"talk to a Claude Code session from the browser; for pages and tools on the dev server (localhost only)"},
];
