import { Page, md } from "/app.js";

export default new Page({
	meta: import.meta,
	title: "Research",
	description: "Ancient technology, depictions, disclosure — a continuous research program. Being built.",
	content(){ md("Being built — the research program."); },
});
