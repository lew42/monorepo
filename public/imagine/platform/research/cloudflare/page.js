import { Program } from "/framework/ext/Research/Program.js";
import "/framework/ext/Research/Topic.js";

export default new Program.Topic({ meta: import.meta, title: "Cloudflare", icon: "cloud", description: "Workers, Durable Objects, D1, KV, R2, local dev and the cost table — is one DO per topic the right shape?" });
