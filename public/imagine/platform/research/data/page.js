import { Program } from "/framework/ext/Research/Program.js";
import "/framework/ext/Research/Topic.js";

export default new Program.Topic({ meta: import.meta, title: "Data", icon: "storage", description: "Where each kind of data lives so the site stays static and the platform stays cheap — five architectures weighed." });
