This module's own doc page — a `Doc` over `AITask`, its files, its notes.
Nothing else in `public/` imports this file; a `page.js` documents its
module, it doesn't run as part of it.

## Improvements

1. **None outstanding** — rewritten to `new Doc({ … })` in this pass; see
   the module's audit report for what changed and why.
