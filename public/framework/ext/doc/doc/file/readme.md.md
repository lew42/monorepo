The maintainer's document: the dilemmas, what was tried, why the current shape won,
what is still open. The reader's document is `page.js` — blurring the two is the
failure this split exists to prevent.

It is served twice: cited by a maintainer reading the directory, and collapsed at
the bottom of the Overview tab via `md.details(import.meta, "readme.md")`.

## Improvements

1. **The Decisions section will outgrow one screen**, as every mature readme here
   has. The skill's rule is that Decisions/Traps/Open are exempt and the
   *explanatory* half is what breaks out — worth re-reading this file against that
   rule the next time it is edited. *(simple, useful)*
