Vendored **highlight.js 11.11.1 core** (`@highlightjs/cdn-assets`, the `es/`
build), BSD-3-Clause. 20 KB, minified, ends in `export{te as default};` —
verified zero `import` statements and no `sourceMappingURL`, so it is a real
standalone ES module with no bundler needed to consume it. `highlight.js`
imports it as `hljs` and calls `hljs.registerLanguage()` and `hljs.highlight()`
on it; nothing else in this module touches it directly.

Don't hand-edit. To upgrade, replace the whole file from the same `es/` build
and re-check the version string in the header comment.
