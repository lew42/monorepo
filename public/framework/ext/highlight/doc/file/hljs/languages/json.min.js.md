The `json` grammar, vendored from highlight.js 11.11.1. Smallest of the five
(511 bytes) — JSON's whole grammar is punctuation, strings, numbers and three
literal keywords. Standalone ESM, no imports. Registered under `code.json`.
