The `xml` grammar, vendored from highlight.js 11.11.1 — it also covers HTML,
since HTML is a tag-and-attribute grammar XML already handles. Standalone ESM,
no imports. Registered under both `code.html` and `code.xml`, which is why the
accessor table in `readme.md` lists two names for one grammar.
