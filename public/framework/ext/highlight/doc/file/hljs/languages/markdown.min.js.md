The `markdown` grammar, vendored from highlight.js 11.11.1. Standalone ESM, no
imports. Registered under both `code.md` and `code.markdown`. Independent of
`ext/markdown`'s own parser (`marked.esm.js`) — this grammar only *colours*
markdown source shown as a code sample; it never renders markdown to HTML.
