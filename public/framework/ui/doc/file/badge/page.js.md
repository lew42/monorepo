The badge exhibit plus one variant (`icons`) showing an icon-and-text pill
built from the same span with a body function instead of a string.

## Improvements

Nothing ranked: the page correctly defers the status-color gap to
`doc/record.md` rather than re-arguing it, and the dot's `currentColor` trick
(free re-tinting per variant) is already called out in prose.
