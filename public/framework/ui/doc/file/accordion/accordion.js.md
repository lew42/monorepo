Two rules, both relationships: a hairline between consecutive items
(`+`), and a margin under a summary's own paragraph.

## Improvements

Nothing ranked: two declarations, and the `+` selector's job (keeping the
first item from wearing a rule above it) is commented in place.
