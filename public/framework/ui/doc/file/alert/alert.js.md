A left-edge accent, three lines of CSS: base, `.accent`, `.error`. Imports
`../../core/View/View.js` for the sole reason that its `> .icon` selector
names a class this file doesn't emit — the loading-edge-not-annotation rule
from `code-architecture` in miniature.

## Improvements

Nothing ranked: six declarations, every one a token, the import comment
already states why it's there.
