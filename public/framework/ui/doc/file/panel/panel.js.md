Three rules, each a relationship: a hairline under the head, a hairline over
the foot, an optional raised shadow. The shadow mixes `var(--ink)` rather
than an `rgba` literal, so it stays a fixed percentage of contrast in both
light and dark.

## Improvements

Nothing ranked: three declarations, each with a one-line reason in the
comment above it.
