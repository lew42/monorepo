The maintainer's document: why capturing works the way it does, why nine other
methods deliberately don't exist, and a ranked backlog — `## Proposed` — from an
every-member audit that nobody has applied yet. The reader's document is
`page.js`; this one is for whoever next has to change `View.js` and needs to know
what was already tried.

It is served twice, same as every module's readme: cited by a maintainer reading
the directory, and collapsed at the bottom of the Overview via
`md.details(import.meta, "readme.md", …)`.
