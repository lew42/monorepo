# Page overview — a `browse()` wall, cards drawn live from each demo's own `page.js`

Four bands, declared as `BANDS` in [`page.js`](./page.js) — the source of truth for what's on the wall and in what order.

Add a card: a directory here with a `page.js`, and its name added to the right band's string in `BANDS`.
