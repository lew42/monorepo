Second in the rail: the `web` tree shows the three `children` shapes side by side
— a bare function (`HTML`), an options object (`CSS`), and a pre-built `new
Page(…)` (`JS`) — as siblings in one object, so the reader compares the three
without leaving the page.

## The order inside the tree is the argument

Leanest first: a function needs nothing but its title-as-key; the object adds an
`icon`; the real `Page` is adopted "as it stands". The closing line — "three
shapes, one result" — is provable only because `content(){ this.previews() }`
renders all three as identical cards regardless of which shape produced them.

## Improvements

1. **No `doc/file/overview/children/page.js.md` existed.** *(simple, important —
   done in this pass.)*
