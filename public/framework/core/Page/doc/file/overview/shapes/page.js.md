Sixth in the rail, and the last of the "Basics" group: the `studio` tree's three
children each declare a different `classes:` — the default `standard`, `full
pad`, and `full fill flex v` — so walking the rail shows the page shape changing
under an otherwise-identical demo.

## The one demo in this module with harness options set

`rail: true, height: "18em"` — both `demo.tree()` harness flags, not part of the
lesson. Per `../readme.md`, that is fine here specifically because the visible
box chrome (a url strip, a fixed height) claims nothing the printed source would
need to justify — unlike `labels/`, whose rail *was* the lesson.

## Improvements

1. **No `doc/file/overview/shapes/page.js.md` existed.** *(simple, important —
   done in this pass.)*
