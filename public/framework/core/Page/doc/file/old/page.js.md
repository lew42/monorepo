`Old`'s own page: a plain `Page`, not a `Doc` — `children: "overview nav children previews shell flow"` and a `render()` copied from `ext/Doc/Doc.js`'s `section()`, the exact `.tabs.vertical` left-rail shape a Doc section builds, minus everything a Doc infers. `old.css` (the 4th-level tab invert) loads here rather than beside `overview/page.js`, since `View.stylesheet`'s second argument resolves against `import.meta.url`.
