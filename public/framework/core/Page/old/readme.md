# Old — the first `Page` docs, moved out of the Overview tab and kept as reference

A left rail (`.tabs.vertical`) over `overview nav children previews shell flow` — the same pages, unedited, while `page-docs-rewrite` replaces the Overview tab with a visual grid.

`overview/` is one rail item now, not a catalog: [`overview/readme.md`](./overview/readme.md) has the fifteen demo trees and the 4th-level tabs experiment on top of them.
