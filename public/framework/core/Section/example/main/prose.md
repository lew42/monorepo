This page was read off disk. It is an ordinary page, and it is sitting in the section's
**Main** slot because its directory is called `main` — the layout named the box, the
`page.json` named the child, and the two met by name. Nothing else was wired.
