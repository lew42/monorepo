# `readme.md`

The largest of the three tier readmes by topic count: the Router, `naming()`,
logging conventions (`console` directly, groups only around synchronous work),
scroll behavior (traced to a shared scroller, then fixed), the columns rework
and its two flex traps, and — the throughline — "The column problem, in
detail" through to "SOLVED... `display: contents`", including a sibling
prototype (`new/2`) that was built, measured, and deleted, with its rejection
recorded rather than its code kept.

## Structure worth knowing

Chronological-ish rather than a fixed template: sections read as a log of
problems hit and fixed, ending in "Still unresolved" (8 items) and "Mode is a
class, not a structure — and never a link" as the closing verdict.

## Improvements

1. **None ranked.** Long (700+ lines) but each section earns its length —
   several are load-bearing arguments (`new/2`'s rejection, the column
   problem) cited by `new/1/readme.md`.
