# page.js

The lab's index. Prose in the page's `main` track, the six words live at **full bleed** —
a column ratio demoed inside a 52em prose track would be a demo of the prose track.

A `Doc`, so `notes:` gives it the Docs tab (`indictment`, `words`, `adoption`) and `files:`
gives it this one. `children: "matrix"` is the second page.
