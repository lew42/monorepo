## `page.js`

The demo: `Auth` live in a resizable stage against a `wash` ground, its source open
below; a words band proving `ui-contrast`/`ui-compact` re-skin it with no Auth-side
support; `MagicAuth` as its own tab, declared in `children:`.
