# notes-pages-2 — images 023–038 become note pages (Opus)

Same brief as `../notes-pages/requirements.md` — read it first, in full, and follow it; then `../../2026-09-04/mastermind-platform/minion-rules.md`. Skills: `new-task` (this dir, group `notes`), `code`, `layout`, `new-page`, `documentation`, `finish-task`. A first minion is finishing images 001–022 right now; **you own images `public/notes/inbox/2026-09-06-023.jpg` through `038.jpg`** and nothing else. All sixteen are upright two-page spreads.

## Rules that are yours specifically

- **The owner's name never appears on a page.** Two spreads (035, 036) carry a skill name and a persona name built from it, and one line addressed to the owner by name. Write "the owner" and "the mastermind skill"; a persona list keeps its other names and drops that one. This is a global rule of the repo, not a preference.
- **Third parties → initials** (027 has a person's habits list beside a name; 028 names a relative). **Personal reflections** (027 left, 028 both pages) are transcribed as the owner asked. **One line to omit**: the apartment line at the top of 024's left page.
- **Shared file:** `public/notes/page.js` `children:` — read it immediately before each edit; if the edit fails because the line changed under you, re-read and retry once. Add only your own slugs. Never touch `public/notes/doodles/` or any note dir you did not create.

## What these hold — the framework's own design history, so the links are the point

023 AI research (generative search, annotate md, mdjj, log.jsonl, ai → mcp) and "list UI/UX, AI screenshotting, #1 1-column #2 2-col" → link `ext/Research`, `ext/DesignTool/vision`, `core/Layout` if it exists by then. 024 split responsive viewer + synced scroll, 3D interactive, AI dashboard, layout generator/panels/editor, drag & drop, OOP JS → link `ext/demo`, `ext/Panel`, `ext/Playground`, `/framework/ai/`. 025 `app.use(new Dev())`, content is sections, "simplify my website", publish to npm?, homepage fly/game/ui/ux → link `core/App`, `/imagine/sections/`, the root readme. 026 laws/rules/suggestions, decision trees, layout tool (reading width, alignment), "fewer pages of AI slop, hide them away", parallel AIs + arbitrator, modular/interactive classes, auto-regenerate → link `CLAUDE.md`'s laws (as `/framework/` readme), `ext/DesignTool`, the mastermind runs. 027 per-module sessions, 3D web via CSS transforms, Full Swap, ELEMENTS/SECTIONS "3–5 of the most categorical examples as a quick starting point", hover-based layout actions ctrl+S → split → link `/imagine/paging/mechanisms/`, `/imagine/layouts/`. 028 map UX, resizable columns → modes, LAYOUT TOOL: analyze spacing as % (0→1), analyze scale, both relative → link `ext/DesignTool` `rate()`, `/imagine/design/spacing/`, `/imagine/design/size/`; premium/levels → the levels verdict. 029 column vs swap sketch, "each page has children in a right sidebar → no col needed, just swap", sections aren't lib-able but examples are, design generators (generate → crowd-improve → learn), panel system, page system ⇒ page.json/jsonl, selection system → history → link `/imagine/paging/`, `make/`, `ext/Panel`. 030 fill vs split for render/code, mobile demos full-screen with their own nav, "full grid of previews", ELEMENTS · UI · LAYOUTS · PAGES · SITES/APPS, the EDITOR (left sidebar = layers/pages; any el/ui/layout/section/page loads into it; go fullscreen; viewport ⇒ emulators) → link `ext/demo`, `/imagine/paging/build/`, `core/Layout`. 031 edit mode with a toolbar, devbar at bottom on mobile, `new Page({title, content})`, "resizing w/ multiple columns is a little jumpy", PageDemo extends Page → link `core/Page`, `/imagine/paging/navigation/`. 032 demo viewport/fullscreen, "my docs are bad", demo bar/stage, master/detail, base/theme, components tracked "are we using this?" → link `ext/demo`, `ext/CSSDoc`, `styles/css-scopes.txt`'s page. 033 `$nav` show/hide, `.page.active { display: var(--page-display) }`, framework.css 4 layers, docs injected into code, `page.layout()` → link `core/Page/Page.css`, `framework.css`'s doc, `ext/Doc`. 034 tabs add sub pages, lazy pages via router, `page.views[]` ← sidebars/page/links, lew42.com as a fork of the framework → link `ext/tabs`, `core/Router`, the templates deliverable in `../mastermind-graduate/layout-brief.md`. 035 the mastermind skill's origins ("we think", MVP → exploration → v1, decide by vote, generate all work onto the FS, PROMPT POLLUTION), app takeover/full modes → link `.claude/skills/mastermind` (not a page — link `/framework/ai/` instead), `/imagine/paging/mechanisms/`. 036 `page.activate()`, load but don't render, decision flow (fork 3×, compare), the council personas → link `core/Page`, the retired council note if a page exists. 037 nested rendering, app independent of router, Class.method.toString() as docs, CSS chunk matching, the status colours → link `core/Router`, `ext/CSSDoc`, `ext/Doc`. 038 AI docs readme 1–N, `code.demo(fn)`, route registry?, top-down sequential loading → link `ext/Doc`, `core/Router`.

Build to match where a sketch is UI: 023's five tiny layout boxes as a strip; 029's column-vs-swap as two small frames; 030's editor sketch as a wireframe; 037's status colours as five swatches. Everything else is transcription and links.

## Prove it and fences

As the first brief: every page and the index at four widths, zero console errors, every link resolves, images upright and web-size. Use the shared server `http://localhost:8123/` — start none, kill none. Write `public/notes/<your slugs>/**`, your slugs in `public/notes/page.js`, this task dir. Budget ~300k tokens. Report in ≤ 10 plain lines: your sixteen slugs as "slug — one sentence — what it links", and the shared-file edits you made.
