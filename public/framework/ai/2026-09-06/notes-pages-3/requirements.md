# notes-pages-3 — images 039–054 become note pages (Opus)

Same brief as `../notes-pages/requirements.md` and the rules of `../notes-pages-2/requirements.md` — read both in full and follow them; then `../../2026-09-04/mastermind-platform/minion-rules.md`. Skills: `new-task` (this dir, group `notes`), `code`, `layout`, `new-page`, `documentation`, `finish-task`. Two other minions own 001–038; **you own `public/notes/inbox/2026-09-06-039.jpg` through `054.jpg`** and nothing else. All sixteen are upright two-page spreads, the framework's own design history, so the links are the point.

## Rules that are yours

- **The owner's names never appear** — 040 uses the owner's first name as a case-sensitivity example: write `Name && name`; 045 has a product idea named with the owner's surname: write "the [surname-initial]. Cube". Everything else is product thinking. No third parties in this batch that I saw; if you find one, initials.
- **When you leave anything out or reduce it, link the original**: `/notes/inbox/2026-09-06-0NN.jpg` with the sentence "the original, which only opens on a local checkout (the inbox is not in the repo)". The owner asked for this.
- **Shared file:** `public/notes/page.js` `children:` — read it immediately before each edit; if the edit fails because the line changed, re-read and retry once. Add only your slugs.

## What each holds, for links and builds

039 routing: `/app.js ← /page.js ← m/a ← m/a/b`, walk & dynamic-import each vs jump-to-topic, "FIND KEYS IN COUCH", move app.routes into Router by default, Page is only a View w/ show?, versioned files (`/1/Thing1.js`, `/1/0/…` published at 1.0.x); pre-loading pages, "if you import a page that uses app before new App() it blows up", dormant `new Page()`, sequential loading `/deep/path/to/` → `/deep/page.js`, test → responsive viewport, node test, stubs, Playwright → link `core/Router`, `core/Page` (dormant pages), `ext/DesignTool`/the ui-test tooling page if one exists. 040 direct people to the site, email/domain case-insensitive (`toLowerCase()`), "EVERY /public/ topic = no bueno", `app.main()` renders the holy-grail layout, render `parent.nav()` after each page, `app.nav()` vs `page.nav()`; starter repo vs scaffolding, submodule, git CDN, `npm i -g lew42`, `public/package.json`, mandatory importmap, `app.theme("name")` switches theme classes → link `core/App`, the root readme (setup), `styles/` themes. 041 the site's first nav list (About, Fly, Framework, Marketing, Design, Development, Layout), a theme dropdown, GitHub bundle, Squeeze (collapse pad to 0, container queries), section padding blows out on mega, iframe previews or full-screen layouts, simulate desktop at 1920×1080; "any layout could add any other layout", Base/Framework/Lew42, an element browser (Elements · Layout · Sections · Components → Text Btn Img Input / Flex Gap Wrap), page-title sketches → link `core/Layout` (the tree), `/imagine/sections/`, `styles/elements/`, `ext/demo`. 042 mobile tabs are tricky (h-scroll or dropdown), documentation two-level nav (Getting Started / Concepts / API), `directory.json` vs manual sub-page imports vs Vite, app tabs like VS Code, "should BACK always work?", inner tabs tricky, simple menu, HashRouter → link `ext/tabs`, `ext/Doc`, `core/Router`, `/imagine/paging/navigation/`. 043 sidebar topic vs site, global nav optional, left sidebar (home link, user at bottom, full height), desktop display pages, `export default page()` does not auto-render, HashRouter imports all pages?, TABS vs BREADCRUMBS ("show all at 1 level" vs "show deep path", "bad = jumpy UX"), breadcrumbs + tabs → link `core/Sidebar`, `/imagine/paging/navigation/` (stable vs dynamic — the "jumpy UX" line is its thesis), `ext/tabs`. 044 `lew42.com/framework/` vs `framework.lew42.com`, basic web dev via codepen, one page to document all the styles, `@util` on top of defaults, gitpm → app, marketing (YouTube videos), monorepo vs individual sites, "OOP course + responsive web design = freedom", "moving from this v0.0.0 monorepo towards a full featured framework = a lot of decisions" → link `styles/` (the one styles page), the root readme, `/blog/`. 045 page previews & columns ("why does this resist me?"), variants/examples/code, sections `section.c(`, `page.md`, themes (switch the class on body), dev sidebars ctrl+\, `page({icon, title, meta, render, subpage})`, `pg.add?`, `pg.sub("one","two","three")` auto-import, `export default section.c("s34", …)` auto-renders, wrap in a page to stop auto-render, "can each page be a pager, activate itself? → root page is the pager", `/dum/page.js` → link `core/Page` (`children:` auto-import IS `pg.sub`), `core/Page/overview/columns`, `ext/demo`. 046 importmap → jsdelivr vs local, pinned versions, monorepo messy, `npx servex`, site-repo = public/index.html + app.js + page.js; jsdelivr means no access to framework files, bump version breaks things, "aside from hosting/versions the main objectives: document View & App usage, setup the lew42/site starter, testing, ui/ux, page/pager, router" → link the root readme, `core/View`, `core/App`. 047 GitHub CDN for sites, core/Page → Pagers → Router, generate /framework/ w/ node, git submodules, `v0.0.0 ▾ → /v0/0/0/`, "is import & pg.link() the best?", monorepo → lew42.com how?, local dev strategy, importmap → CDN or cdn.localhost, localhost cdn that fetches from GitHub → link `core/Router`, the root readme (deploy). 048 "what's the fix? easy fs w/ vc, trees w/ subversions", w/o npm the minimal site objective is harder, "having these guys work autonomously isn't going to work… I might need to do most of the decision making", too many patterns to exemplify, DESIGN × DEV nav sketch, merge pages, make notes & nav; typography/prose/content list, "would most sites only need /app.js?", framework shouldn't be edited per site, CDN ⇒ no import maps, `/dev/Thing/page.js` → link `/framework/ai/` (the autonomy line vs today), `styles/elements/`, `core/Page`. 049 UI ↔ AI ↔ Server, monorepo works for devs w/o npm, site subject to bundle or importmap, `npm install @lew42/server`, vite-site, `loader.js → fetch package.json → dynamic import map`, "first type=module script must be req AFTER dynamic import map"; `lew42/monorepo: Server, Servex, public/framework/core, /ext, public/package.json workspaces @lew42/core @lew42/thing`, "Build Now, Architect Later", lean into npm, servex global, "framework could load from anywhere? local servex-based cdn" → link the root readme, `Server/`'s readme page if one exists. 050 "FORGOT TO BRANCH (repair)", framework/Server/Servex coupled?, "if ΔB means ΔA you might not want 2 repos", multi-site usage, site-a/site-b consume npm? npm link? file:; "LIFE = potential (for anyone who wants to work for it) — it shouldn't be so hard to pay rent", html importmap `@lew42/core → node_modules/@lew42/core/index.js`, deep imports, npm pkgs cannot have "/", ext → one pkg or many, git granularity vs npm granularity → link the root readme (branches). 051 git submodules?, "Class Builder = hashed string composer = git?", super file system, focus on CSS & layout & basic UI, "just put it all in framework/", main is for the simple template starter, fork the whole repo for addons, `framework.lew42.com` importmap cons; "do all imports after an index follow importmap?", whole framework hosted locally like a CDN, updates "live"; NAVIGATION: links that don't break = import & `pg.link()`, ignorant routing (only routes that are imported; fallback), "in essence this fw is just slightly better than static html; however it has potential", SPA-like pushState via auto-import, `app.pages.get("/one/two")` → link `core/Router`, `core/Page` (`link()`), `ext/LiveReload` or the livereload research. 052 STRAIGHT TALK / the site / web of lies / game theory, memes → $ → levels → skills → outcomes → experiences, a product named with the owner's surname (reduce), "Draw anything (quickly)", merge framework + server + framework-site into one generic site repo, use org w/ perms, org ↔ local, dev branch deploys; Framework: `import { el, div, p } from View.js; div("hello world") ← new View`, index.html → app.js uses `window.location.pathname` to load `/path/page.js`, app awaits the import of page.js before injecting, static compat host anywhere, no bundler pure static, lew42/framework "all the code is in one place; if you fork it you can't push/pull updates independently → BAD", framework → CDN? → link `core/View`, `app.js`'s doc, the root readme, the levels verdict. 053 `app.page = current page?`, `page("Title", () => {})`, PERFECTLY SIMPLE SYNTAX, types of pages (extend Page? `new Page({…})`), `page("Name", {preview(){}, title, icon, img, layout: Layout.Four, classes})`, default pager, `app.pager?`, "does each page need its own pager? sounds complicated"; BASICS: routing/tabs aren't hurting, TYPOGRAPHY `dum.p1()`, h1/h2/p stacks, blockquote/callout/card, dated entries (a July 2026 date and times — keep the dates, they are the owner's), `page("One")` vs `h1("One")` vs `export default page(` auto-render vs not, "Level 1 = free, Level 2 = $10/mo unlimited" → link `core/Page` (the blessed shape), `styles/elements/`, the levels and payments verdicts. 054 typography → site → portfolio, 3-col grid → visual nav (preview, clickable, interactive), typographic rhythm (`* + *` margin-top, the lobotomized owl), sections w/ flex & gap "most Figma-like but pita", `:is(hx):not(:first-of-type)`, `.prose`/`.markdown` opt-in, "page ≠ prose", `app.$root.ac("page")`, `app.$layout.ac("full")`, bg > layout > page, "to get dashboards & content out of the same system", markdown w/ nav, `md({file})` → link `framework.css`'s doc (`.flow`, the rhythm rule), `core/Page/Page.css`, `ext/markdown`, `/imagine/design/spacing/`.

Build to match where a sketch is UI: 041's element browser as a small four-tab wireframe; 043's tabs-vs-breadcrumbs as two tiny frames; 045's page-preview sketches as three small cards; 054's 3-col grid as a strip. Everything else is transcription and links.

## Prove it and fences

As the first brief: every page and the index at four widths, zero console errors, every link resolves, images upright and web-size. Use the shared server `http://localhost:8123/` — start none, kill none. Write `public/notes/<your slugs>/**`, your slugs in `public/notes/page.js`, this task dir. Budget ~300k tokens. Report in ≤ 10 plain lines: your sixteen slugs as "slug — one sentence — what it links", and the shared-file edits you made.
