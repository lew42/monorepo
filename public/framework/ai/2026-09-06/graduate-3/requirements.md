# graduate-3 — slice 3: one copy of every word list, the arrangement word points at the layout tree, the doc page (Opus)

Three laws: less is more — one copy, deleting beats adding; clear beats brief, by far — the doc page is for the overwhelmed newcomer; prioritize. Length budget: the doc page is one screen with one page file shown before/after; the report is 10 lines.

Read first: the repo's `CLAUDE.md`; `../mastermind-graduate/requirements.md` (the calls) and `../mastermind-graduate/layout-brief.md` (the owner's layout brief — the arrangement catalogue IS the layout tree); `../../2026-09-04/mastermind-platform/minion-rules.md`; **the plan `../graduate-plan/plan.md`** §1 (the map), §3 slice 3, §5; **slice 2's landing `../graduate-2/task.jsonl`** — read every log line, especially the three refutations (core has the word CLASSES but not the word LISTS; the frame draws pages while the stage draws demos; `grow()` stays) and the Cross break (the `<34em` stacking query in `Page.css` wrecked 49 cells; a `container-type: normal` rule patched it in the lab); **slice A's landing `../layout-a/task.jsonl`** (`core/Layout/` — thirty layouts with ids; `tree.js` is a near-copy of `ext/catalog/browse.js`); `core/Page/Page.class.js`, `Frame.js`, `Page.css`; `imagine/paging/blocks.js`, `imagine/sections/sections.js`, `imagine/layouts/system.js`, `styles/sections/tone.js`. Skills: `new-task` (this dir, group `paging`), `code`, `css`, `new-css-class`, `layout`, `documentation`, `finish-task`.

## The job — five things

1. **The word lists move into core.** `core/Page/words.js` exports the five lists the lab keeps in `blocks.js` (navigation, width — with `room` as the alias, arrangement, surfaces for both colour words, type size), each entry `{ key, label, means }` exactly as the lab's control surface needs them, so `blocks.js` becomes a re-export and `imagine/sections/sections.js` and `imagine/layouts/system.js` import from `/app.js` (or the core path). The lab's own `CONTENT`, `BLOCKS`, `CONTROLS`, `DEFAULT` stay. After this, `grep -rn "SURFACES\s*=" public/` finds ONE definition. `styles/sections/tone.js`'s `TONES` stays four words and gains the one comment the plan asks for.
2. **The arrangement word points at the layout tree.** Each arrangement entry names the `core/Layout` id it compiles to (the plan's `LAYOUTS` / `layout_of()` move to core with the word), and the lab's arrangement control links each value to `/framework/core/Layout/<id>/`. The lab's seven values keep their names.
3. **The `<34em` stacking query is scoped.** Core may stack a frame's rail and panel under 34em ONLY for a frame that is the page's own (not one nested inside a cell): find the selector that hit Cross, scope it (`:not` a nested frame, or a class the page-level frame carries), remove the lab's `container-type: normal` patch, and prove Cross's 49 cells and the throwaway-style six-word page both hold at 400.
4. **`tree.js` folds back.** The three facets and the 60-card cap that `core/Layout/tree.js` copied from `ext/catalog/browse.js` move INTO `browse()` as options (facets read page props; the cap is a number), `tree.js` is deleted, and the layout tree calls `browse()`. Every other `browse()` caller renders byte for byte (list them by grep; pixel-diff three).
5. **The doc page.** `core/Page/doc/words.md` (or the name the `documentation` skill says): one screen — the six words in a table (word · what it does · the values · what it maps to in core), then ONE page file shown before and after it says three words, then a link to the layout tree and to the paging lab. `core/Page/readme.md` gains one line pointing at it. Register the doc so it appears on `/framework/core/Page/`'s Docs tab.

## Prove it — every page, not six

The site has 568 pages in `core/Search`'s corpus — crawl ALL of them at 1280 (and the paging realm, `/imagine/sections/`, `/imagine/layouts/`, `/framework/core/Layout/` at 400 / 1280 / 1920 / 3440), zero new console messages against a before-run (the known probe 404 stays). This morning's slice missed 38 broken pages by crawling six; do not repeat it. The nine-step use test from `../../2026-09-05/paging-audit-8/requirements.md` still passes. Two numbers that must agree: definitions of each word list before (by grep) and after (one each).

## Fences and budget

Write: `core/Page/words.js` (new), `core/Page/Page.css` (ONLY the stacking-query scope — read the file fresh immediately before the edit; another minion is adding one rule beside `.page-previews`), `core/Page/doc/**`, `core/Page/readme.md`, `core/Page/Page.class.js` only if the lists need a hook there (say so), `imagine/paging/blocks.js` and the arrangement control's file, `imagine/paging/paging.css` (remove the patch), `imagine/sections/sections.js`, `imagine/layouts/system.js` (and, if you delete `LayoutsCard.js` / `number.js` because `core/Layout` replaces them, their importers in that realm — the plan §5 lists them), `styles/sections/tone.js` (one comment), `ext/catalog/browse.js`, `core/Layout/tree.js` (delete) and `core/Layout/page.js` (call `browse()`), this task dir. Never `@layer util` in `Page.css` beyond the one scope, never `Page.css:280–390`, never `imagine/paging/words.js` or `baseline.js`, never `public/notes/**`, never `ext/Playground` (another minion deletes it). Shared server `http://localhost:8123/` — start none, kill none. Never `find /`; never spawn agents; never `git stash`/commit. Budget ~400k tokens. Report in ≤ 10 plain lines: one line per thing with its proof, the crawl numbers, what you doubted.
