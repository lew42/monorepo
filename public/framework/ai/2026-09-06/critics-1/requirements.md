# critics-1 — two critics walk what today built (Opus × 2, one dir each)

Same method as the paging loop that reached the bar yesterday (`../../2026-09-05/paging-audit-8/requirements.md`, the rubric at `../../2026-09-05/paging-audit-2/requirements.md`): name your exit condition FIRST, walk, say whether it is true, then a ranked FIX LIST or "cosmetic only". The owner's bar: *simple, powerful, organized, intuitive, easy to browse, infinite potential.* CLAUDE.md's Presentation rule: level 1 is one screen, shown; the overwhelmed newcomer.

**What landed today** (read `../mastermind-graduate/task.jsonl` — every `agent` line with an `outcome`): core Page learned the six words; the paging lab runs on core's frame and goes straight into the demo with an address; `core/Layout` is a browsable tree of thirty layouts with a seven-width strip and fixtures; `core/Section` picks a fitting layout; `core/Search` lives on its own page; the size standard (one `--size` knob, `cqi`); one control grammar; the notes program (66 pages + doodles); the fat nav, bled walls, tabs, the rail pad, forty erroring pages, the server spin — all fixed at the cause.

**Critic A — the overwhelmed newcomer** (this dir): cold walk at 1280 and 3440 with screenshots, in this order: `/` → `/framework/core/Layout/` (open one layout, drag its handle, read the strip) → `/framework/core/Section/` (turn editing on, pick a layout) → `/imagine/paging/` (the six words, the path bar, click Overview, change Content, nest a page) → `/framework/core/Search/` (press `/`) → `/notes/` (open two notes). Score the owner's six words 1–5 for the WHOLE (not per realm), with one sentence each saying what earned the number. The use test: nine steps you write before walking (a newcomer's goals — find a two-column layout that fits a rail, make a section use it, build a page from six words and send its link, find a note about tabs). What still cannot be done. The ranked FIX LIST (file:line where you can), or "cosmetic only" first. And one plain paragraph for the owner, under 80 words: what this site is now and how to use it.

**Critic B — the systems designer** (`../critics-1b/`, create with `new-task`): three conditions named before walking — (1) ONE copy: every word list, every control size, every spacing token has exactly one definition (grep counts); (2) ONE seam: the paging lab, `core/Section` and the layout tree all render through `Page.Frame` and the six words, with no parallel implementation left (name any); (3) the proofs hold: the crawl of all 631 corpus pages at 1280 has zero console errors right now, the 20-page spacing medians match the size study, the controls page reads one height. Then the honest closing question: what would a reader notice, and what is authors' tidiness. The ranked FIX LIST or an empty one with the reason.

## Rules

Fix nothing; change nothing under `public/` except a scratch `.md` in your own task dir; delete any page you make. Shared server `http://localhost:8123/` — start none, kill none; one browser, ≤ 4 pages; Playwright at `file:///C:/Users/mike/AppData/Roaming/npm/node_modules/playwright/index.mjs`. Never `find /`; never spawn agents; never `git stash`/`git rm`/commit. Budget ~250k each. Report in ≤ 10 plain lines plus the list.
