# A note written during slice 2

This file did not exist when the paging realm was written. It is here so the
nine-step use test has **content the tester wrote** to put in a box — step 6.

If you are reading this inside a page's content box, the `content` word took an
address, fetched this file, and drew it. That is the ninth answer to *what is in
the box*, and it is the one that is not a list.
