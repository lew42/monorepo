# mastermind-run-4 — the Workspace build (2026-08-19)

Laws: less is more · clarity · prioritize. Run task for the `ai-ops` group; continues [mastermind-run-3](/framework/ai/2026-08-18/mastermind-run-3/) (the evening of 08-18, landed).

## The ask (the owner, 2026-08-19, condensed — each quote is verbatim in the task briefs)

Playground at `ext/Panel/playground/` (+ new document, left rail = logo + "Framework" + documents, no ✕) · the main area is a **Workspace** (zoom + px, owns an optional shell, viewports one/all/twin, Fill vs Fit, the right rail as the responsive handle, documents as files) · **no container queries for now** · keep `zoom_scrub` and the scenes · **race to an MVP** · a Demo tab (UX demos as flows beside follow-alongs, flex and grid from scratch first) · groups (first click selects the outer group; hover wakes inside a selected group; drag whole sections; centre-drop nests INTO an empty panel with padding) · `pad`/`gap` words with a knob · docs must carry every answer.

## The plan (the approved design's order)

A sizing (Opus, landed 11:45) → B Workspace + documents ∥ Demo tab ∥ Groups (Sonnet) → C playground + viewports ∥ D split verb + sow (Sonnet, after B) → pad/gap (after D + groups). Briefs: `workspace-a-sizing/`, `workspace-b-module/`, `panel-demos/`, `panel-groups/`, `panel-pad-gap/`; C and D written when B lands.

## The owner's input today, in order, verbatim — the master record (each also lives in the task brief it shaped)

1. *"I'd probably avoid containers for now... I'd like to explore that later, but I ran into some funky side-effects of using containers, and I don't want to wonder if that's the problem. Yes, begin."* → `workspace-design/`, `workspace-a-sizing/`
2. *"adding a new column or row is fundamentally different from splitting an existing one […] splitting a panel should reuse the style of that panel […] adding a panel next to an existing one can create a similar layout, but we're starting from scratch - choose a new section/style."* → `workspace-design/` §5, `workspace-d-verbs/`
3. *"the Panel/full/ page has the panel-flow bar at the bottom of viewport, but the panel area is like... 1/6th of the available space"* → fixed inline (panel.css: document sections `flex: 1 0 auto`); *"ok, full height restored"*
4. *"the 'roll a layout as panels' function works, but it's all 'split mode', and so we get like 12 scrollable mini panels […] in our layout skill, make a note about paying attention to scrollbars, and to avoid them unless desired."* → layout skill; `workspace-design/` addenda; `workspace-a-sizing/`
5. *"i like the zoom scrub, why delete? if it's not hurting anything leave it for now. the decorative scenes are fine […] unless it conflicts.. now, on the layout: flex 0 0 auto, I'm not sure […] We need clarity […] let's race to the finish line. Get an MVP working"* → `workspace-a-sizing/` (amendments + addenda)
6. *"how do we introduce fixed-ish sizing? […] fixed-aspect-ratio boxes […] fixed (basis) sidebars […] make sure the documentation shows it"* → `doc/sizing.md` via `workspace-a-sizing/`
7. *"create a new Panel page tab (demo) […] classdoc style inner left nav […] a starting panel, and a description of what to do […] demos are not about the code, more about the UX […] a note in the Panel readme to demo all features […] panel flow feature can be adapted for demos […] split screen demo: left side is the prebuilt panel-flow demo, the guide, and the right side is just the starting point"* → `panel-demos/`
8. *"i'm assuming you're spawning minions to work faster and keep token costs down?"* → yes; run log
9. *"we need grouping. I would recommend a PanelGroup extension […] sections […] the first layer of groups […] first click selects parent, always, second click allows selection of next level"* → `panel-groups/`
10. *"let's focus on a demo for simple flex and grids […] each demo needs a workspace, to configure the available space, center the root panel? maybe the panel-flow should be a workspace-configured feature?"* → `panel-demos/` addenda, `workspace-b-module/` addenda
11. *"how do I just add a panel to a panel? […] do they use the Item/List system […] having panels within panels... is this possible?"* → `panel-groups/` addenda
12. *"are you able to simulate drag and drop events? […] maybe it's because it just gets added to the parent's panel […] we need modular 'pad', 'gap' (possibly more) panel modules. when present, the button turns into a section, and has a widget to edit."* → `panel-groups/` addenda, `panel-pad-gap/`
13. *"when dragging one section into another empty panel, i get this inset drop indicator. what I would expect […] the dragged panel goes inside the transparent bg empty panel […] with padding added."* → `panel-groups/` addenda
14. *"i don't see Panel > Demo page yet? can we get it roughed in? I'd like to SEE progress. I don't see Panel > Workspace yet, can we get a page for that?"* → `panel-demos/`, `workspace-c-playground/`
15. *"maybe demos aren't done yet, but i don't see any panels on these pages"* → `panel-demos/` follow-ups (bleed, visible tones, wrap)
16. *"it seems the mastermind-run-3 task is stale?"* → run 3 landed; this run opened
17. *"do a sweep of all the popover menus, many get clipped, but also, there are too many little icons […] don't have a Wrap > Wrap/NoWrap drill down […] a strong note in readme.md to be careful with selection/deselection […] grouping, multi-selecting, multi-editing […] remove most of the overlay buttons […] the template switcher - should be a dropdown (ext/Dropdown) […] a block, flex, grid display icon? or a native select"* → `panel-bar-sweep/`
18. *"make sure you're adding all my input to the requirements for the task(s), so we don't lose any of these"* → this list; addenda appended to every brief that was only relayed by message.
19. *"the panel, especially a FlexPanel (consider extension classes?), GridPanel, etc.. should handle sub-panel UX […] they should at least be selectable, so the sidebar can display flex/grid properties per item"* → `panel-items/`
20. *"switch Panel>Workspace tabs to left inner sidebar tabs, like Panel > Demo"* → done by the mastermind: `Workspace/page.js` `render()` override (doc-section + vertical rail); the other four nested Docs await a Doc-wide rule (the owner's call)
21. *"we need these vertical tabs and sub pages to bleed (not necessarily full, bleed, we want padding...) but they're locked down […] i tried .tabs.vertical.bleed.pad, but it makes the inner left sidebar misalign with the page title […] make a note in the layout skill that we want to maintain the ability for any page/section to 'bleed', when possible […] at least want to be able to opt-in to any available space"* → `ext/tabs/tabs.css` `.page > .tabs.vertical { grid-column: wide }` (replaces the 78em cap); layout skill Q2 note
22. *"remove the padding on the playground page, the sidebar looks weird inset"* / *"that sidebar should be full-height"* → `playground.css` `--page-pad: 0`; `Rail` → `PlaygroundRail` (the `.rail` region class collision); code skill trap
23. *"sometimes, when clicking a panel, it doesn't select... sometimes the right sidebar flickers. often a second click works...?"* → `text.js`: a click on text focuses the panel first (drill: group → leaf → run); the run only inside the already-focused panel; re-click hands the rail back to the panel. The other half is by design: a document root's sections are groups (first click = the section, second = the leaf) — readme/selection-care notes in `panel-bar-sweep/`
24. *"move the edge hover + button over a bit more, it interferes with underlying buttons"* → `insert.css` `--insert-start: calc(var(--panel-bar-h) + 0.7rem)` — the stub starts past the bar on both axes
25. *"the template switcher dropdown doesn't seem to do anything, and should have a 'thin' scrollbar"* → relayed to `panel-bar-sweep/` (pick must repaint — the old bar called `roll()`; list `overflow: auto; scrollbar-width: thin` + max-height)
26. *"the 4 tone tiles are currently a 2x2 grid, they should be 1x4 to utilize space better"* → relayed to `panel-bar-sweep/` (`glyphs.js` tone `cols: 4`; every row of ≤ 6 pictures on one line)
27. *"these root demo panels, they don't have resize grips on the root panel? i'm thinking: panels should probably default to default 'div' behavior - auto-height, fill width... you could give a panel a min-height to prevent it from collapsing. but, the edges should still have resize handles. maybe for a default situation, the top and left resize handles aren't useful (they wouldn't work properly anyway), but right and bottom? also, right click should reset the resize handle's effect. that way, for any panel in 'flow', adding a panel or section or content within should automatically grow the panel(s) themselves, not just crop/scroll"* → `panel-flow-sizing/` (Opus): default `h: hug` + `--panel-min` floor, `w: fill`; right/bottom edge = click split · drag resize (fixed `w_at`/`h_at` in em) · right-click reset; demo panes hug; adding inside grows
28. *"In the 'Add a column, then a row' demo […] splitting the root panel into 2 columns works as expected. But then, splitting either in 2 (which […] shows the panel getting split again in 2 (25% of the original panel), we click, and that's not what happens -> we get 3x 33% panels, instead of 25, 25, 50. the crux here, is whether we're splitting a nested panel, or adding a panel to the parent. also, if we're truly splitting the parent, you'd think, logically, that the parent would transfer its current 50:50 columns into one side or the other, and again arrive at a 25, 25, 50 split, via nesting. Really focus on this dynamic.."* → addendum to `panel-flow-sizing/` (a split halves the struck panel's share — the result must equal the preview; flat halving vs nesting decided in decisions.md)
29. *"the + button on some of our list templates is nice, but should probably only appear when hovered. And, that hover should be restricted to when the parent is selected. So, for example, while hovering, each panel's border should change a bit (it doesn't now, we only get the subtle icon button overlays). whenever hovering, we should get a strong visual feedback of what will be selected when we click. it seems sometimes this doesn't happen. write that down..."* → `panel-selection/` (hover = what a click selects, via the same drill(); `+` on hover only)
30. *"i'm still getting selection state issues.. we really need to lock this down... right sidebar: 'nothing selected', but an orange border on the last selected panel that doesn't disappear (and clicking off doesn't help)"* → `panel-selection/` (Opus: one selection per page across every root; rings cleared through the live views; clicking-off decided)
31. *"add a right drawer selection for the document itself in the playground, and add a red delete button at the bottom of the drawer, with an 'ok' accept prompt"* → done by the mastermind: the bar's document name selects the root; the drawer's document block (name + red Delete behind `confirm`); `default` never offered; the playground goes home after a delete
32. *"template: word ('lew42') should be caps 'LEW42'"* → `templates.js` word → LEW42
33. *"using the template switcher still doesn't update the screen... only on reload"* → *"nevermind, now it does? i think i had a stale tab"* — the sweep's fix held; the underlying one-view-per-item `views` map (paint.js) is still the sweep's open proposal
