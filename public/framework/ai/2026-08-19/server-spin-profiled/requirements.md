# server-spin-profiled

**Ask (verbatim):** "fans are spinning" · "fans again?" · "what caused it? did you fix it?"

**Scope:** find and stop whatever burns CPU; for the dev-server spin (`node server.js` at ~140%), profile before killing — the evidence `Server/doc/spin.md` has never had — then write down what the profile and the follow-up measurements say. Leave a tool that names the flooding path next time.

**Files:** `Server/doc/spin.md` (this task), `~/.claude/skills/fans/` (watchspy.mjs + one SKILL.md line). Nothing else in the repo.
