# workspace-c-playground — task C: the playground page and the viewport set (Fill · one · all · twin · Fit · zoom)

Laws: less is more · clarity · prioritize. **Deliverable: `/framework/ext/Panel/playground/` (left rail: logo + "Framework" as the way out, the document list, "+"; main: the Workspace with its bar complete; right: the drawer docked, its grip the handle), the viewport set in `ext/Panel/Workspace/viewports.js` (one / all / twin, Fit vs Fill, zoom + px readout lifted from `ext/demo/stage.js`), `Workspace` and `playground` as child tabs of the Panel page; proven headless; final message ≤ 15 lines.** Sonnet. Task B has landed; read `ext/Panel/Workspace/readme.md`, `Workspace.js`, `documents.js`, `workspace.css` first, then [`../workspace-design/design.md`](../workspace-design/design.md) §3, §6, §7 (C row).

The owner, verbatim (2026-08-19): *"ext/Panel/playground/ has a + button, where we can create a new document […] a left sidebar with all available documents, we don't need this X button […] Just put the framework sidebar's logo + framework header on the playground sidebar, so we can jump back out […] zoom + rendered px width […] switch between viewports (mobile, desktop, mega, fluid/manual), or do all viewports simultaneously (zoomed to fit, of course), or do the responsive side-by-side thing […] a medium gray, 1em border around each viewport […] maybe we need a 'fit' button, so they auto-zoom? I suppose the right sidebar can be the viewport's responsive handle […] The normal mode should just be 100% (no zoom), auto-width (fill width)."* And: *"I don't see Panel > Workspace yet, can we get a page for that?"* — so `ext/Panel/page.js` `children:` gains `Workspace` and `playground` (the demos minion added `demo`; make a minimal string edit).

## Build

1. **The playground page** — `ext/Panel/playground/page.js` (+ `playground.css`, `readme.md`, `doc/decisions.md`): a whole-window layout (reuse `styles/layouts/full.js`'s shape or its `.layout-full` class — but NO ✕: the left rail's logo + "Framework" link to `/framework/` is the way out); left rail = core `Sidebar` if it composes (`core/Sidebar/readme.md`), else a `flex v` column: logo/header, then `documents.list()` as links (the open one lit), then `+` → `documents.create()` then open it; main = `new Workspace({ saver: documents.open(name), flow: true, … })` with its bar; right = `ext/drawer` docked at load (`dock()` exists in `ext/Panel/tools.js`), its `ext/grip` is the Fill-mode responsive handle (zero new code — the drawer pushes; ⚠ `.layout-full` opts out of the push and restates the reservation: apply the `--rail-push` token `ext/drawer/readme.md` calls "proposed, not applied" — one token in `drawer.css`, read by `.layout-full` and `.app`; say what you did). The url carries the document: `/framework/ext/Panel/playground/<name>/` via `route(name)` (the day page's pattern), default = `default`.
2. **Viewports** — `Workspace/viewports.js`: `fill` (default: 100% zoom, the root fills the available width — the handle is the rail), `one` (a device width: mobile 390 · tablet 810 · desktop 1440 · mega 3440 — reuse `stage.js`'s list if exported, else the same numbers), `all` (the four tiled), `twin` (390 beside 3440, both live — `ext/demo/twin.js`'s `pane()` idiom: export `pane`/`twin(fn, devices)` from twin.js, ≤ 10 lines change). Each viewport is a VIEW of the one root (B's `mount()` keys boxes by root — N boxes, one tree) inside a 1em medium-gray frame (`--line`-ish, `panel-viewport` class) with its px width labelled on the frame. **Fit**: each non-fill viewport `zoom`s to fit its cell (`stage.js`'s `simulate($view, width, room)` — import it, do not wrap `stage()`); **100%** unzooms; a manual zoom knob + px readout lifted from `stage.js` (`magnifier()`/`ruler()` are private in its `tools()` — export them, `demo`'s strip unchanged). All of it in the Workspace bar's slot B left.
3. **Docs**: Workspace readme Use/Watch-out lines for viewports; `doc/viewports.md` (one topic); playground readme (index shape); Panel readme one line pointing at the playground; `ext/demo/readme.md` one line for the new exports.
4. **Prove headless** (`file:///C:/Users/mike/AppData/Roaming/npm/node_modules/playwright/index.mjs`; scratchpad probes `c-playground-*.mjs`; live socket ONLY for documents named `probe-*` which you remove at the end; block it otherwise): `/framework/ext/Panel/playground/` loads, lists `default`, the Workspace shows the default document (panel count = `/framework/ext/Panel/`'s persisted workspace); `+` creates `untitled` and the url becomes `/playground/untitled/`; `all` → 4 frames with widths 390/810/1440/3440 labelled, Fit → each frame's `zoom` < 1 and no horizontal overflow at 1600; `twin` → two panes on one height; back to `fill` → one view, zoom 1; drag the drawer's grip 200px → the fill view narrows by 200px (two numbers equal); zero console errors; pngs `playground.png`, `all-fit.png`, `twin.png`. Remove `untitled` at the end (`documents.remove`).

## Fences

`ext/Panel/playground/**` (new), `ext/Panel/Workspace/viewports.js` (new) + `Workspace.js` (the bar slot + viewports wiring) + `workspace.css` + readme/doc, `ext/Panel/page.js` (ONE minimal edit: `children:` gains `Workspace playground`), `ext/Panel/readme.md` (one line), `ext/demo/stage.js` (exports only) + `twin.js` (export pane/devices) + `ext/demo/readme.md` (one line), `ext/drawer/drawer.css` + `styles/layouts/layouts.css` (the `--rail-push` token, smallest change) + `ext/drawer/readme.md` (one line), `ext/Panel/tools.js` NOT (read `dock()`), this dir. NOT `Panel.js`, `split.js`, `glyphs.js`, `toolbar.js`, `properties.js`, `focus.js`, `size.*`, `templates.*`, `demo/**`, `flow.js` (tasks D/E/pad-gap). ⚠ Task D adds ONE `+` button to the Workspace bar concurrently (a minimal string edit in `Workspace.js`) — never rewrite that file; edit by string replacement only.

## Rules

- Load `code`, `css`, `layout`, `new-page` once; `new-css-class` for new `panel-` names. Run `new-task` first (dir + brief exist; write `task.jsonl` line 1 and the `day.jsonl` line in `ai/2026-08-19/`; group `panels`); the ledger logs edits; `documentation` then `finish-task` (`"tokens": null`). A skill that misleads you gets one line in its `improvements.md`. Timestamps from the clock; forward slashes; never Out-File a `.jsonl`; never a person's name — "the owner". Every CSS rule inside a layer; no container queries; only `p()`/`h1`–`h6` read backticks; no DOM after an `await` outside a callback. Wait in the foreground.
