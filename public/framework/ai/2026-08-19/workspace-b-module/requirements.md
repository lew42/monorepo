# workspace-b-module — task B of the Workspace build: the Workspace class and documents-as-files

Laws: less is more · clarity · prioritize. **Deliverable: `ext/Panel/Workspace/` (Workspace.js · documents.js · workspace.css · readme.md · doc/ · page.js) — a class that HOLDS a Panel root, `workspace()` kept as its thin door (no caller changes), documents as `/data/panels/<name>.json` + an index the Workspace writes, the `/full/` route retired; proven headless; final message ≤ 15 lines.** Sonnet. Runs after task A has `landed_at` — wait for it: `while (-not (Select-String -Path public/framework/ai/2026-08-19/workspace-a-sizing/task.jsonl -Pattern landed_at -Quiet)) { Start-Sleep 20 }`. The spec is [`../workspace-design/design.md`](../workspace-design/design.md) **§1, §4, and the "N viewports = N views of ONE root" line of §3** — read the whole design once.

## Build

1. **`Workspace`** — a class (not `extends Panel`; it holds `this.root`, `this.saver`, `this.mode` (reads/writes `root.data.mode`), `this.zoom = 1`, `this.viewports = ["fill"]`, `this.$view`); `workspace(options)` in `ext/Panel/workspace.js` becomes `new Workspace(options).$view` (same options: `saver templates tools seed mode`) so `ext/editor`, `ext/files`, `space/compose`, `ext/Panel/page.js` change nothing. `mount(root, $root)` in `workspace.js` gains `$roots[]` (several boxes, one root) — `draw()` empties them all — task C will mount N viewports; you build the seam and prove it with two boxes of one root. The module-level `saver` const moves to `documents.js`.
2. **Documents** — `documents.js`: `list()` (reads `/data/panels/index.json` → `{ names: [] }`; static: `LocalStorageSaver({ key: "panels/index" })`), `open(name)` (a `FileSaver({ path: "/data/panels/<name>.json" })` dev / `LocalStorageSaver({ key: "panels/<name>" })` static), `create()` (mints `untitled`, `untitled-2`…; `new Panel({ saver })` with `data.mode = "document"`; appends to the index; saves both; returns the name), `remove(name)` (`FileSaver.delete()` exists — check; else say so). The index is seeded with `default` → today's `/data/panels.json` (no migration; `FileSaver` must accept a full path for `default`). Nothing reads `directory.json` for this (`Server/plugins/Directory.js:21` ignores `.json`).
3. **The Workspace bar** (its own, above the root, `panel-` prefixed classes in `workspace.css`, `@layer theme`): `mode` (fill · document — the same key `glyphs.js` WORDS already writes; this bar is a second writer), the document name, and a slot task C fills (viewports · fit · 100% · zoom). Keep it to one row. The root's words leave the hover-bar/rail predicate alone for now (design §8 says that deletes later, after C).
4. **Retire `/full/`**: remove `route(name)` and the `full` import from `ext/Panel/page.js` and the two `/full/` links (`page.js` prose, `readme.md`) — the playground (task C) is the whole-window view; until C lands, `/framework/ext/Panel/` keeps its inline workspace. ⚠ `ext/Panel/page.js` is also touched by task E (it adds the word `demo` to `children:`) — make minimal string edits only, never rewrite the file.
5. **`Workspace/page.js`** — a `Doc` for the module (`files:` `notes:`), one demo: `new Workspace({ saver: new MemorySaver(), seed })` mounted twice (two views of one root — split in one, the other follows). `readme.md` index shape (what · Use · Watch out · More); `doc/documents.md` (the file layout, the index, why not directory.json); `doc/decisions.md` (holds-not-extends; door kept).
6. **Prove headless** (`file:///C:/Users/mike/AppData/Roaming/npm/node_modules/playwright/index.mjs`; scratchpad `C:/Users/mike/AppData/Local/Temp/claude/c--Code-lew42-monorepo/a14ec0db-4e8c-4ce1-a14c-378e52ac01a0/scratchpad/`, probes `b-module-*.mjs`; block the dev socket `page.routeWebSocket(/.*/, () => {})` for everything but the documents test — for that, use your OWN names (`probe-1`, `probe-2`) against the live socket and `remove()` them at the end; `git status public/data Server/data` must be clean of anything but your own files, then clean): two views of one root — split in view 1 → panel count rises in view 2 (two numbers equal); `create()` twice → `index.json` lists `default probe-1 probe-2`; `open("default")` loads today's document (panel count = what `/framework/ext/Panel/` shows); `/framework/ext/Panel/`, `/framework/ext/editor/`, `/framework/ext/files/`, `/framework/styles/layouts/space/compose/` load with zero console errors and unchanged panel counts. One png: `two-views.png`.

## Fences

`ext/Panel/Workspace/**` (new), `ext/Panel/workspace.js`, `ext/Panel/page.js` (the route block + the `/full/` link only), `ext/Panel/readme.md` (the `/full/` line + one Use line for `Workspace`), `ext/Panel/doc/decisions.md` (one entry), `ext/Saver/*` ONLY if `FileSaver` lacks `delete()` or a full-path option (then the smallest addition + its readme line), this dir. NOT `Panel.js`, `split.js`, `size.*`, `templates.*`, `glyphs.js`, `toolbar.js`, `properties.js`, `flow.js`, `demo/**`, `playground/**`, `ext/demo/*`.

## Rules

- Load `code` and `css` once; `new-css-class` for new `panel-` names; `new-page` for the page. Run `new-task` first (dir + brief exist; write `task.jsonl` line 1 and the `day.jsonl` line in `ai/2026-08-19/`; group `panels`); the ledger logs edits; `documentation` then `finish-task` (`"tokens": null`). A skill that misleads you gets one line in its `improvements.md` (`skill-improvement`).
- Timestamps from the clock; forward slashes; never Out-File a `.jsonl`; never a person's name — "the owner". Imports flow down (`Workspace.js` imports `Panel.js`, `workspace.js` helpers, `documents.js`; nothing in Panel imports `Workspace/` except `workspace.js`'s door). Only `p()`/`h1`–`h6` read backticks; no DOM after an `await` outside a callback; every CSS rule inside a layer. Wait in the foreground.

## Addenda — the owner's later words, verbatim (relayed by message)

> maybe each demo needs a workspace, to configure the available space, center the root panel? maybe the panel-flow should be a workspace-configured feature?

> I don't see Panel > Workspace yet, can we get a page for that?

Mastermind's reading: `Workspace({ flow, height, center })` options (built); the Workspace page joins the Panel tabs in task C.
