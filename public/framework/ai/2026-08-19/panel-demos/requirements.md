# panel-demos — a Demo tab on the Panel page: a walk-through of the UX, each demo a flow beside a follow-along

Laws: less is more · clarity · prioritize. **Deliverable: `ext/Panel/demo/` — a `demo` tab on `/framework/ext/Panel/` with an inner left nav (one entry per demo), each demo = a prebuilt replayable panel-flow on the left (the guide) beside a fresh starting workspace on the right (follow along), with one short description of what to do; the flows recorded as JSON in the repo; a readme note to demo every feature; proven headless; final message ≤ 15 lines.** Sonnet. Runs after task A (`../workspace-a-sizing/`) has `landed_at` — wait for it: `while (-not (Select-String -Path public/framework/ai/2026-08-19/workspace-a-sizing/task.jsonl -Pattern landed_at -Quiet)) { Start-Sleep 20 }`.

The owner (2026-08-19), verbatim:

> create a new Panel page tab (demo). use the classdoc style inner left nav for each demo. for each demo, add a starting panel, and a description of what to do. create a series of demos that walks us through exactly how this thing works. demos are not about the code, more about the UX... make a note in the Panel readme to demo all features. maybe the panel flow feature can be adapted for demos? then, you have a split screen demo: left side is the prebuilt panel-flow demo, the guide, and the right side is just the starting point, the workspace, the follow-along portion.

## Build

1. **The tab.** `ext/Panel/page.js` is a `Doc`; a Doc takes `children` as tabs (see how `ext/Doc` renders `api/`, `doc/`, `files/` and how another module adds a child tab — grep `children:` in `ext/*/page.js` for a Doc with one). Add `ext/Panel/demo/page.js` (a Page with its own `children:` = the demos, each an inline child page or a dir — pick the smaller; an inline `children: [ {...}, ... ]` object list like `styles/layouts/word.js` does is fine) and the word `demo` to the Panel Doc's `children:`. ⚠ `ext/Panel/page.js` is ALSO being edited by task B (it removes the `/full/` route block) — you may make exactly one minimal string edit there (add the word to `children:`), never rewrite the file; if B has not landed, `git diff public/framework/ext/Panel/page.js` before and after your edit must show only your line.
2. **The inner left nav** — the "classdoc style": the rail ext/Doc draws for member pages (`ext/Doc/Doc.js` `member_page`/section rail, the Docs tab's left list). Reuse that shape — a `Sidebar`/nav listing the demos in order, the current one lit — do not invent a third nav. If the Doc's own child-tab mechanism already gives a rail for a child's `children:`, use it and say so.
3. **The demos, in this order (UX, not code; one screen each; the description ≤ 4 lines):** 1 split a panel (edge click → ghost → commit) · 2 add a column, then a row · 3 resize a seam (drag; it is a ratio) · 4 fill · hug · fixed (the rail's width/height rows; what each does to a sibling) · 5 a leaf's flex words (`dir gap wrap justify items` on the `cells` template) · 6 grid words (`cols gap dense`) · 7 document mode: sections and scrolling (root `mode`, split below appends) · 8 swap content (templates, tones) · 9 roll a layout / a preset (`sow`) · 10 drag a panel (edge and centre drops) · 11 close and absorb · 12 the flow scrubber itself (step back, build from a step). Each demo page = `div.c("flex gap")`: LEFT `panel()`/`workspace({ saver: new MemorySaver() })` mounted with a **recorded flow loaded and the scrubber visible, recording OFF** (the guide — step through it); RIGHT a fresh workspace with the same starting point, recording ON (follow along); above them the description; `--panel-height` ~ 24em. Reuse `ext/Panel/flow.js` — it has `Flow.mounted`, `save()` (hands back the step array); **add the minimum it lacks**: a `load(steps)`/`replay`-only door and a way to mount the scrubber on a given workspace (read `flow.js` + `doc/flow.md` first; keep the change ≤ 30 lines and document it in `doc/flow.md`).
4. **Record the flows headless, once, into the repo**: drive each demo's gestures with Playwright (`file:///C:/Users/mike/AppData/Roaming/npm/node_modules/playwright/index.mjs`; scratchpad `C:/Users/mike/AppData/Local/Temp/claude/c--Code-lew42-monorepo/a14ec0db-4e8c-4ce1-a14c-378e52ac01a0/scratchpad/`, probes named `demos-*.mjs`; block the dev socket with `page.routeWebSocket(/.*/, () => {})`; use the no-saver demos/your MemorySaver workspace, never the persisted document), `flow.save()` → `ext/Panel/demo/flows/<n>-<name>.json` (12 files; small — a 6-step flow is ~1.5KB). Two numbers that must agree: steps recorded per flow (the file) and steps the scrubber shows when that page loads.
5. **Readme:** `ext/Panel/readme.md` — one line under More: "Demo — every feature, as a flow beside a follow-along: /framework/ext/Panel/demo/" and a Watch-out-style note "a feature without a demo is undemonstrated — add the flow when you add the feature". (Task A edits the readme too and lands before you start.)
6. **Prove:** every demo page loads at 1280 with zero console errors; the left scrubber shows N/N steps, stepping ⏮ then ▶ three times changes the left tree (panel count differs from step 1); the right workspace starts at the starting point (1 panel) and a real edge-click split there adds a step to ITS recorder, not the left one; one png of demo 7 and one of demo 12 in this dir.

## Fences

`ext/Panel/demo/**` (new), `ext/Panel/flow.js` + `flow.css` (+ `doc/flow.md`), `ext/Panel/readme.md` (your two lines, after A landed), `ext/Panel/page.js` (ONE minimal edit: the `children:` word), `ext/Panel/doc/file/flow.js.md`, this dir. NOT `workspace.js`, `Panel.js`, `split.js`, `size.*`, `templates.*`, `glyphs.js`, `toolbar.js`, `properties.js`, `Workspace/**`, `playground/**` (tasks B–D).

## Rules

- Load `code` once (and `new-page` for the page). Run `new-task` first (dir + brief exist; write `task.jsonl` line 1 and the `day.jsonl` line in `ai/2026-08-19/`; group `panels`); the ledger logs edits; `documentation` then `finish-task` (`"tokens": null`). A skill that misleads you gets one line in its `improvements.md` (`skill-improvement`).
- Timestamps from the clock; forward slashes; never Out-File a `.jsonl`; never a person's name — "the owner". Only `p()`/`h1`–`h6` read backticks; no DOM after an `await` outside a callback; every CSS rule inside a layer. Wait in the foreground.
