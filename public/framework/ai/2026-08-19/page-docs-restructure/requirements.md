# page-docs-restructure — core/Page: an "Old" tab with a left rail, the 15 demos out of the Overview, a 4th-level tabs experiment

**Laws first** (CLAUDE.md): less is more — ASAP; clarity is the one exception; prioritize. **Length budget:** the restructured pages ARE the deliverable; report ≤ 10 log lines + one headless screenshot of `/framework/core/Page/old/overview/` at 1280. Load the `code` skill before editing JS; `css` + `new-css-class` before the one small stylesheet.

## The owner's words (2026-08-19)

> my current core/Page pages are absolute trash… 12 simple examples on Overview that are all basically the same, slight syntax variants. Get those out of there. make a left inner navbar (.tabs.vertical, I believe, like the classdoc system). Move all these current pages (overview, navigation, children, previews, shell, page flow) to a new tab: "old". we're going to rewrite the page docs…
> demo.app() is a great tool, but the catalog preview bar just didn't work very well (the scrollbar on the rail just hovers there). I like the left inner nav… settle on top tabs -> left inner rail. that gives us 3 levels of navigation. a 4th level of "top tabs" could work, but you'd need to switch the inner and outer colors. try that out.

## Target

`/framework/core/Page/` (an `ext/Doc`) keeps its top tabs: **Overview | Old | API | Docs | Files**. `Old` is a section with a `.tabs.vertical` left rail (exactly the shape `ext/Doc/Doc.js` `section()` builds — copy it) whose items are the six moved pages: `overview nav children previews shell flow`. The Overview tab is the Doc's `content()` as it is today MINUS the paragraph beginning "**The cards on the left are the demos**" (the demos have left) — a one-screen placeholder; the follow-up task `page-docs-rewrite` replaces it with the visual grid.

## Fences

- You own `core/Page/**` EXCEPT `core/Page/Page.class.js` and `core/Page/Page.css` (another agent is renaming the class stamp `page-<name>` → `page--<name>` in those and in `ext/Doc/Doc.js` at the same time — use `page--` in anything NEW you write, and do not touch those three files). Link-only edits in the inbound files listed in step 4. Never `ext/Panel/**` (another session). Use the **Edit** tool outside `core/Page/`, never Write a whole file.

## Steps

1. **Move.** `git mv` each of `public/framework/core/Page/{overview,nav,children,previews,shell,flow}` into `public/framework/core/Page/old/` (create `old/` first). `core/Page/doc/file/*.md` are named after the `files:` list — `git mv` any whose source moved so the Files tab keeps its notes (`ext/Doc/readme.md`: `doc/file/<path>.md`).
2. **`core/Page/old/page.js`.** A Page (not a Doc): `children: "overview nav children previews shell flow"`, `title: "Page — old docs"`, `label: "Old"`, `description` one line ("The first page docs, kept while they are rewritten."), `render(){ return this.view ??= div.c("page doc-section", () => this.tabs().ac("vertical")).ac("page--" + this.name); }` — `ext/tabs` is already imported by Doc.js. `old/readme.md`: three lines, what and why. Run the `new-page` skill once for the shape.
3. **`core/Page/page.js`.** `children: "old"`; delete `overview:`; `files:` paths → `old/…` (and drop the `overview/*/page.js` lines, or move them under one `old/overview/…` run — keep the list honest); delete the "cards on the left are the demos" paragraph; everything else stays. ⚠ With no `overview:`, `Doc.overview_section()` still calls `this.catalog()` (`ext/Doc/Doc.js:59-80`) — load the page and LOOK: if the Overview draws a rail with only the intro card, override `overview_section()` in `core/Page/page.js` (the Doc readme: "a different shape overrides `sections()`") to a plain section that renders `this.content()` with no catalog. Smallest override that works; copy the `render()` shape from `overview_section()`, including `.ac("page--" + this.name)`.
4. **Inbound links.** 35 files point at the old paths — rewrite `core/Page/(overview|nav|children|previews|shell|flow)/` → `core/Page/old/$1/` in each. Find them: `grep -rlE 'core/Page/(overview|nav|children|previews|shell|flow)/' public --include=*.js --include=*.md --include=*.css | grep -v 'core/new' | grep -v '/ai/'`. Known list (relative to `public/framework/`, except `web/nav/readme.md` which is `public/web/`): `audit/doc/core-App.md audit/doc/core-Page.md audit/doc/core-Sidebar.md audit/doc/ext-demo.md audit/doc/ext-tabs.md audit/doc/styles.md audit/modules/core-App.md audit/modules/core-Page.md audit/modules/core-Sidebar.md audit/modules/ext-demo.md audit/modules/ext-tabs.md audit/modules/styles.md core/App/doc/decisions.md core/Page/children/page.js core/Page/doc/columns.md core/Page/doc/layout.md core/Page/doc/method/chain.md core/Page/doc/property/children.md core/Page/nav/page.js core/Page/overview/readme.md core/Page/previews/page.js core/Page/shell/page.js core/Sidebar/doc/decisions.md core/doc/decisions.md ext/DesignTool/audit/pages.js ext/DesignTool/knowledge/ideal-ranges.md ext/Doc/page.js ext/catalog/doc/decisions.md ext/demo/doc/file/sample.js.md ext/demo/doc/record.md ext/demo/page.js ext/tabs/doc/decisions.md faq/page.js styles/doc/decisions.md`. Also RELATIVE links inside the moved pages (`./overview/…`, `../nav/`) and `old/overview/readme.md`, whose instructions say "add the name to `overview:` in core/Page/page.js" — that line is now "add the name to `children:` in `old/overview/page.js`". **Two numbers:** files matching the old pattern before (35) and after (0).
5. **The 4th level — try it.** `old/overview/page.js` was the catalog rail's index (15 demo trees in three `group:`s). Under Old it is a rail item; give IT top tabs: `children: "page children add labels route shapes wall catalog dashboard strip deep columns landing docs site"` and a `render()` (or `content()`) that draws `this.tabs().ac("block")` — so the ladder is sidebar → Doc top tabs → Old's left rail → Overview's top tabs. The owner: "you'd need to switch the inner and outer colors" — the outer `.tabs.block` sits in Doc's tinted well (`ext/Doc/Doc.css`: `--tab-fill: var(--wash)`), so the inner set should invert: strip tinted, selected tab cut back to the page. ONE small stylesheet `core/Page/old/old.css` (≤ 10 lines, inside `@layer theme`, class prefix per `new-css-class`), loaded with `View.stylesheet(import.meta, "old.css")`. Screenshot it headless at 1280 and link the png. If it reads wrong, say so plainly — this is an experiment the owner asked to see, not a verdict.
6. **Verify headless** — never the owner's live tabs; `site` MCP `shot`/`eval` (ToolSearch `mcp__site__shot`) or Playwright from the scratchpad (`page-docs-restructure-probe.mjs`; import path `file:///C:/...`). Load `/framework/core/Page/`, `/old/`, `/old/nav/`, `/old/overview/`, `/old/overview/columns/`: zero console errors, a `.page.active-page` on each, the top bar reads Overview · Old · API · Docs · Files, the Old rail shows six items, the inner bar shows 15. Click-through test: from `/framework/core/` the Page card → Page → Old → nav. An old url (`/framework/core/Page/nav/`) 404s cleanly — expected.
7. `documentation` skill for `core/Page/readme.md` (the More line — paths) and `old/readme.md`; `skill-improvement` if a skill misled you; land with `finish-task`, `links` = the five urls + the png.
