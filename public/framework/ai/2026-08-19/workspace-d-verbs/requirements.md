# workspace-d-verbs — task D: split vs add as two verbs, two gestures; the seam `+` stops eating seam drags; the group toggle on the bar

Laws: less is more · clarity · prioritize. **Deliverable: `Panel.restyle()` + the `split` verb on edge click, `add` on the Workspace bar's `+` (appends a section in document mode), `insert.js`'s `+` no longer over the grip, the group toggle in the bar too; proven headless; docs current; final message ≤ 12 lines.** Sonnet. Task B has landed; read [`../workspace-design/design.md`](../workspace-design/design.md) §5 and `ext/Panel/split.js`, `Panel.js`, `workspace.js` (`sown()` landed in A; `mount()` changed in B), `ext/Panel/Workspace/Workspace.js` (the bar), `insert.js`, `grip.js`, `toolbar.js` (`word_pops`), `properties.js` (the group toggle the groups task put in the rail).

The owner, verbatim: *"splitting a panel should reuse the style of that panel, creating an empty but equally styled panel that shares 50% of the space of the old one... if it's a white panel that's split, the new panel is the same. — adding a panel next to an existing one can create a similar layout, but we're starting from scratch - choose a new section/style."* And: *"if it's not hurting anything leave it for now […] unless it conflicts"* — `insert.js`'s seam `+` (z-index 5) sits over `grip.js`'s seam (z 2) and eats a nested seam drag (panel-flow's finding) — that is a conflict: fix, do not delete.

## Build

1. **`Panel.restyle(from)`** (Panel.js, ~3 lines): copy `Panel.shared` minus `template seed text` (+ `grow` from the struck panel) — the list exists; one method.
2. **`split` on edge click** — `split.js:57`'s `commit`: `item.divide(dir, new Panel().restyle(item), at)` — the struck panel's look, empty, 50% (divide already does the even share). Document in the split.js header comment: edge = split (twin), bar `+` = add (fresh).
3. **`add` on the Workspace bar** — a `+` in `Workspace.js`'s bar: on the focused panel (or the root when nothing is focused) `divide(dir, new Panel(), false)` with the template picker open on the new panel (the bar's Template pop — `toolbar.js` exposes `pop`/the picker; if it cannot be opened programmatically in ≤ 10 lines, the new panel simply takes the default template and the rail's template row lights — say so); in `mode: document` on the root, `+` appends a section at the end (`root.divide("col", …)` → last).
4. **`insert.js` over the grip** — the seam `+` must not cover the grip's hit strip: lower its z-index below the grip's, or inset it from the seam's centre so the grip stays reachable (measure which keeps both usable; `insert.css`/`grip.css` one line each at most). Prove: a nested column's outer seam drags 100px with the `+` present.
5. **The group toggle on the bar** — `toolbar.js` `word_pops()` draws root words; the groups task added the `group` toggle to the rail only (`properties.js` beside the `mode` row) because the WORDS table is leaf-only. Give splits the same toggle in the bar (one predicate in `word_pops()` like the root's — the groups task's `doc/focus.md` says where).
6. **Prove headless** (`file:///C:/Users/mike/AppData/Roaming/npm/node_modules/playwright/index.mjs`; probes `d-verbs-*.mjs`; socket blocked; build trees via the page's loaded modules): edge-click a toned, grid-worded leaf → the twin has the same `tone`/`display`/`cols` words and an EMPTY template, grow 1/1 (two numbers equal); bar `+` on a leaf → a fresh default panel beside it; bar `+` on a document root → a new section at the end (count +1, last); the nested seam drag with `+` present moves 100px; the group toggle flips `.panel-group` from the bar. Zero console errors on the Panel page and the playground (if C has landed) or `/framework/ext/Panel/Workspace/`. Two pngs: `split-twin.png`, `add-fresh.png`.
7. **Docs**: `doc/decisions.md` one entry (two verbs, two gestures; the z-index fix), `readme.md` Use line for `restyle`/the gestures, `doc/words.md` if the verbs are mentioned there, `doc/file/split.js.md`/`insert.js.md` de-staled.

## Fences

`ext/Panel/Panel.js`, `split.js`, `insert.js`, `insert.css` OR `grip.css` (one line), `toolbar.js` (the predicate + nothing else), `ext/Panel/Workspace/Workspace.js` (the `+` in the bar — ⚠ task C is adding the viewport controls to the same bar concurrently: make one minimal string edit adding one button; never rewrite the file; put the `+` FIRST in the bar and say so), `readme.md`, `doc/*` (your entries), this dir. NOT `glyphs.js`, `properties.js`, `focus.js`, `size.*`, `workspace.js`, `flow.js`, `demo/**`, `playground/**`, `viewports.js`.

## Rules

- Load `code` once. Run `new-task` first (dir + brief exist; write `task.jsonl` line 1 and the `day.jsonl` line in `ai/2026-08-19/`; group `panels`); `documentation` then `finish-task` (`"tokens": null`). A skill that misleads you gets one line in its `improvements.md`. Timestamps from the clock; forward slashes; never Out-File a `.jsonl`; never a person's name — "the owner". Wait in the foreground.
