# workspace-a-sizing — task A of the Workspace build: sizing with no containers; `hug` means AUTO

Laws: less is more · clarity · prioritize. **Deliverable: `ext/Panel` with zero `container-type` / `cq*` in the sizing path, `hug` measuring its content, a rolled layout in a document reading as sections that hug, proven headless with the numbers below; docs current; final message ≤ 15 lines.** Opus — one wrong line blanks every panel. Blocks B, C and D.

The design is [`../workspace-design/design.md`](../workspace-design/design.md) **§2 (read it whole, it is your spec)** and the scrollbar rule in it. The owner approved with two amendments, verbatim: *"i like the zoom scrub, why delete? if it's not hurting anything leave it for now. the decorative scenes are fine, they're not hurting anything. unless it conflicts.. […] let's race to the finish line. Get an MVP working."* Also: *"I'd probably avoid containers for now."*

## Do

1. **Delete the containment** — `panel.css:21` (`.panel-workspace, .panel-items { container-type: size }`), `:72`, `:74` (`.panel-body` variants), `:135`'s `container-type: inline-size` (keep its `flex: 1 0 auto`). Then `hug` = `flex: 0 0 auto` on the main axis, basis auto, measured by content (`size.css` `:29` `:38` — the `min(var(--panel-hug), 100cq…)` floors go; cap with `max-inline-size: 100%` on `.panel` per the design). `--panel-hug` survives once, **renamed `--panel-section`**, as `mode: document`'s `min-block-size` floor (keep `flex: 1 0 auto` on sections — landed yesterday). A **scene** (`blank word wall clock` and the four decorative ones) declares its own minimum in `templates.css` because it has no content to measure — that is the scene's floor, not hug's meaning.
2. **Every other `cq` line — convert, do not delete** (the owner's amendment): paint values (gaps, padding, radii, `background-size`, `max-inline-size`) → `%` against the same box; type sizes → their `clamp()` with the middle term in `em`; `.panel-t` `100cqh` → `100%` + `.panel-body:has(> .panel-t) { grid-template-rows: 100% }` (`.panel-t-screen` proves it at `:184`); `.panel-t-screen`'s `1.5cqw` → `1em` (zoom is the Workspace's job, task C); `wall` is 2 columns, no `@container` count; the decorative scenes `haze aurora drift depth` STAY — their 16 cq lines convert like the rest; `toolbar.css`'s three `@container` fold blocks → fold unconditionally (handle · `more_horiz` · close always visible; `100cqi` → `100%`); `tools.css`'s 3 live lines convert (the align overlay stays off but stays; **`zoom_scrub` stays**). Two numbers that must agree at the end: `grep -cE 'container-type|container-name|container:|[0-9]cq[bwhi]|cqmin|cqmax|@container' ext/Panel/*.css` = **0** live (comments may mention it — say how many are comments), and your per-file table of 61 → 0.
3. **The roll on a document root.** `sow(item)` on a root in `mode: document` sows into ONE fresh child (one roll = one section) — one `if (item.document())` in `workspace.js`'s `sow:` factory (design §5). On a section, unchanged.
4. **Prove it, headless** (`file:///C:/Users/mike/AppData/Roaming/npm/node_modules/playwright/index.mjs`; scratchpad `C:/Users/mike/AppData/Local/Temp/claude/c--Code-lew42-monorepo/a14ec0db-4e8c-4ce1-a14c-378e52ac01a0/scratchpad/`, name your probe `a-sizing-*.mjs` — the scratchpad is shared; block the dev socket with `page.routeWebSocket(/.*/, () => {})` — the persisted workspace will not load then, so test on the no-saver `panel()` demos on `/framework/ext/Panel/` and on your own `workspace({ saver: new MemorySaver() })` page in the scratchpad served… no — the scratchpad is not served; instead build the tree in `page.evaluate` on `/framework/ext/Panel/` using the module imports the page already loaded (`import("/framework/ext/Panel/workspace.js")` etc.) and mount into a fresh div; say how). Numbers: (a) a `hug` leaf holding a short template vs a long one measures two DIFFERENT heights (today both read 16em); (b) `/full/`-style document root with a rolled layout: the sections' heights sum to the workspace `scrollHeight`, and the count of boxes with `scrollHeight > clientHeight` inside the workspace is **0 or exactly the ones you name as intended** (the scrollbar rule — list them); (c) the Panel page, `/framework/ext/editor/`, `/framework/ext/files/` (uses `workspace()`), `/framework/styles/layouts/space/compose/` load with zero console errors and the same panel counts as before at 1280 (record before/after); (d) a fill leaf in a row still fills (regression: the 24 `.panel-t-cell` demo still shows three tracks). Pngs: `hug-auto.png`, `document-roll.png` in this dir.
5. **Docs:** `doc/templates.md` (hug measures now; scenes declare their floor), `doc/words.md` (remove "a section does not grow with its content, ever"), `readme.md` Watch-out (the `container-type` trap line goes; a new one if you find one), `doc/decisions.md` one entry (what was deleted/converted and why; the 61 → 0 table), `doc/file/*.md` for any file whose note went stale.

## Fences

`ext/Panel/panel.css`, `size.css`, `size.js`, `templates.css`, `templates.js`, `toolbar.css`, `tools.css`, `workspace.js` (the one `sow` branch), `readme.md`, `doc/*`, this dir. Nothing else — `Panel.js`, `split.js`, `glyphs.js`, `properties.js`, `toolbar.js`, `flow.js`, `display.*`, `paint.js`, `generate.js` are tasks B–D's. Do NOT delete `zoom_scrub`, the align overlay code, `insert.js`, or the four scenes.

## Rules

- Load `code` and `css` once. Run `new-task` first (dir + brief exist; write `task.jsonl` line 1 and the `day.jsonl` line; group `panels`); the ledger logs edits; `documentation` then `finish-task` (`"tokens": null`). A skill that misleads you gets one line in its `improvements.md` (`skill-improvement`).
- Timestamps from the clock; forward slashes; never Out-File a `.jsonl`; never a person's name — "the owner". Every CSS rule inside a layer; no `**/` inside a block comment; only `p()`/`h1`–`h6` read backticks. Wait in the foreground.
