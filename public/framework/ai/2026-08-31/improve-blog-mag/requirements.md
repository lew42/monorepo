# improve-blog-mag

## The ask, verbatim

> TASK — improve the blog and the magazine: look, brainstorm, build. First: run `new-task` (slug `improve-blog-mag`, group `pages`).
>
> LOOK first (read + drive at all widths): `/blog/` (magazine front, 6 posts, sections, meta.mjs pipeline — `blog/readme.md` + doc/) and `/imagine/mag/` (Issue 01, cover states, tone rungs). BRAINSTORM 8-12 ranked improvements each as log lines. BUILD the top 2-3 that are S/M — candidates to weigh: blog — an **RSS/Atom feed** (static, generated by meta.mjs from posts.js — the classic missing feature; validate the XML), reading-time on cards + posts (computed from the manifest at stamp time or live from the md), a "next post" flow at the end of each post (the reading path never dead-ends), post-date visibility polish; mag — Issue 01 self-navigation polish (next-article was built — verify and extend to a full circular flow), a cover animation restraint-check, or a second lead treatment. Keep the manifest as the single source (never a second copy of titles); keep every url cold-loadable; re-stamp shells if the manifest gains fields.
>
> FENCE — `/blog/**` (incl meta.mjs, posts.js), `/imagine/mag/**`. NOT public/page.js, NOT ext/**.
>
> VERIFY: the feed validates (parse it with a real parser or validator logic; curl shows correct content-type from the static server — note express.static's XML handling), reading times accurate ±10% on 2 hand-counted posts, next-post flow clicked through all 6 posts headless, og shells still stamp `ok` (10/10), zero console errors at 400/1920/3440. Keepers + `links`.

## Fence

- Write: `public/blog/**`, `public/imagine/mag/**`
- Do not touch: `public/page.js`, `public/framework/ext/**`
- Never kill/restart the :80 dev server. Private server on 8097, torn down after.
- Never stash, never commit. Never write the owner's name.
