# Vocabulary + tags (verbatim ask)

> have them create a vocabulary with tags, and tag sites with these unique features. have
> them find the blog, docs, etc. make special note of multi-level navigation… focus on
> proper/simple categorization.

Follow-on scoping note (from the mastermind dispatch): external websites are
egress-blocked in this container — build the vocabulary and tag OUR OWN site's realms as
the first corpus; shape `tags.js` so external sites can be appended later from the
owner's PC. Inherit tags from tonight's navigation-study and layout-study rather than
inventing parallel ones. Keep the whole vocabulary to ≤30 tags across ≤5 axes.

## Scope / fences
- Files owned by this task: `public/imagine/design/vocabulary/` (tags.js, page.js, doc/
  if needed) + this task dir.
- Do NOT edit the hub (`public/imagine/design/page.js` already lists `vocabulary` in
  `children:`) or `/imagine/page.js`.
- Do NOT edit navigation-study or layout-study's files — read only.

## Steps
1. Read navigation-study + layout-study logs and pages; lift their tags verbatim.
2. Add only what's missing to describe a foreign site (top-nav, hamburger, mega-menu,
   footer-nav, sticky-header, infinite-scroll, horizontal-scroll, single-column,
   split-view, holy-grail, content-kind tags).
3. Build `tags.js` — axes + sites (our own ~8 realms tagged as the first corpus).
4. Build `page.js` — glossary + tagged table + "how to add a site" note.
5. Verify tag-count parity (defined == used-and-shown), shoot at 1280/3440.
6. finish-task.
