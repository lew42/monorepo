# ux-doc-convert — brief (Sonnet)

**Three laws (CLAUDE.md rules all — read it first):** Less is more — ASAP. Clarity is the one exception. Prioritize.
**Length budget:** final report = one screen, clickable paths.

## The job

Owner-approved: convert the eight ux module pages from plain `Page` to `Doc` so each module's `doc/*.md` gets the rail and pretty routes — `ux/Auth`, `ux/Wizard`, `ux/Tree`, `ux/Course`, `ux/Filter`, `ux/Menu`, `ux/Pagination`, `ux/Tags`. Then fix the cross-links that used literal `.md` paths because the pretty routes did not exist. Verify all eight headless.

## Read first

1. `public/framework/ux/Tree/doc/decisions.md` — the RECORDED REASON Tree shipped as `Page`: "`Doc` puts `content()` inside the Overview rail's intro card and would have re-columned the bleed exhibit." That objection has a landed counter-example: `public/framework/ui/page.js` IS a `Doc` with a full-bleed browse wall — read how it does it (its `bar()` override, its `render()` region wiring, `.ac("bleed")` on the wall). Copy that pattern where a module's demo needs the full width.
2. The `documentation` skill (load it) — `notes:`/`files:` declaration, and its SKILL.md carries the corrected trap note about doc links (updated today).
3. `public/framework/ux/page.js` — the root Doc (leaf: true, children) — your eight are its children; do not edit this file.
4. One already-Doc module for the target shape: `ux/Auth/page.js` was converted to a `Doc` by its builder this morning — read it first; if it is already right, it needs only its cross-links checked (then it is 7 conversions + 8 verifications).

## Deliverables

1. Each of the eight `ux/<Name>/page.js`: a `Doc` with `meta`, `title`, `notes:` naming its doc/*.md topics (e.g. "decisions"), `files:` naming its real source files (Auth.js, MagicAuth.js, page.js, readme.md — per module, real names only), `leaf` NOT set (these are leaves by being childless — check what ui components' Doc pages do and match), and the module's existing demo content preserved EXACTLY — this is a chrome change, not a content rewrite. Where the demo used `bleed` or full-width exhibits (Tree's explorer, Filter's dashboard, Course's three regions), verify the width survived; if Doc genuinely re-columns it, apply the ui/page.js pattern (bar()/regions) rather than reverting — and if THAT fails too, leave that module a Page, shoot the failure as evidence, and log it (an evidenced exception beats a broken page; the owner reads evidence).
2. Cross-link fixes: grep `ux/**` (readmes, docs, page.js files) and `ui/tree`, `ui/menu`, `ui/pagination`, `ui/tags` pages for literal `doc/decisions.md`-style hrefs pointing at ux modules; where a pretty route now exists (`/framework/ux/<Name>/doc/<topic>/` — confirm the exact route shape from a working Doc like `ux/` root's own doc links), update them. Do not touch links that already work.
3. Child pages (`Tree/drag`, `Tree/keys`, `Tree/words`, `Filter/chips`, `Filter/words`, Auth's children if any) must still render — a Doc parent changes the tab strip; verify each child route.

## Verification (before landing)

Port 80 is DOWN — NEVER start or touch it; never kill any server (8918 is the mastermind's static server, up now). Recipe:
`node C:/Users/mike/AppData/Local/Temp/claude/c--Code-lew42-monorepo/0375cdd4-082c-41fa-9ebe-fa4bbb0f2a23/scratchpad/ux-shoot.mjs http://localhost:8918/framework/ux/Auth/ <out.png> <width>` — ignore only the repeated ws://localhost:8918 LiveReload console error. For EACH of the eight: the Overview at 1280 (and 3440 for Tree, Filter, Course — the wide demos), the Docs tab rendering a real doc page, one child route where children exist. Zero non-LiveReload console errors, zero `.md-error` markers, overflow_x 0. Money shot (one module's Docs tab with the rail) into this task dir, linked in the landing line.

## Fence

Yours alone: the eight `ux/<Name>/page.js`, cross-link lines in `ux/<Name>/readme.md` + `ux/<Name>/doc/*.md`, and the named `ui/<name>/page.js` pointer-link lines only. READ-ONLY: everything else — `ux/page.js`, `ux/readme.md`, `ux/doc/**`, `ui/words/**` and `styles/framework.css` (⚠ an Opus is editing BOTH right now — collision risk is real, stay out), `core/**`, `ext/**`.

## Process

- Load skills: `code` before editing page.js; `documentation` (mandated — this task IS its subject); `finish-task`; `skill-improvement` if a skill misleads.
- Log to `public/framework/ai/2026-08-21/ux-doc-convert/task.jsonl`: line 1 `assign` (Write tool, group "web-ui"); appends via Add-Content, **pure ASCII**.
- **Timestamps are READ FROM THE CLOCK in the same command that writes the line, never typed** — `Get-Date -Format "yyyy-MM-ddTHH:mm:sszzz"` inline in the SAME append command.

## Safety (non-negotiable)

Never kill or restart any server; never drive the owner's browser tabs; never `git stash`; never commit or push; scratch stays in the scratchpad (`ux-docconv-*` names).

## Traps

A `Doc` that is also a nav section needs `leaf: true` — but a childless module Doc must match what ui components do (check, don't guess); `bar()`/`tabs()` register mount regions from the same list (ui/page.js's comments explain the failure mode: a name dropped from bar() loses its region and renders OVER the framework sidebar); only `p()`/`h1`–`h6` read backticks; `**/` in a JS comment closes the block; ui.table cells take plain text; a declared child without a page.js 404s; hidden tabs do not lay out — verify through 8918 shots.

## Cut first if squeezed

The ui/ pointer-link fixes → 3440 re-shots for the narrow modules (Menu/Pagination/Tags) → nothing else. Never cut: eight Docs tabs verified rendering, the wide demos re-verified at 3440, children still routing.
