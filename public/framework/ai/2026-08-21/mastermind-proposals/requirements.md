# mastermind-proposals — run task

Second program of 2026-08-21. The owner, verbatim: "i'm going for a run, why don't you just run with all 7 proposals. work autonomously mastermind! go!"

All 7 proposals from the morning program are hereby approved work orders:

Playground side (`../playground-mastermind/proposals.md`):
1. Gesture cost — supervisor's call: modifier-click (Shift-click + → Flex), not auto-flex.
2. Dev-server-down save loss — ship the LocalStorage fallback; newest-wins, nothing deleted.
3. Parked geometry — grid handles, .pg-node min-width skew, wrapped-flex (fix or bound with evidence).

UX side (`../ux-system-plan/` page):
4. framework.css tokenization — exactly the 4 one-line edits with fallbacks (--pad-cell, --pad-control x2, --muted), then words remap + re-measure.
5. Doc-vs-Page — convert the 8 ux modules to Doc, fix cross-links to pretty routes.
6. ui.tree retirement — GATED on the Playground migration (Playground.js:170,184 → ux/Tree) landing first; supervisor relays the gate.
7. --density default — the ux mastermind decides with evidence and documents the verdict.

Execution: resume both Fable sub-masterminds warm (SendMessage); cold fallback briefs at `../pg-proposals/requirements.md` and `../ux-proposals/requirements.md`. Fences unchanged from the morning: playground owns ext/Playground/**; ux owns ui/** + ux/** + ONLY the 4 named framework.css lines. Sequencing of #6 runs through the supervisor, never a worker wait.
