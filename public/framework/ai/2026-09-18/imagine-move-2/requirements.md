# Brief: Move 2 — /imagine/design/ studies into the design system

Source: owner's message to the orchestrator, relayed verbatim below. Full context: `public/framework/ai/2026-09-17/imagine-integration/proposal.md` and `inventory.md`; how Move 3 was done: `public/framework/ai/2026-09-18/imagine-move-3/task.jsonl` and its `requirements.md`.

## Owner's brief (verbatim)

**The move:** Move 2 of `public/framework/ai/2026-09-17/imagine-integration/proposal.md` (read it and `inventory.md`; then read how Move 3 was done today — `ai/2026-09-18/imagine-move-3/task.jsonl` and its `requirements.md` — including the regex that ate backticks and how it was caught: rewrite links per url with a boundary check, never a blanket pattern, and scan for damage after). The design studies under `/imagine/design/` join the design system: `/framework/styles/system/` is the page that IS the system (landed yesterday; read its `page.js` and `readme.md`); nine studies move under `public/framework/styles/system/studies/<name>/` and four go to the module they belong to, as the proposal's table says — but the realm has grown since the proposal: `color/sections/`, `type/anchors/`, `spacing/nesting/`, `lists/` landed in the last day; move each with its parent study and say so. Every old address keeps a one-line stub `page.js` (Move 3's pattern). The system page's level 1 stays one screen: the studies appear one click down as a "Studies" group, not on the first screen.

**Do, in this order, with a count at each step in your log:** (1) `git status --porcelain public/imagine/design public/framework/styles` clean for what you move — if a sibling is editing any of it, stop and report; (2) count the live links per old url (`rg -c "imagine/design/<name>/" public --glob "*.js" --glob "*.md" --glob "*.json"` — drop any leading slash); (3) `git mv` each dir; add the studies to the system page's children (one click down) and to the module pages for the four; (4) rewrite links per url with a boundary check, recount (counted = rewritten); (5) stubs at every old address including the sub-studies; (6) the layout skill and the css skill link several of these studies (`rg -n "imagine/design" .claude/skills` — the `.claude` writes allowed here are exactly those link rewrites); (7) crawl on your private server (`PORT=8138 node server.js`, background, killed by its real Windows PID): every new url with its own `document.title`, every old url with its stub, zero console errors and zero 404s across them and `/framework/styles/system/` and `/imagine/`, at 1280; `rg` for any remaining old url across `public/` (excluding `framework/ai/**`): zero; (8) the `/imagine/` rail's Studies group loses the moved realms (edit only those entries in `public/imagine/page.js`); `/imagine/design/` itself becomes a stub too if nothing is left under it — say what is left.

**Fence:** the moved dirs, `public/framework/styles/system/**`, the four module pages that receive a study (the children word and one line each), the files whose links you rewrite (named in your log), `.claude/skills/*/SKILL.md` link rewrites only, `public/imagine/page.js` (the group entries), the stubs, your task dir. Nothing else. The owner's dev server on port 80 is running: never touch it; LiveReload pushes moves to the owner's tabs — do the moves and the link rewrites in one sitting. Never `git stash`, never `find /`, never drive the owner's tabs. Two numbers that must agree: links counted and rewritten; new urls and stubs. Final message: one screen — studies moved (and where), links rewritten, stubs, the crawl verdict, what is left under /imagine/design/, what was left and why.
