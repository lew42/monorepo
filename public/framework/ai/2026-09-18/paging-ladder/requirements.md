# Paging ladder — brief

**The finding:** `public/imagine/paging/paging.css` carries 131 of the site's 1,894 hand-typed spacing numbers, 1.7x the next-worst file (`ai/2026-09-18/overlap-study/overlap.md`; the census: `ai/2026-09-17/spacing-census/census.json`, rows whose file is paging.css). The design system's ladder is at `/framework/styles/system/` (`--pad`, `--gap`, `--gap-70/-50/-35/-25`, `--flow`; the rule: a spacing value is a token or a rung, never a multiplier and never a raw number; a control's own padding stays in em). `ext/AITask/ai.css` was converted last night with a 0.52% pixel diff (`ai/2026-09-17/design-system/task.jsonl` — read how it proved it: the stylesheet swapped by `page.route()` in one run, never a live page diffed against itself).

**Do:** classify every one of paging.css's spacing declarations (container padding/gap/margin -> the nearest rung or token; a control's own padding, height, or icon-to-label gap -> stays em, with a one-line `/* control */` note; keyword values stay); convert; keep the census's row set for the file as your before/after table (131 raw -> N raw, rungs, tokens, control-em). Prove on your private server (`PORT=8134 node server.js`, background, killed by its real Windows PID): the paging hub, one template page, one mechanism page and `/imagine/paging/make/` at 1280 and 3440, the pixel diff by stylesheet swap under 3% per page (report each), zero console errors. Docs: one dated line in `public/imagine/paging/doc/decisions.md`; the spacing census's `census.md` gets a one-line addendum with the new count (its task dir is `ai/2026-09-17/spacing-census/`).

**Fence:** `public/imagine/paging/paging.css`, `public/imagine/paging/doc/decisions.md`, `public/framework/ai/2026-09-17/spacing-census/census.md` (the addendum line), your task dir. Nothing else — not `make/` (another minion may be in it), not the other paging files. The owner's dev server on port 80 is running: never touch it; one write, verify within a minute. Never `git stash`, never `find /`, never drive the owner's tabs.

**Final report:** five lines — raw before -> after, rungs and tokens used, control-em kept, the per-page pixel diffs, the link.
