# corpus-polish — the wireframe recreation on a site's page, the section index, the two prefixes, one deletion (Sonnet, group `websites`)

Three laws: less is more (ASAP); clear beats brief, by far; prioritize. Length budget: five small edits, each proven by a picture; your report eight plain lines.

Read first: `../mastermind-playwright/minion-rules.md` (every rule); `../mastermind-playwright/requirements.md` §"The ask" — the sentence this task serves: *"you screenshot the site, and then try to recreate it, without any content, in pure layout form … the minimal recreation can be re-rendered multiple times (400, 1920, 3440)"*; `public/layouts/doc/wire.md` (the drawing spec — `Layout.js` already draws a wire at any width, and `/layouts/<id>/` already draws one at 400 / 1920 / 3440 side by side: reuse that, do not copy it); `public/websites/readme.md`, `doc/schema.md`, `Site.js`. Skills: `new-task` (this dir, group `websites`), `code`, `new-css-class` (before any class name), `documentation`, `finish-task`.

⚠ Three capture minions are writing `public/websites/site/<name>.json` and running `tools/index.mjs` right now. You never write a site json except the three seeds (`mdn-css`, `stripe`, `wikipedia-css`, whose author has landed). When you edit `index.mjs`, write the whole file in one go and re-run it once; a sibling running the old copy a second earlier is fine.

## Do, in this order

1. **The recreation on the site page.** A record may carry `"wire": { … }` (the spec in `doc/wire.md`) in its hand half. When it does, `Site.js` draws it under the four real screenshots at the same three widths the encyclopedia uses — 400, 1920, 3440 — with a one-line caption ("The same page with the content taken out: only the boxes that decide the layout"). When it does not, nothing is drawn and nothing says so. Import the drawer from `/layouts/Layout.js` (imports flow across tiers by absolute url; never copy the class). Add `wire` to `doc/schema.md` (who writes it: a person, after looking at the 1920 and 400 shots; link `/layouts/doc/wire/`).
2. **Three wires by hand** — write `wire` into the three seed records after looking at their 1920 and 400 shots with the Read tool, through `tools/lib.mjs`'s merge (`read_record` / `write_record`; read `lib.mjs` for the exact names). MDN and Wikipedia are `3-holy-grail` at 1920 and `1-flow` at 400; Stripe is `1-centered` with bands. The wire must produce the same picture with the content removed — compare your drawing against the shot at both widths and adjust until a stranger would match them.
3. **The site page's name.** `/websites/wikipedia-css/` is titled `wikipedia-css` (the slug) in the tab and the heading. Make it the record's `title`, shortened at the first ` | ` or ` - ` (`CSS - Wikipedia` → `CSS`; `Stripe | Financial…` → `Stripe`), the slug in small type beside it. `Site.js` line ~56 explains why the manifest may not have arrived when `route()` runs — respect that; the title may settle a tick later.
4. **Sections become clickable.** `tools/index.mjs` also writes `"sections": { "<layout id>": ["<name>", …] }` — every site that uses the id as a *section* layout (`sections[].layout`) — as a NEW key beside `tags`, changing nothing that exists (`/layouts/` pages read this file). `/websites/tag/<id>/` shows a second list under the first: "as a section, on:" with the same cards. `/layouts/<id>/` gets the same second list (it already reads `site/index.json`).
5. **Two prefixes, one deletion.** Add `site-` (`/websites`) and `std-` (`/layouts`) to `public/framework/styles/css-scopes.txt` — the exact lines are written in `public/websites/doc/decisions.md` (line ~118) and `public/layouts/readme.md` (line ~34); then delete those two "not yet in css-scopes" notes. Delete the `4-equal` entry from `public/layouts/layouts.json` — its own "when to use it" says to use `n-wall` (the encyclopedia author recommended the deletion) — and make `/layouts/4-equal/` land on `/layouts/n-wall/` through the alias table the file already has; `rg 4-equal public/` afterwards must find only the alias and the decisions doc, which gets one line saying why.

## Proof

Headless at 400 and 1920, screenshots into this dir: `/websites/wikipedia-css/` (the four shots, then the three wires, the new title), `/websites/tag/3-holy-grail/` (both lists), `/layouts/3-holy-grail/` (the section list), `/layouts/4-equal/` (lands on n-wall), `/layouts/` (13 → 12 cards, no gap). Zero console errors on each. `node public/websites/tools/index.mjs` prints the sites and tags count and the new sections count.

## Fences and budget

Write ONLY `public/websites/Site.js`, `Site.css`, `page.js`, `tag/**`, `tools/index.mjs`, `doc/**`, `readme.md`; the three seed jsons `site/mdn-css.json`, `site/stripe.json`, `site/wikipedia-css.json` (through the merge; never another site's); `public/layouts/**`; two lines in `public/framework/styles/css-scopes.txt`; this dir; scratch named `corpus-polish-*`. `:8123` serves the site. Budget ~200k tokens. Report in ≤ 8 plain lines: the url where the recreation shows, how close the three wires are to their shots (your honest word), the new index key, what you deleted.
