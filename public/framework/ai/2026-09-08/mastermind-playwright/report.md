# Morning report — the Playwright and layouts night, 2026-09-08

**Everything you asked for exists and is clickable; nothing is committed.** Open these in order.

1. **[/layouts/](/layouts/) — the encyclopedia.** Twelve named layouts in four branches (`1` `2` `3` `n`), each drawn content-free at 400, 1920 and 3440, each answering grid, flex and wrap. Ids are `N-name` as you asked at 00:05: `2-sidebar`, `1-bands`, `n-wall`; the technique and the paint are tags. Twenty words are defined so each describes many layouts ([the rule](/layouts/doc/naming/)). Every tag chip leads to everything that carries it — [`cards`](/layouts/tag/cards/), for example. `4-1` is the one honest escape hatch: a mosaic nobody could name.
2. **[/websites/](/websites/) — the corpus.** 47 real sites photographed at four widths, scanned for their CSS, looked at by a minion, tagged, and each redrawn as a wireframe under its own screenshots ([Wikipedia](/websites/wikipedia-css/), [IKEA](/websites/ikea-home/), [GOV.UK](/websites/gov-uk-home/)). Click a tag to walk sideways ([`2-sidebar`](/websites/tag/2-sidebar/)). A site that allows it can be loaded live in a resizable, zoomed-out frame, on click.
3. **[What emerged](/websites/patterns/)** — every number counted live. The sidebar is the commonest desktop layout and collapses to one column on a phone every time; the band stack is the only layout a phone leaves alone. [The best of each kind](/websites/patterns/best/) names one site per category and says why, with three layouts to copy and three to avoid.
4. **[The blog post](/blog/ai/playwright/)** — what Playwright is, what people use it for, the three ways Claude Code drives a browser, and where it stops working; six exhibits from [ten runnable demos](/blog/ai/playwright/demo/readme.md); [the research behind it](/imagine/research/playwright/), 32 graded claims.
5. **Two site bugs found on the way, fixed:** a failed font fetch blanked every page white ([`core/App/Font.js`](/framework/ai/2026-09-08/font-fallback/)); a long title word split in half at 400 ([`core/Page`](/framework/ai/2026-09-08/title-wrap/)).

**The outage.** At about 01:20 your modem dropped and you suspected the browsing. What ran was three crawlers, one site at a time, nine page loads per site, 45 sites in 40 minutes. I paused every external load from that moment; everything after was drawn from the screenshots already on disk, and the live viewer now loads only on click. Twelve of 59 candidate sites were lost to bot walls or the cut ([the list](/framework/ai/2026-09-08/site-scout/candidates.md)). Say the word and the tools take the rest.

**Three critic rounds ran over what was built** — at 400 and 1920 on each tier, then at 3440 across both and the post. Each found real failures and fixed them: a first screen that was three paragraphs, a thumbnail wall lying about its width, a "best of" page with no picture, a three-widths row using half of a 3440 screen. The last crawl was 185 urls at four widths, 740 loads, zero errors, nothing sideways, no broken image ([the critic's log](/framework/ai/2026-09-08/critic-3440/)).

**Left, with reasons.** `4-1`'s claimed phone fallback has one counter-example (Nike stays two-across), not enough to rewrite it. A page title can still wrap oddly at 3440 (`core/Page`, outside every fence tonight). The corpus grows only when browsing may resume.

**Spend.** 22 minions, 5.2 million tokens (Sonnet two thirds, Opus one third); the session window peaked around a third, the weekly moved 43 → 48. Everything else is in [the run log](/framework/ai/2026-09-08/mastermind-playwright/).
