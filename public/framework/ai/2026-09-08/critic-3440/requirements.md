# critic-3440 — the newcomer at 3440 and at 400, across both tiers and the post; the live viewer loads on click (Opus, group `websites`)

Three laws: less is more (ASAP); clear beats brief, by far; prioritize. Length budget: fixes are edits; your report ten plain lines. **Resolve, don't park.**

⚠ **NO INTERNET.** Never load an external website from a script. Note: a site page whose record says `embed: allowed` currently loads the live site in an iframe the moment the page opens — that IS an external load. Item 1 below changes it to load on click; until you have made that change, do not open site pages of embeddable sites headless (`site/index.json` does not carry `embed`; the record does — check `site/<name>.json` first, or do item 1 first).

Read first: `../mastermind-playwright/requirements.md` — the owner's words, all of them; `../mastermind-playwright/minion-rules.md`; the two critics' verdicts (`../critic-layouts/task.jsonl`, `../critic-websites/task.jsonl` — the last fifteen lines of each: what they fixed and what they left, so you do not redo it); the prime objective in the mastermind's memory: *organized, visual, browsable, mobile → 3440* — both critics tested 400 and 1920; nobody has judged 3440. Skills: `new-task` (this dir, group `websites`), `code`, `layout` (its five questions are exactly the 3440 test), `css`, `new-css-class`, `documentation`, `finish-task`.

## Do

1. **The live viewer loads on click.** In `public/websites/Site.js`, the iframe viewer for an `embed: allowed` site must not load until the reader presses a plain button ("Load the live site"); the four-shot strip is what shows first. One sentence beside the button says the site will be fetched from its own server. Prove it: a headless load of `/websites/wikipedia-css/` makes NO request to any host but `localhost` (`page.on("request")`, print the hosts), then a click makes the frame appear. Update `doc/decisions.md` (why: a page view should not fetch a third party unasked).
2. **The 3440 newcomer test** — headless at 3440×1440 (and 400×844 for the pair), screenshots into this dir, Read every one: `/layouts/`, `/layouts/2-sidebar/`, `/layouts/tag/cards/`, `/websites/`, `/websites/patterns/`, `/websites/patterns/best/`, `/websites/tag/2-sidebar/`, two site pages (a blocked-embed one such as `/websites/mdn-css/`, and after item 1 an allowed one), `/blog/ai/playwright/`. At 3440 ask the `layout` skill's questions: is the room used or wasted (a 46em column in a 3440 window with nothing beside it is waste; a wall that puts 12 cards in a row is noise)? does the first screen still say what the page is for? do the four screenshots on a site page still read as one page at four widths, or spread so far the eye loses them? do the wireframes' three widths sit at one height? At 400: nothing sideways, nothing clipped, the first screen the thing itself. Fix what fails, in the tier that owns it; keep every fix easy to change; a container-query or the framework's existing size words before any new rule (`css` skill; the spacing doubles at 3440 by the owner's rule).
3. **The blog post at 3440** — the reading column hard left with the exhibits beside it is the blog's own layout (`public/blog/readme.md`); judge only whether the exhibits (the demo pictures) read at that size, and whether the first screen holds. Fix only inside `post.md` / the exhibit figures; `node public/blog/meta.mjs --write` once if prose changed.
4. **Crawl** every url you touched at 400 / 1280 / 1920 / 3440: zero console errors, zero 404s, no sideways scroll, no broken image. Print the count of loads and problems.

## Fences and budget

Write ONLY `public/websites/**` (never a site record's machine half or `wire`; hand-half keys through the merge if a record is wrong), `public/layouts/**`, `public/blog/ai/playwright/post.md` and the generated files `meta.mjs` writes, this dir; scratch named `critic-3440-*`. Never `public/page.js`, never `core/`, never `framework.css`. Budget ~300k tokens. Report in ≤ 10 plain lines: the hosts a site page requests before and after item 1, one line per page judged at 3440 (verdict + what changed), what you left and why.
