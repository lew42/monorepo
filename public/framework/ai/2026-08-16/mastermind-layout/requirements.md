# Mastermind run — layout, continued

> be a mastermind, don't stop, keep working. you make the calls. stay on token
> pace. begin!  — Mike, 2026-08-16 20:25

Invocation is the grant of autonomy. This is the executive log for the run that
follows [`layout-generator-rules`](../layout-generator-rules/) and
[`taste-corpus`](../taste-corpus/); state lives here, not in the chat.

## Standing calls made without Mike

Two items were surfaced to him and he answered "you make the calls":

1. **`--measure: 52em` runs ~117 characters a line at 3440.** Site-wide token
   change = RULE#1 surgery, so it is **a written proposal with measurements**,
   never an autonomous edit. Dispatched as `measure-token-proposal`.
2. **`web.js` has no unique prose**, so `repetition` reads ~0.95 on every layout
   in the rail against a site median of 0.23. Contained content change with a
   wide blast radius, so it ships **with before/after measurements on all
   sixteen layout pages**. Dispatched as `web-prose-variety`.

## The queue

| | what | tier | why |
|---|---|---|---|
| 1 | `--measure` proposal, measured | Sonnet | site-wide finding, needs evidence, safe (no edit) |
| 2 | `web.js` unique prose | Sonnet | unblocks a whole band for the whole rail |
| 3 | reachability scout | Haiku | "a page nobody links to does not exist" — cheap, wide |
| 4 | `frame-gap` role split | Opus | contested design call, the tier's own Open item |
| 5 | the gallery disagreement | Opus | fold into 4 — judge a case at the width it applies to |

## Budget doctrine

Green at dispatch. Cap of three agents in flight, Sonnet default, Opus only for
the contested call. Every brief carries the file fence and "check usage before
wide work". `ext/Panel/` is owned by another session and is out of bounds for
everyone on this run.
