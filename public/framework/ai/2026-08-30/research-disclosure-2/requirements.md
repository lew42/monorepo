# Round 2 — research-disclosure-2

Run the `research` skill FIRST. Then `new-task` (slug `research-disclosure-2`, group `pages`).

TASK — round 2 on the DISCLOSURE arc: deepen `public/imagine/research/disclosure/`
(round 1: 41 entries, 6 threads — read its log + pages; append via
`node public/framework/ext/Research/entry.mjs`). Dig:

- the live AARO maritime case round 1 flagged (everything findable: primary
  statements, named analysts on both sides, prosaic candidates offered)
- the FY2027 NDAA/UAPDA conference state
- the strongest RESOLVED case as a methods exemplar (pick one the record
  documents end-to-end: anomaly claim → analysis → mundane resolution — the
  arc's epistemics in miniature)
- the academic entry into UAP study (Galileo Project's published results vs
  its press, peer-reviewed UAP literature 2023-2026)

≥20 new validated entries, ≥10 new sources of record; update pages where
credence moves.

VERIFY: --check clean, pages render on `$env:PORT='8091'; node server.js`
(torn down), links resolve.

Report: new entries/sources, the maritime-case state, the methods-exemplar
chosen.

HARD RULES (from the orchestrator): never kill/restart the :80 dev server,
never stash, never commit.
