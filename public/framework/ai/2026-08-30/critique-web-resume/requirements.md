# Ask (verbatim)

Repo: c:\Code\lew42\monorepo. Laws: 1. Less is more. 2. Clarity is the one exception. 3. Prioritize
a hiring visitor's first five minutes. Final report <=12 lines. READ-ONLY except your task dir
especially: /resume/ is the owner's personal page; critique it, never edit it. HARD RULES: never
kill/restart the :80 dev server (if DOWN use private $env:PORT='8095'; node server.js, tear down
after); never drive owner tabs; never stash; never commit. Screenshots to the scratchpad
(crit-wr-*), keepers to your task dir.

TASK - hiring-lens critique of the two visitor surfaces not yet critiqued: /web/ (the guide tier,
linked from the homepage nav) and /resume/. First: run new-task (slug critique-web-resume, group
pages).

METHOD: arrive cold from the homepage like a recruiter or hiring engineer would - click Web, browse
6-8 of its pages at 400/1920/3440; then Resume, read it fully, scroll the 3D parallax, check it at
all three widths + dark mode + a middle-click/new-tab pass (does it have its own meta/og card? curl
it). Judge: does /web/ teach or just exist (is it finished enough to show, or should the homepage
stop linking it?); does /resume/ load fast, read clean, degrade at 400, have a social card; do the
blog and resume cross-link (a recruiter on one should find the other)? Rank 10-15 improvements
(#rank where - weakness - fix - S/M/L) as log lines + top-5 keeper shots + name what's genuinely
strong. The don't-link-it-yet verdict on /web/ is a first-class answer if the evidence says so. Two
numbers that agree: items logged vs reported.

Report: the /web/ verdict (show it or hide it, why), the /resume/ card + cross-link status, top 5
items, the strong things.

## Scope

- Read-only against the repo except this task dir.
- /resume/ especially: critique only, never edit.
- Screenshots: working shots to the session scratchpad as crit-wr-*, keeper shots copied into this
  task dir.
- Steps: dev server must already be on :80; do not kill/restart it. If down, use a private
  $env:PORT='8095' instance and tear it down after.
