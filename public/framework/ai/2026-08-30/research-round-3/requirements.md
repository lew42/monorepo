# research-round-3 — the convergence round

## The ask, verbatim

Repo: c:\Code\lew42\monorepo. Laws: 1. Less is more — this round CONVERGES; the deliverable is the program reading as one work. 2. Clarity is the one exception. 3. Prioritize. Final report ≤12 lines. HARD RULES: never kill/restart the :80 dev server (private `$env:PORT='8094'; node server.js`, tear down after); never stash; never commit. Run the `research` skill FIRST (note its foreground-digging warning). Then `new-task` (slug `research-round-3`, group `pages`).

TASK — round 3: the CONVERGENCE round across the whole program at `public/imagine/research/` (304 entries, 4 topics, 2 rounds — read all four logs + the synthesis page + each topic's index first; append via `node public/framework/ext/Research/entry.mjs`).

1. **The program essay**: rewrite/extend `theories/synthesis/` into the program's capstone — what two rounds actually established across all four topics, written for the owner as a reader: the invented-traditions signature (now 4 cases), the one-measurement problem (Dunn) and its parallel in the vase dispute, the confidence-gradient lesson from resolved UAP cases, the retraction dynamics as science working, the four-domain "agency displacement" pattern (name its parts honestly: hyperdiffusionism where named, unnamed where not — and note the sibling-topic echoes), and the epistemics thread that emerged unprompted: AI-citogenesis caught twice, prophecies invented recently, fabricated citations — the program's own method as a finding. Every claim linked to its log entry's topic page.
2. **Cross-topic questions**: the 3-5 questions two rounds left sharpest, one entry + one short section each, each with its "what would settle it" — these are round 4's targets, written down so any future round (or the owner) can pick them up cold.
3. **Digging, foreground, where the essay needs it**: fill only the gaps the synthesis exposes (>=10 new validated entries — quality over count this round).
4. **The presentation check**: does the Program front serve the program it now holds (300+ entries)? If a front tweak is needed (e.g. the stream cap, a synthesis link card), it is in `ext/Research/Program.js`'s fence — make the smallest change and log it.

FENCE — `theories/synthesis/**`, >=10 entries via the writer into whichever topic log each belongs, `ext/Research/Program.js` (smallest tweak only), your task dir. Topic pages beyond synthesis: read-only (their owners' shapes stand).

VERIFY: --check clean on all four logs, the synthesis renders at 400/1920 with every internal link resolving, the front shows the synthesis prominently, zero console errors. Report: the capstone's thesis in 2 lines, the round-4 questions (one line each), entries added, the front tweak if any.

## Fence

- Write: `public/imagine/research/theories/synthesis/**`
- Append: `>=10` entries via `entry.mjs` into whichever topic log each belongs
- Edit: `public/framework/ext/Research/Program.js` — smallest tweak only
- This task dir
- Everything else (topic pages, other topics' `.md`, other `page.js`): READ-ONLY

## Baseline at open

- stone 104, depictions 71, disclosure 72, theories 57 = 304 entries; `--check` clean on all four.
