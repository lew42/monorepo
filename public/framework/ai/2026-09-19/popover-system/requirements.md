# popover-system — one way to pop things over: tooltips, menus, a tree menu; both placements built and compared

Load the `minion` skill first. Then this brief. Model: Sonnet.

**Three laws.** Less is more (one small module, the platform does the hard part). Clear beats brief by far. Prioritize (the comparison the owner asked for first; the tree menu on the AI page second).

## The owner's words (2026-09-19)

> put a drop down on the framework/ai page, a select element that has a two in it essentially right now, but it's going to list the tree. We might actually need to use a custom drop down so that we can put UI inside the drop down. […] it can be kind of like a pop-up menu, whether it drops down or pops over to the side. I don't think we have a proper menu system. Get some minions building a tooltip pop-up system. A lot of times there's absolute positioning used to get them to pop over other things, fighting. It's really hard to get the z-indexes to work properly if the elements are nested. You can't do absolute z positioning across the entire site unless you put them all in the same element, I think, right? Now the trick there is that if you were to put the popovers nearby the content it's for, it would automatically position itself properly in flow without much effort. So explore both those options, maybe build out both of them. It shouldn't take long.

## What exists (read first)

- `ext/Dropdown` — one choice from a list; the list is a `[popover]` (the browser's **top layer**: above everything, no z-index, outside-click and Escape are the browser's), placed once per open from `getBoundingClientRect()`. The mastermind put one on `/framework/ai/` today (`public/framework/ai/page.js`, `VERSIONS`) — it works; its trigger stretches 640 px wide.
- `ux/Menu` + `ui/menu` — a `<details>` menu, in flow. `ext/Panel` has its own overlays. `rg -n "z-index" public/framework --glob "*.css"` shows about forty hand-set z-indexes (values 1 to 100).
- Tooltips today are the native `title` attribute.

## Build

1. **The comparison page** — `/framework/ux/Popover/` (a new `ux/` module: `Popover.js`, `readme.md`, `page.js`, `doc/decisions.md`; declare it in `ux/page.js`'s children). One screen that SHOWS the owner's two options side by side, each inside a hostile box (an `overflow: hidden` card inside a scrolling column inside a `z-index` stacking context, next to a sticky bar):
   - **A — near its content, in flow:** the popup is a child of its trigger's wrapper, `position: absolute`. Show honestly where it wins (no placement code, moves with the content) and where it breaks (clipped by `overflow`, trapped under a sibling's stacking context — the owner's z-index point is right: z-index only competes inside one stacking context).
   - **B — the top layer:** the `popover` attribute. The element STAYS next to its content in the DOM (so it is authored "nearby", as the owner wants) but paints in the browser's top layer, above everything, with no z-index at all. Placement: **CSS anchor positioning** (`anchor-name`, `position-anchor`, `position-area`, `position-try-fallbacks` so it flips at an edge) where the browser has it, and the one-shot `getBoundingClientRect()` fallback `ext/Dropdown` already has where it does not. Check the installed Chromium supports anchor positioning and say which version.
   - The page states the conclusion in one sentence at the top, with the two demos under it. The mastermind expects B to win — it gives both things the owner asked for (authored beside the content, never clipped or out-z-indexed) — but build both and let the demo say it. Write the `decision` line with the alternative and its caveats.
2. **`Popover` — the small class the winner becomes.** `new Popover({ anchor, content(){…}, place: "bottom" | "right" | "top" | "left", trigger: "click" | "hover" })`: opens, places, flips at the viewport edge, closes on outside-click and Escape (the browser's), returns focus. Any UI can go inside (a filter box, a tree, buttons). About a hundred lines; no dependency.
3. **Three things on top of it, each a few lines:** `tooltip(el, "text")` (hover and focus, a short delay, `role="tooltip"`); a **menu** (a list of actions, arrow keys, opens below or **to the side**); a **tree menu** — `ux/Tree` inside a `Popover`. Show all three on the page.
4. **The AI page's version picker becomes the tree menu:** in `public/framework/ai/page.js`, replace the `dropdown({ options: VERSIONS … })` with a trigger that hugs its label ("Version 1 ▾") and pops a small tree of the versions (`/framework/ai/` and whatever `public/framework/ai/v/page.js` declares — today `2`), built with `Tree.from(page)` or a nodes array. Picking one navigates. Keep it one short line under the title.
5. **Say what should move onto it later** (do not move them today): `ext/Dropdown`'s own placement, `ux/Menu`, the Panel overlays, and how many of the forty z-indexes it would retire — a count and the list, in `doc/decisions.md`.

## The owner's words, three additions for the version picker (2026-09-19, relayed by the coordinator)

> Three additions from the owner for the version picker on /framework/ai/ (step 4). (1) Names are just "V1" and "V2" — the mastermind has already renamed the two labels in public/framework/ai/page.js, so re-read that file before you edit it. (2) The title and the picker share ONE flex row: the "AI" h1 on the left, the picker hugging its label right beside it, vertically centred, wrapping under the title only at phone width. Do it without editing core: find how the page draws its title (core/Page/Page.class.js render(), `h1.page-title`, and how this page's catalog() lays out its intro) and choose the smallest honest way — e.g. the page draws its own head row and declines the default title — and say what you chose. (3) A "New version" action at the bottom of the picker's menu: on the dev server it scaffolds the next version — creates public/framework/ai/v/<n+1>/ by copying the newest version's page.js and its css (renaming the title to "AI v<n+1>"), adds <n+1> to the children string in public/framework/ai/v/page.js, and navigates there; the picker lists versions from that children string, so V3 appears by itself. Use the existing dev write RPC (find how Make or ext/Saver writes files through the socket — `rpc:write`), put the scaffold in one small new file public/framework/ai/v/new.js, and hide the action when there is no dev socket (production is static). Your fence widens by exactly: public/framework/ai/v/new.js (new) and the children line of public/framework/ai/v/page.js. A sibling (ai-v2) is editing public/framework/ai/v/2/** right now — read its files, never write them. Prove it by scaffolding a V3 on your private server, shooting the picker with V3 in it, then deleting the v/3 dir and restoring the children line (leave no V3 behind; the owner presses the button themselves).

## Prove

Private server `PORT=8136 node server.js` (background; kill by its real Windows PID at landing). Headless shots at 1280 and 400 of the comparison page with both popups open inside the hostile box (A visibly clipped or buried, B whole), the tooltip, the side menu at the right viewport edge (it must flip), and the AI page's tree menu open. Keyboard: Tab to a trigger, Enter opens, arrows move, Escape closes and focus returns — assert each. No console errors.

## Rules

- `new-task` before the first edit (your dir: `ai/2026-09-19/popover-system/`); `code`, `css`, `new-page`, `documentation`, `ui-test`; `finish-task`; `skill-improvement` if a skill misled you.
- **Fence:** `public/framework/ux/Popover/**` (new), `public/framework/ux/page.js` (the children line only — a sibling is adding `Dictate` to the same line today: re-read the file right before your one-line edit), `public/framework/ai/page.js` (the picker only), `public/framework/styles/css-scopes.txt` (register your prefix), your task dir. Not `ext/Dropdown/**`, not `ux/Menu/**`, not `ux/Tree/**`, not `core/**`, not `framework.css`.
- **Never kill or restart the owner's dev server (port 80) or the mastermind's (8123), never drive the owner's tabs.** Never `git stash`, never `find /`; an `rg` pattern starting with `/` returns nothing here. A bash heredoc containing an apostrophe fails in this harness — write files with the Write tool. Do not write the owner's name anywhere.
- Playwright: `import { chromium } from "file:///C:/Users/mike/AppData/Roaming/npm/node_modules/playwright/index.mjs"`; block the socket with `page.routeWebSocket(/.*/, () => {})`. Scratch in the session scratchpad.
- Landing `outcome`: one screen — the conclusion in a sentence, the two demos' link, what `Popover` is in three lines, the z-index count it could retire, what was left and why.
