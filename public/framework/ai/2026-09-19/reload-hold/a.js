// One of three placeholder modules page.js imports just so they show up as real
// loaded resources — the reload-hold PROVE step edits these three files while a
// hold is on, to show the batch queues as ONE reload instead of three. Nothing
// here matters beyond the string changing.
export default "a: unwritten";
