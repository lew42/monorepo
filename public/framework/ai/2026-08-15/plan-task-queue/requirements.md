# plan-task-queue

## The ask, verbatim

> if you finish before the second window expires, plan a new feature for the ai
> task management system. keep improving!

Planning only — the deliverable is `proposal.md` beside this file. No code.
Feature chosen: **the task queue** — the concrete form of "instead of the ai
driving, the task UI could facilitate? we could then have programmatic
control?" from the same conversation (2026-08-15). Runner-up folded in as a
companion: **steering a running task from its chat thread via the Stop gate**.
