# vision-measure — turn 2 gets the measurements; can it write a correct delta then?

Laws: less is more · clarity · prioritize. **Deliverable: decl precision on ≥ 20 deltas when turn 2 sees rects and computed colours beside the DOM outline; the same referee method as [`../vision-direction/quality.md`](../vision-direction/quality.md). Final message ≤ 25 lines.**

Where we are: directions **0.93** (the model names the right property and sign from the picture); decls **0.25** (n=4) — turn 2 retracts (0.75 right) rather than writes, and 3 of its 4 deltas were wrong about *rendered* state it could not see (`.dt-page` on a `.doc-page`; `repeat(5,1fr)` on five equal cards; dropped the `.muted` multiplier). Verdict of that task: *"the next knob is not a prompt — give turn 2 the measurements."*

## Do

1. **Measurements into turn 2.** In `run.mjs`, for every finding of a shot, before turn 2: run `ext/DesignTool/probe.js`'s node walk on the same url/width (headless, the way the region picker already does) and attach for the finding's `where` (best-effort match: the region sel if it has one, else the top-level container whose box the `where` phrase most plausibly names — log the match) — `rect {x,y,w,h}`, `computed {color, background-color, font-size, padding, margin, gap, grid-template-columns}` for that node, its parent, and its first three children; **contrast ratios** computed for text nodes (`color` vs effective background). Also fix `sheets()` so turn 2 is offered the css that actually cascades — the module css of the *page's* module (e.g. `/framework/ai/` → `ext/AITask/ai.css`; resolve via the page's `page.js` imports or a `data-module`), not only files in the url's own directory. Cap the measurement block at ~60 lines per finding.
2. `prompts/css-v4.md`: same as css-v3 (delta or retract for every finding), plus: *"The measurements are the rendered truth — trust them over your reading of the CSS. A delta must change a measured value in the finding's direction; say which value and by how much you expect."*
3. **Run**: `--replay` the same 6 pngs (`../vision-fixes/shots/`), Sonnet, `critique-full-v4` findings already exist in `../vision-direction/vision.jsonl` — reuse them (a `--from <run>` that skips turn 1 and takes the findings as input; if that's more than ~20 lines, run turn 1 again, it is $0.71) → `--turn2 css-v4` → `--out public/framework/ai/2026-08-17/vision-measure` (≈ $3–4; `--dry`; ceiling $6).
4. **Referee (you, Opus), same method as vision-direction**: rects, computed colours, pixels sampled, **every decl injected live** to see what moved. Precision for decls · retracts · the "expected value change" claim; by class. Two numbers that must agree: decls + retracts + no-CSS = findings. `quality.md` here (≤ 50 lines): the table, worst three, best three, $ per finding, and the verdict — **≥ 0.6 on decls with n ≥ 15 → the retract/decl buttons are worth wiring; else the dominant failure and whether another knob exists or the ceiling is the seat.**

## Rules

- Files: `ext/DesignTool/vision/{run.mjs,prompts/*,readme.md}`, this dir. Not `browse.js`/`page.js` (a `Preview fix` button was wired there tonight — leave it), not site CSS, not `probe.js` (import it; if it lacks something, copy the ten lines and say so). `code` skill once; `finish-task`. Log in `task.jsonl` (bash `printf`; timestamps from `date -Iseconds`); bump step. Wait for runs in the foreground. Never Mike's live tabs.
