# design-system — one page that IS the design system, a minimal spacing ladder from the census, and the AI cards as its first consumer

Load the `minion` skill first. Then this brief.

**Three laws.** Less is more (fewer names, not more; a ladder of three beats a ladder of five). Clear beats brief by far (a newcomer reads the page and knows which class to type). Prioritize.
**Length budget:** the page's level 1 is ONE screen at 1280: the tokens shown, the classes that request them, the ladder. Everything else one click down. Your landing report is one screen of plain sentences with links and the before/after counts.
**The reader is the overwhelmed newcomer.** Shown, not told.

## The owner's words (2026-09-17, 21:10)

> why do the ai cards use custom padding? i thought we're trying to build a minimal design system. if we need a new tool in the toolbox, we can potentially create one. what should i look at to see our padding? we had an imagine/design/padding/ page... but that just shows several intervals.. WE NEED A DESIGN SYSTEM. MINIMAL TOKENS USED WHEREVER POSSIBLE... .ac("pad") -> --pad? where is this documented?

And from the morning (the same voice): "in my Figma token system I was using like a standard padding and then a section padding, so maybe we have two different, kind of like a large and small, but then that can get carried away really quickly."

## What the census found (`ai/2026-09-17/spacing-census/census.md`, read it first)

1,894 spacing declarations in scope: **raw 1,064 (56%)**, **token × N 451 (24%) with 28 distinct N** (0.5 ×76, 0.4 ×61, 0.6 ×55 lead), token alone 125 (7%), zero 209, control-em 45. `ext/AITask/ai.css`: 55 declarations, 31 on-token (24 scaled), 12 raw, 6 control-em. The tokens live at `framework.css` 225–247 (`--pad --gap --flow` from one `--size` knob on `:where(*)`) and 635–641 (`.pad .gap .mb`). Twenty-five docs and seven pages explain them; no single page says "this is the design system".

## Deliverables (each ticked against the owner's sentences at harvest)

1. **`/framework/styles/system/` — the page that IS the design system.** Level 1, one screen: (a) the spacing tokens painted live at the current width — a `--pad` box, a `--gap` gap, a `--flow` gap, each labelled with its px right now, and the `--size` ladder 0.75 / 1 / 1.5 beside them; (b) the classes that request them, as the exact strings you type — `.pad` `.gap` `.flow` `.all-pad` `.mb` and whatever else framework.css offers (read 630–645; the `.ac("pad")` → `--pad` mapping is the owner's own example, show it as one line of code); (c) the ladder (deliverable 2) painted, with its class names; (d) the control rule in one sentence (a control's own padding is em, never a spacing token; link `/imagine/design/size/`). One click down: the census table, the type levels (`styles/doc/theme.md`), the color tokens (`/imagine/design/color/`), the layers, and links to every study that explains a piece. Add `system` to `public/framework/styles/page.js` `children:` AND make it the first line of the readme's Use (the readme is the reader's index; "the design system: [/framework/styles/system/]"). Count `a[href='/framework/styles/system/']` on `/framework/styles/` to prove the link.
2. **The minimal spacing ladder, from the census.** 28 multipliers cannot all be right. Propose the smallest set that covers them: the owner's own instinct is two levels (standard and section — that is `--pad` and `--flow` already) plus at most one tighter step. Fit the 451 token × N rows to candidate ladders (e.g. {0.5, 1, 2} or {0.5, 1} against `--gap`, with `--flow` as the section step) and report, per candidate, how many rows land within 15% of a rung — the ladder with the fewest rungs that covers ≥ 80% wins; say the number. Then ADD it to `framework.css` in the standard block, additively (nothing existing changes value): the rungs as tokens derived from the existing ones (`--gap-half: calc(var(--gap) * 0.5)` — name them plainly, the owner reads names) and one utility class per rung in the same style as `.pad`/`.gap` (`.gap.half`? `.gap-half`? read how `.size-small` and `.flex.three` are named and follow the house pattern; run `new-css-class`). The rule for the next author, written on the page in one sentence: *a spacing value is a token or a rung, never a multiplier and never a raw number; a control's own padding is em.*
3. **The AI cards as the first consumer.** Convert `public/framework/ext/AITask/ai.css` to the ladder: every `calc(var(--gap) * N)` becomes the nearest rung (or the plain token), every raw container padding/margin becomes a token or rung, control-em stays. Prove it: screenshot the run task's Asks tab and a day dashboard at 1280 and 3440 before and after; the after must differ from the before by less than 3% of pixels (compare with a quick PNG diff in node, or say the number from Playwright's `toHaveScreenshot` threshold) — the point is fewer numbers, not a redesign. Report the census row set for ai.css before and after (55 rows: on-token / rung / control-em / raw).
4. **Docs:** `styles/readme.md` Use leads with the page; `styles/doc/decisions.md` gets the dated record (the census, the candidates, the winner, the numbers); the `layout` skill's Spacing section gets ONE line naming the rungs (that is the only `.claude/` write allowed, and it is additive).

## Rules

- Load `code`, `layout`, `css` (read framework.css 220–250 and 630–645 yourself), `new-css-class`, `new-page`; `new-task` before the first edit (your dir exists: `ai/2026-09-17/design-system/`); `documentation` then `finish-task`; `skill-improvement` for any skill that misled you.
- **Fence:** `public/framework/styles/system/**` (new), `public/framework/styles/readme.md` and `page.js` (the link and the children word), `public/framework/styles/doc/decisions.md`, `public/framework/framework.css` (ADDITIVE lines in the standard block and the utility block only — never change an existing value or selector), `public/framework/ext/AITask/ai.css`, one additive line in `.claude/skills/layout/SKILL.md`, your task dir. Nothing else.
- ⚠ framework.css is loaded by every page and every other agent's server: make its edit in ONE write, then load a page headless and read the console before anything else. A backtick inside `css(\`…\`)` kills every page; every rule inside a layer.
- Never kill or restart the dev server, never drive the owner's tabs, never `git stash`, never `find /`. The owner's server (port 80) is NOT running; start your own: `PORT=8103 node server.js` from the repo root, in the background; kill it by PID when you land. `ui-test` has the headless recipe. A hidden tab does not lay out.
- Two numbers that must agree: ai.css rows before (55) and after; the ladder's coverage on the census json recomputed by you.
- **Resolve, don't park.** Findings as `log` lines; timestamps from the clock; never Out-File for jsonl.
- Landing: `outcome` = a headline (the page, the ladder with its coverage number, ai.css N numbers → M), the links, one screenshot of the page at 1280, what was left and why. One screen.
