# Everything the owner asked for in this run — with status (generated 2026-09-19T17:37-05:00)

One line per request: **id** (topic) — what was asked → what happened. Generated from the `ask` lines of `task.jsonl` beside this file (merged by id, the last line wins) by the script `asks-md.py` (a copy is beside this file); regenerate it rather than editing it. The owner's verbatim words are each ask's `quote` in the ledger, and every relayed prompt is a `chat` line (`from: "owner"`).

Totals: 104 done · 6 in flight · 9 open.

**New mastermind: start with the OPEN and IN FLIGHT items of 2026-09-19 — they are what the owner is waiting on.**

## 2026-09-19 — 54 requests

### OPEN (8)

- **sidebar-designs** (Layouts) — Work on sidebar UI designs (the dictation was cut off after this sentence). → Sidebar UI designs are queued behind the sidebar repair, so the designs start from rows and icon frames that are right. Tasks: `sidebar-designs`.
- **log-voice** (AI log) — The log evolves from raw words to refined items; speech transcribed live into the log, verbatim, timestamped, authored by the owner; optional audio recording for re-listening; a hover play button that reads an item aloud. → Queued behind the dev bar chat tab: live speech into the log through Whisper, optional audio kept locally, and a read aloud button using the browser's own voice. Tasks: `log-voice`.
- **spatial-timeline** (AI log) — V3 timeline view: full bleed, hour and minute lines, times outside the cards, adaptive gaps; important items must not sink under streams - a pinned strip, a Live toggle, the head row as the toolbar, auto-select a newly arriving card unless the owner has selected something. → Specified in full and queued as the next task on V3, to start when the Now view lands. Tasks: `spatial-timeline`.
- **fps-meter** (AI log) — Resizing columns spins the fans: investigate whether pages repaint too often (many resize listeners, container units everywhere); build a small frames-per-second meter in the dev bar; the owner remembers a layout where container units misbehaved. → Measured: resizing for three seconds gave 181 frames with none over 50 ms, and the spacing tokens made no difference; a trace of a real column drag and the frame rate meter are queued. Tasks: `fps-meter`.
- **change-shots** (AI log) — Proposal, refined by the owner: a skill fires an already-running Playwright monitor per page on every change, all resolutions into a set folder; the picture goes to the minion who made the change; a git-like record and UI of changes as they land, committed last if checks pass; measure the loop time; folds into the worktree flow. → Proposed as one card with four parts: the health watcher also saves a picture per change and the hook hands it to the editing agent; each landed task becomes a change record with before and after and an undo; worktree landings use the same one command; three skills change by a sentence each. Say do it. **Waits on the owner:** say 'do it' to the change-shots proposal (or the mastermind starts it when the budget has room) (1 min).
- **v3-prompts** (AI log) — Dashboard: a new item lands on top without moving scroll or selection, a New item button, Spacebar jumps to it and selects it; cards can carry yes/no controls so the dashboard can prompt the owner; a toolbar per column; cards referenced again rise and can embed a preview of another card; write no CSS unless needed. → The needless CSS rule goes now; the rest is recorded for the next V3 task, together with the spatial timeline. Tasks: `v3-timeline`.
- **timing-audit** (AI log) — Measure timing end to end - prompt arrival, echo, card, relay, mastermind pickup, UI update - and show it visually on the timeline. → Queued as a small task; two hops are already measured (echo about 3 s, file to screen 13 ms). Tasks: `timing-audit`.
- **dashboard-first** (AI log) — The owner wants to leave the VS Code chat and live in the AI dashboard: the assistant and the every-prompt skill should lean into dashboard cards as interactive UI - things shown as they are designed, revisable - instead of chat text; the owner reads none of the chat. → Direction recorded for the new mastermind: the dashboard is the interface. Already true: every answer is a card. In flight: cards with buttons, the streaming message box on the page. Next: designs shown as live, revisable cards.

### IN FLIGHT (6)

- **ai-v2** (AI log) — A second version of the AI dashboard at ai/v/2: full screen beside the sidebar, where the mastermind can put content on the screen in real time. → AI v2 exists and is live: I append one line to the run's log and it is on your screen, no reload. Tasks: `ai-v2`. [AI v2](/framework/ai/v/2/) [the version picker on the AI page](/framework/ai/)
- **verbatim-echo** (AI log) — Every owner prompt shows word for word in the dev bar log the instant it is submitted, before any answer; how to find a VS Code tab's session id. → The assistant now echoes your exact words to the log and my inbox as its first act; a hook will do it mechanically at the moment you send, and the dev bar will draw your cards as yours. Tasks: `prompt-relay`, `devbar-chat`.
- **full-log** (AI log) — The dev bar becomes a full log: every owner prompt verbatim at once; the assistant, the mastermind and any minion can create or stream a log item; prove the assistant-to-mastermind channel; how the inbox is discovered and how fast. → Anyone on the team can now post or stream to the log with one command under their own name; the channel is proven (eight relays, each by doorbell and inbox); my real reply delay is 12 to 30 seconds, and the instant layer is the streaming assistant being built. Tasks: `devbar-chat`, `assistant-stream`, `prompt-relay`.
- **answer-cards** (AI log) — When the owner asks a question the answer appears at once as a big visual card; card size follows current focus; the assistant's card can be marked as the focus. → A card can now be marked as the current focus and the assistant marks its answers that way; the dashboard growing the focus card is with the V3 minion. Tasks: `devbar-chat`.
- **realtime-v3** (AI log) — V3 must be real time; a pure append-only streaming log whose entries can also transform or append to existing cards; build the timeline grid; nothing should wait on the owner unless dangerous - audit what is on hold; monitor V3's console errors and relay them to its minion at once; never two minions on one page; cards of random widths. → Every held decision is made and moving; V3's defects are being relayed to its one minion from headless shots and the health log; both rules are in the mastermind skill. Tasks: `devbar-chat`, `card-word`.
- **v3-card-look** (AI log) — V3 cards: no black border; a less white fill that lightens on select; the requested layout (full bleed, two equal resizable columns, a left timeline with hour lines) is still not right. → Done on V3: no dark border; unselected cards a step off white, the selected one white. The full-bleed timeline layout is the next task, brief ready; framework.css's own .selected should follow. Tasks: `dashboard-next`, `v3-timeline`.

### DONE (40)

- **reload-on-load** (AI log) — Pages reload themselves right after loading; find what triggers it and stop it. → Pages reloaded because reading a file counts as a change on Windows; the watcher now ignores reads. Your server has the fix after one restart. Tasks: `server-self`. [the fix and its proofs](/framework/ai/2026-09-19/server-self/)
- **server-self** (AI log) — The mastermind runs and manages a dev server itself; a second identical server on another port; whether several watchers can coexist; no more 'restart the dev server' items for the owner. → The dev server restarts itself when its own code changes, and several servers run side by side fine; I run my own on port 8123. Tasks: `server-self`. [the fix and its proofs](/framework/ai/2026-09-19/server-self/)
- **sidebar-messed-up** (Layouts) — The site sidebar looks wrong: rows too roomy, the filter double-boxed, the footer a big white block instead of one button row, icons too high. Filter as a darken bar with a white field; icons in square frames sharing one centre line; an anonymous person in the avatar; find out whether anyone looked at it. → The sidebar reads like a file tree again: 32 px rows, visible arrows, centred icons, a gray filter bar with a white field, a one-row footer; nobody had looked at the whole rail before. Tasks: `sidebar-repair`, `blunder-critic`. [before and after, every width](/framework/ai/2026-09-19/sidebar-repair/) [the critic's six findings across 60 pages](/framework/ai/2026-09-19/blunder-critic/)
- **message-on-page** (AI log) — Write something on the run page right now, to see how fast it shows up. → You saw the hello; messages now go in the notes panel of AI v2, written live. [AI v2](/framework/ai/v/2/)
- **run-page-compact** (AI log) — The run page wastes space: compact it; look at the design system's scale controls; some text is already small. → The dense V2 board is finished: newest run found by itself, a density switch, no wasted top or sides, headings that never split. Tasks: `ai-v2`. [AI v2](/framework/ai/v/2/)
- **whisper-local** (AI log) — Dictation shows nothing; set up whisper.cpp locally with large-v3-turbo outside the repo, wire it to the dictation UI, make that UI a proper ux component; words should appear while talking. → Local Whisper dictation works, and after your one server restart the dev server starts Whisper by itself. **Waits on the owner:** try Dictate with your own voice and say if it cuts you off or waits too long (the thresholds are first guesses) (2 min). Tasks: `whisper-local`. [try Dictate](/framework/ux/Dictate/) [the install and the proofs](/framework/ai/2026-09-19/whisper-local/)
- **popover-system** (Layouts) — A dropdown on the AI page listing the versions as a tree; a proper tooltip, pop-up and menu system; explore both placements - all popups in one top element versus popups beside their content - and build both. → The top layer wins: a popup written beside its content but painted above everything, never clipped; a Popover class with tooltip, side menu and tree menu exists. Tasks: `popover-system`. [both options, side by side](/framework/ux/Popover/) [the picker](/framework/ai/)
- **version-picker** (AI log) — The version picker: name the versions V1 and V2; the AI title and the picker share one flex row; a button makes the next version (V3) automatically. → The picker says V1, V2, V3 beside the AI title, and New version makes the next one by copying the newest. Tasks: `popover-system`. [the picker](/framework/ai/)
- **ai-v3** (AI log) — A V3 of the dashboard: the same layout, blank, one card that says ready; spacing from tokens that grow with the page in container units; the mastermind answers on this page instead of in the chat. → V3 is live and I answer there: each thing you say becomes a card, updated as the work moves; a card can show code and the code running. [AI v3](/framework/ai/v/3/)
- **never-break-the-page** (AI log) — Minions must not break the live page; audit mistakes as they happen with a minion that studies the skills - reshape or remove rules, not only add; a decision auditor, skill-use reports, a persistent auditor instance. → The rules existed; the failing agents never loaded them. A hook now stops a broken write at once, an auditor skill exists, and five proposals wait for you. Tasks: `mistake-audit`. [the audit, one screen](/framework/ai/2026-09-19/mistake-audit/)
- **background-layer** (Design system) — A modular, swappable background layer: a div with class background, inset 0, behind the content - a colour, a design, a repeated icon as texture, scattered icons, vectors and shapes; maybe wrap the content out of habit. → One line puts a swappable background behind any box: ten kinds, no images, every one readable; wrap the content, because unwrapped content paints behind the layer. Tasks: `background-layer`. [the wall of backgrounds](/framework/ui/background/)
- **devbar-chat** (AI log) — Can the mastermind reread every past session, minions included? A chat window in the dev bar, the default tab, updating live; the owner's prompts live only in VS Code tabs today. → The dev bar has a path bar, a log that evolves and never truncates, and a sessions browser with every past session and minion. Tasks: `devbar-chat`. [AI v3, the inbox](/framework/ai/v/3/) [the dev bar record](/framework/dev/DevBar/doc/chat/)
- **reload-hold** (AI log) — A live reload blocker agents use around a batch of writes: hold every reload on every page, write, then one reload; written into CLAUDE.md; then look into hot module reloading. → One command holds every reload during a batch of writes, then one reload; it is in CLAUDE.md and the minion skill. Style changes already repaint with no reload. Tasks: `reload-hold`. [the hold, live, and the hot-reload study](/framework/ai/2026-09-19/reload-hold/)
- **devbar-path-bar** (AI log) — The dev bar's route section becomes a slim path bar in place of the word DEV: a home icon, the path in the code font on one solid background, slashes between parts, each part clickable with its own hover; no label. → The word DEV is now a slim path bar: home icon, the path in the code font, each part clickable. Tasks: `devbar-chat`. [AI v3, the inbox](/framework/ai/v/3/) [the dev bar record](/framework/dev/DevBar/doc/chat/)
- **keep-the-log-fresh** (AI log) — Updates take too long; keep the log fresh so the current state is always visible; use minions for the work. Written into the mastermind skill. → Written into the skill: post first and work second, a state card on every change and at least every ten minutes, a landing card within a minute, and the mastermind never hand-edits the screen you are reading.
- **assistant-mode** (AI log) — An assistant mode: a light, fast session (Sonnet, low effort) whose sole job is to answer the owner at once and relay their words to the mastermind, so the mastermind keeps working with a lean context. → The assistant works end to end: you spoke to it, it rang me directly and wrote your words in my inbox, and I answered in the dev bar. [the board it writes to](/framework/ai/v/3/)
- **page-health** (AI log) — An is-it-working watcher: a hidden browser loads the pages being worked on after every change, and any error is passed at once to whoever made the edit; everything logged, with archiving; and stop the unknown verb note console errors. → A hidden browser checks the pages each edit could break and tells the agent that broke one at its next write; the note errors are gone; findings are logged by day and archive themselves. Tasks: `page-health`. [page health, live](/framework/ai/health/) [the task and its proofs](/framework/ai/2026-09-19/page-health/)
- **stream-with-block** (AI log) — With Block ticked the chat stream stops updating until a reload; Block should stop reloads only. → It was never Block: the board's live stream went deaf after its first load because of a url mismatch. Fixed; refresh once. Tasks: `reload-hold`. [the hold, live, and the hot-reload study](/framework/ai/2026-09-19/reload-hold/)
- **mastermind-log** (AI log) — Explain how the assistant passes messages (real time or a polled log); the dev bar log's cards should evolve in place rather than only append; rename the title to the mastermind log. → The assistant rings my session directly and also writes the inbox; the wait is my step time. Evolving cards and the new title are with the dev bar minion. Tasks: `devbar-chat`. [AI v3, the inbox](/framework/ai/v/3/) [the dev bar record](/framework/dev/DevBar/doc/chat/)
- **prompt-relay** (AI log) — The assistant relays every prompt automatically and is reminded who it is on every prompt; use hooks for it. → The hook that echoes and relays every assistant prompt mechanically, and logs all your prompts by day, is built and tested; it is off until you say yes to one settings entry. **Waits on the owner:** say yes to the prompt hook in the mastermind tab (one settings entry) (1 min). Tasks: `prompt-relay`.
- **joint-timeline** (AI log) — A request for the owner's yes appears as a card in the log; cards on the left plus a log on the right is two places to look - one joint timeline instead; timestamps are on the wrong side of the log items. → One joint timeline is being built: you, the assistant and me in one stream, shared by the dev bar and the V3 page, with needs-you cards pinned on top; the timestamp regression goes back to the left. Tasks: `devbar-chat`. [AI v3, the inbox](/framework/ai/v/3/) [the dev bar record](/framework/dev/DevBar/doc/chat/)
- **assistant-stream** (AI log) — Replies should stream: the socket sends each new chunk as an append-only action and the log appends it as it arrives, so a reply types itself out. → Replies type themselves out, and the message box is on the page: in V3's Now view and in the dev bar log. Tasks: `assistant-stream`. [the streaming demo](/framework/ai/2026-09-19/assistant-stream/)
- **site-down-audit** (AI log) — The site went down: get it back up, find who or what broke it, file a report, and audit why it broke and how to stop it happening again. → Fixed for good: a changed server is tried on a spare port first and the healthy one is kept if it fails; the self-restart can no longer get stuck. One hand restart of yours picks it up. **Waits on the owner:** restart node server.js once - safe now, the mastermind's own server is already running the new code (1 min). Tasks: `incident-site-down`. [the incident report](/framework/ai/2026-09-19/incident-site-down/) [the boot test and its eight proofs](/framework/ai/2026-09-19/server-boot-test/)
- **idea-cards** (AI log) — Dev bar = the detailed real-time log; the AI dashboard = big idea cards by importance. White cards with a colored left edge only; every card a real route; nested cards with a top-three preview of what is inside; say how the wall is sorted; two equal columns; the standard pad. → The dashboard's cards: white, a colored left edge for status, each with its own URL, a top-three preview of the cards inside it, sorted by importance with a newest-first switch. With the dev bar minion, to land as one finished batch. Tasks: `devbar-chat`. [AI v3, the inbox](/framework/ai/v/3/) [the dev bar record](/framework/dev/DevBar/doc/chat/)
- **padding-audit** (Design system) — Dev bar log cards: text pushed right unevenly, ~3 px padding - one standard pad for all cards; audit why padding decisions keep going wrong, what the skills say, and whether containers are declared so container units work when nested. → Built: a card class padded by definition, the one-line spacing rule in three skills, and two lint checks in the page health watcher. Tasks: `padding-audit`, `devbar-chat`. [the padding audit](/framework/ai/2026-09-19/padding-audit/) [the card, live: region, card, control](/framework/styles/system/)
- **assistant-cards** (AI log) — The assistant, being fast, should be the one that updates the live dashboard: the prompt shows at once and turns into its summary card right away; decide how it gets write access; color-code the cards. → Decided: the assistant writes each dashboard card itself with one command - a topic id it reuses so cards evolve, an icon, a status, and a pointer to the prompt it answers; I refine the same card afterwards.
- **multi-level-assistants** (AI log) — A multi-tier design: a fast Sonnet assistant acts at once and can spawn Opus masterminds; each mastermind owns a worktree and spawns minions into it; sub-worktrees; run several masterminds toward one target, compare the versions, the owner picks. Worry: masterminds coordinated by a lesser model lose understanding. Wanted: feasibility, the coordination design, and cost. → Nine masterminds is possible but wasteful, about thirty task-units with eight versions thrown away; start with one mastermind comparing three worktree versions of a real design question, and measure. Tasks: `worktree-study`. [the worktree study, with the many-masterminds section](/framework/ai/2026-09-19/worktree-study/)
- **assistant-channel** (AI log) — The owner still did not know how the assistant talks to the mastermind (poll? watch?) and asked for one big card explaining the pathway and the real delay; could not find the two needs-you cards; meaningful card icons should be much bigger. → One card says it: a direct session-to-session message, about a second to deliver, no polling; the 12 to 30 seconds is my own turn. Bigger icons for meaningful cards go in the next V3 pass.
- **worktrees-and-cache** (AI log) — Study git worktrees for parallel minions (so their writes do not reload the live site and they do not step on each other) and what the one-hour prompt cache changes; where is yesterday's worktree answer. → Worktrees are usable today even with 1,500 uncommitted files. Decided: use them for server and shared-module edits, the two kinds that took the site down; fences elsewhere. Resuming an agent inside the hour is cheap. Tasks: `worktree-study`. [the worktree study, one screen](/framework/ai/2026-09-19/worktree-study/)
- **devbar-questions** (AI log) — Four questions about the dev bar: slow streaming; why both a Chat tab and a Page log; what the sessions dropdown is for; what 'Reply with just the word one' is. → Answered as four nested cards. Decision: the log stays the default; the Chat tab becomes a Sessions browser for history, with useful labels and test runs hidden.
- **current-view** (AI log) — A persistent 'current' view on the AI dashboard: the thing happening now in full detail, usable with the dev bar closed; the owner's exact words appear the second they send as a transcription card, then refine into the summary; the main wall stays refined; measure and cut the real prompt-to-card delay. → V3 opens on Now: your newest words in a transcription card, the answer streaming under it, and a message box with the mic on the page itself. Tasks: `dashboard-next`. [AI v3: Now, grid, timeline](/framework/ai/v/3/) [the task and what was cut](/framework/ai/2026-09-19/dashboard-next/)
- **v3-layout** (AI log) — AI v3's layout: two equal resizable columns - a left rail of small preview cards in timeline order, newest on top; the selected item in full on the right, first one selected at load; clicking routes (the URL changes) without switching pages; cards are dynamic, from the log. → V3 is a two-column inbox: large preview cards newest first, the picked card in full on the right, a drag handle, a real address per card. Tasks: `devbar-chat`. [AI v3, the inbox](/framework/ai/v/3/) [the dev bar record](/framework/dev/DevBar/doc/chat/)
- **servex** (Pages) — Study the Servex repo (a server manager that used PM2): structure, a probably stale server submodule, whether it works, and whether to move it in beside Server/ or rewrite it small with our own UI and config; one decision card. → Rewrite it small here: each server registers itself in one small file and one script lists or stops them; no Servex code, no PM2. **Waits on the owner:** run `pm2 kill` once - checking PM2's version left an idle PM2 background process (1 min). Tasks: `servex-study`. [the Servex study](/framework/ai/2026-09-19/servex-study/)
- **speed-and-reuse** (AI log) — Changes arrive slowly: a one-line change should never queue; keep minions alive and reuse them for follow-ups; consider a worktree per minion; requests must be documented less wordily on the dashboard; V3 full bleed with the timeline and the usage bars in a footer. → The CSS line was already deleted by the page's minion; one-line changes are done at once from now; landed minions are woken again for follow-ups (the auditor three times today); the full-bleed timeline with the usage footer is briefed and next.
- **process-redesign** (AI log) — Everything is slow: fix the process, or shut down and restart? And: can a new mastermind pick up where this one left off - exact steps to restart without losing anything. → Three steps: a new Claude tab on Opus; say 'you are the mastermind, begin' and point it at the handover; close the old tab after its first card. Everything said is on disk - the inbox, the dashboard file, every task log. Do not resume the old session: it brings back the same slow context. **Waits on the owner:** when the agents in flight land: start a fresh mastermind tab (Opus), say: you are the mastermind, begin - read the handover (2 min). [the handover for a fresh mastermind](/framework/ai/2026-09-17/mastermind-layout-browser/handover.md)
- **v3-cards-css** (Design system) — V3 card CSS: selection is unclear (orange border, odd ground); a radius token belongs in the framework card class; V3's tile should simply BE the card class; no flex column on cards unless needed; audit V3's CSS and delete what the framework already does - too much CSS is breaking the system. → V3's cards are the framework card class now; its stylesheet was audited onto framework words. Tasks: `card-word`, `dashboard-next`. [AI v3: Now, grid, timeline](/framework/ai/v/3/) [the task and what was cut](/framework/ai/2026-09-19/dashboard-next/)
- **every-prompt** (AI log) — Rename the assistant skill to every-prompt (re-invoked on every prompt), updating every reference; add the two-tier design: a fast Sonnet assistant and a master assistant that receives every prompt after the relay. → Renamed, with both tiers defined; every reference updated, both names accepted by the hooks, and the old command path kept as a forwarder so nothing in flight broke.
- **skill-roles** (AI log) — Skill shape and roles: every-prompt stays minimal and names load-this-if sub-readmes; a master assistant skill that only supervises, answers the fast assistant with short opinions, documents how the process is going on a page, and doubles as the system auditor; assistants sort, log and route to the proper independent mastermind; masterminds do the deep work and spawn minions in parallel. → every-prompt is a third of its size with load-this-if files; a master assistant skill supervises, opines, documents the process and audits; one table names the four tiers. Tasks: `skill-roles`. [the roles, one screen](/framework/ai/2026-09-19/skill-roles/)
- **asks-log** (AI log) — Handover task: one log of everything the owner asked for this session, with status (done, in flight, open), where the new mastermind reads it first. → asks.md sits beside the handover: every request with its status, newest day first, generated from the record so it cannot drift. [everything asked, with status](/framework/ai/2026-09-17/mastermind-layout-browser/asks.md)
- **card-replies** (AI log) — Can the dashboard talk back to a session: a card asks a question with two buttons or two option cards, the owner picks one, and the choice reaches the right session? → Yes, built and proven: a click writes the answer on the card and into the inbox in 13 ms, and rings the right session in about 10 seconds. Any card can now carry buttons; V3's own cards get them in the next V3 task. [try it: a card with buttons](/framework/ai/2026-09-19/card-replies/)

## 2026-09-18 — 31 requests

### OPEN (1)

- **columns-lost** (Layouts) — The imagine Miller columns: things jump, columns switch out and you lose where you were, and every column is the same light gray - a sea of switching. → —

### DONE (30)

- **mastermind-makes-calls** (AI log) — For the calls left to the owner (the spacing ladder, the column count and the rest): decide, document the alternative or make a toggle, and build it; written into the mastermind skill so it never defers again. → — Tasks: `calls-core`, `calls-styles`. [the freeze and the fit toggle](/framework/core/Page/doc/columns/) [the ladder and the gap cap](/framework/styles/system/)
- **model-downgrade** (AI log) — When token usage runs low, lean on Sonnet and lower effort instead of stopping; test the lesser models on real tasks and see how they do. Written into the mastermind skill. → —
- **site-sidebar-tree** (Layouts) — The persistent left sidebar as the desktop essential: vertical tabs that fill 3440, a tree for deep navigation; predefined workspaces where clicking a tree item opens a main area plus a contextual right sidebar for properties. → The site's sidebar is the tree: sticky, resizable, remembered. Tasks: `site-sidebar-tree`. [the sidebar, a tree now](/framework/core/Sidebar/) [the workspace note](/framework/core/Sidebar/doc/decisions/)
- **mobile-lists** (Layouts) — List UI for mobile: the tree as a single column is the mobile shape; nesting cards costs a level of padding each, so only two levels work at 400; study it. Written into the layout skill. → Three nested cards leave 295 px at 400; the tree is tighter than a card. Tasks: `mobile-lists`. [nesting, measured at 400](/imagine/design/spacing/nesting/) [the task](/framework/ai/2026-09-18/mobile-lists/)
- **list-ui-shapes** (Layouts) — Explore single-column list UI: title size, a title bar with menu and settings buttons, accordion in a zero-gap stack, click that drills deeper or launches or opens a new column, an inbox-style preview rail that launches the full view; test them on real content like a team of users, members and groups with drag to assign. → Six single-column list shapes on one wall, each pressable. Tasks: `list-shapes`. [six list shapes, pressable](/imagine/design/lists/) [the task](/framework/ai/2026-09-18/list-shapes/)
- **memery-topics** (Pages) — Past work on the topic system, 'memery': each topic page is a team or community people join; a meme is a visual word-title page (e.g. failure to launch) with visuals, a video, a community sharing stories; Discord-like live chat, maybe voice. Report what exists. → Memery is the topic program; a live chat room exists locally; joining and sign-in were never built. **Waits on the owner:** register a GitHub and a Google OAuth app (for real sign-in) (15 min). Tasks: `memery-scout`. [memery: what exists, what was decided, the gap](/framework/ai/2026-09-18/memery-scout/report/) [the topic program](/imagine/platform/)
- **suggestions-not-laws** (AI log) — Skills state suggestions, not laws: should and could, not do and don't; nesting on one ground with indent is fine, a separate background wants padding, flush or spaced siblings show relatedness; nothing becomes a law until proven time and again. → — [the sentence, in the skills](/framework/ai/2026-09-17/mastermind-layout-browser/requirements/)
- **owner-dictates** (AI log) — The owner dictates from a phone and rarely reads replies: do the things asked; direct questions get a short chat answer, rhetorical ones and thinking-out-loud go in the report; the report is a visual hierarchy of requests, tasks, links and previews ranked by work or outcome, tangents as footnotes. Written into the mastermind skill. → — Tasks: `report-order`. [the report, ranked, with folds](/framework/ai/2026-09-17/mastermind-layout-browser/)
- **reply-in-place** (AI log) — A reply and a microphone on every item of the hierarchy (asks, tasks, decisions): the reply reaches Claude with that item as context so nothing is re-explained; as the owner dictates, asks and decisions appear live and minions start immediately. → Reply or speak on any item; the answer lands beside it. Tasks: `reply-in-place`. [press reply or the mic on any item](/framework/ai/2026-09-18/reply-in-place/) [how it works](/framework/ext/Ask/doc/reply/)
- **workflow-wall** (Pages) — Are the platform's user interfaces built? Lean into the demo-app system: a grid of workflow cards, each a click-through demo (landing to sign-up to logged in) with the click count; Google and GitHub sign-in through the raw APIs, not an aggregator; the avatar from the provider or Gravatar. → Every sign-in flow is two clicks to logged in, on one wall. Tasks: `workflow-wall`. [six sign-in workflows, click counts live](/imagine/platform/workflows/) [the endpoints and the decision](/imagine/platform/workflows/doc/decisions/)
- **pace-under-by-20** (AI log) — Pacing: stay about 20 points under the pace line on average by scaling back model usage, so the run keeps working instead of shutting down to let the window catch up. Written into the mastermind skill. → — [the sentence, in the skill](/framework/ai/2026-09-17/mastermind-layout-browser/requirements/)
- **ux-previews** (Layouts) — Link ux/Auth and the ux tier in the report; every UI and UX thing with an interface should have a preview card (a one-column mobile card or a full-bleed section) - still being figured out. → — [ux/Auth - login, signup, reset, a social row](/framework/ux/Auth/) [the ux tier: eight workflows](/framework/ux/)
- **essence-over-details** (AI log) — Directions are requests: achieve the core essence; the details are not locked in stone. Written into the mastermind skill. → —
- **live-request-log** (AI log) — As soon as a message is sent, its structure appears fast: a chronological log with one condensed row per item asked (a compound ask summarized to one row), later fleshed into requirements; the decisions to figure out surface with their paths and caveats. → — Tasks: `reply-in-place`. [dictate at the top of the Asks tab; cards appear as you speak](/framework/ai/2026-09-17/mastermind-layout-browser/)
- **approve-any-page** (Layouts) — Approve or improve on any page or sub page, dynamic or real, feeding an approved layout library the AI leans on, plus a way of learning which layout to use when and how to make it responsive; demo-worthy UX first, functionality after. → Approve or improve any layout page from its own corner control; the approved ones gather in one library. Tasks: `approve-any-page`. [the approved library, live](/layouts/doc/studies/approved/) [the control on its own page](/framework/ext/Ask/)
- **site-study-haiku** (Pages) — Test Haiku in parallel: study every section and page of the site - what is this thing, its core concepts, major components and features, its demos and how much of it each demo shows (demo coverage), the gaps; a short icon blurb per thing with a click-through. → 68 things scored: mean clarity 3.9 of 5, demo coverage 62 percent. Tasks: `site-study`. [68 things: blurb, clarity, coverage, stage](/framework/ai/2026-09-18/site-study/study/) [the task](/framework/ai/2026-09-18/site-study/)
- **demo-stage-width** (Layouts) — Demo stages go full bleed and at 3440 a 1000px thing spreads to 3000px and looks funny; study every demo, demo app and stage for whether it bleeds and whether it should, and settle a default stage width. → Walls reflow fine at 3440; the one stretch, a short band's cards, is capped at 28em now. Tasks: `site-study`, `stage-cap`. [before and after at 3440](/framework/ai/2026-09-18/stage-cap/) [the stage verdict](/framework/ai/2026-09-18/site-study/study/)
- **problem-solution-spaces** (AI log) — Do we document problem and solution spaces in the decision system? (In the report: the decision verb's paths and caveats, and the research program's topics with verdicts, are that.) → — [a Decisions tab: paths and caveats](/framework/ai/2026-09-17/decisions-system/) [research topics with verdicts](/imagine/research/)
- **steps-beside-demo** (Layouts) — A two-column layout for pages, tasks and reports: the demo (or a screenshot) on one side and minimal explanatory steps on the other - first click here, then do this - so a newcomer experiences the whole feature set and knows what to look at first. → A page can open with its demo beside numbered steps that tick off as you do them. Tasks: `steps-demo`. [the tree page: three steps beside the tree](/framework/ux/Tree/) [demo.steps()](/framework/ext/demo/doc/method/steps/)
- **clarity-over-dryness** (AI log) — Clarity is almost more important than simplicity: simplicity stays law one, but a blurb shortened into something dry that loses the essence is not simpler; a few extra or better words help. Text that stands alone without a demo is much less useful; screenshots need one scale and organization. → — [in the mastermind skill; CLAUDE.md law 2 already says clear beats brief](/framework/ai/2026-09-17/mastermind-layout-browser/requirements/)
- **minion-context** (AI log) — Minions keep their context: the files loaded and the work done stay fresh, so a follow-up continues the same minion and a minion is never switched between contexts; cheap Haiku and Sonnet minions serve as researchers to throw questions at. Written into the mastermind skill. → —
- **overlap-study** (Pages) — Study the overlap between the framework code and the imagine work and between similar feature sets across the site; find where one implementation can absorb another and be used everywhere - simplification in CSS, layout and navigation; a few core navigation systems, the left sidebar with filters, toggles or trees first. → Three real duplicates remain; five merges decided, two built, three in flight. Tasks: `overlap-study`. [the overlap study: nine concepts, five merges](/framework/ai/2026-09-18/overlap-study/overlap/) [the task (five decisions)](/framework/ai/2026-09-18/overlap-study/)
- **related-sidebar** (Layouts) — An internal right sidebar for related topics on many pages: related links as icon and title, resolved from the target page so they update when it changes. → Any page can list related pages by url; their icons and titles come from the pages themselves. Tasks: `related-sidebar`. [related: on a page](/framework/core/Page/doc/property/related/) [a consumer](/layouts/browse/)
- **link-tracking** (Pages) — Track where every page is imported and linked, so a drag-and-drop move updates all the paths automatically; keep that meta in page.json where the page.json system works. → Every link and import to a page is known; moving a page rewrites them all, and undo puts them back. Tasks: `link-tracking`. [the census and the proof](/framework/ai/2026-09-18/link-tracking/) [the record](/imagine/paging/make/doc/decisions/)
- **spatial-trees** (Layouts) — Spatial layouts like Whimsical's flow charts: a tree generator (a, a1, a1a) shown as a family tree, a flow chart and a brainstorm with plain CSS layout, on a canvas you scroll, swipe and pinch; click to expand or recentre; the mobile framing (centred vs top-left root, pan-only full screen) thought through. → A tree draws as a family tree, a flow chart or a brainstorm with plain CSS; on a phone the root sits at the top. Tasks: `spatial-trees`. [the trees lab](/layouts/labs/trees/) [the mobile framing numbers](/layouts/labs/trees/doc/decisions/)
- **page-json-core** (Pages) — A directory with only a page.json is a page (page.js, then page.json, then .md); data: true or an inline POJO loads a page's data before render; page.json doubles as the directory; snapshot plus log; Make becomes an editor over core's tree. Decided; Opus builds it when core is free. → A page says whether it has data, and a folder with only a page.json is a page; Make edits core pages now. Tasks: `page-json-core`. [data: on a page; page.json as a page](/framework/core/Page/doc/data/) [a page.json with no page.js, open as a core page](/imagine/paging/made/notes/)
- **edit-mode** (Pages) — One edit mode: on the dev server (or when signed in with the role) the create, drag and edit controls show; in production they are hidden so they never break or confuse; what the platform decided about users, roles and permissions is reported. → One edit switch: on with the dev socket and a rail checkbox, off in production, so no editor button ever shows where it cannot work. Tasks: `edit-mode`. [the switch and the roles paragraph](/framework/ext/Ask/doc/decisions/) [the task](/framework/ai/2026-09-18/edit-mode/)
- **conclusions-first** (AI log) — Every report item leads with its conclusion as the title; what was asked, the reasoning and the references sit one click down; items sorted easy-and-important first; a reading-time estimate only when digging in takes more than a few minutes. Written into the mastermind skill. → Each report item leads with its conclusion; what you asked is one click down. Tasks: `conclusions-first`. [the report, conclusions first](/framework/ai/2026-09-17/mastermind-layout-browser/)
- **page-data-flag** (Pages) — A page declares whether it has data: data: true loads its page.json (awaited before render), or a POJO inline; pages without it load nothing - no probing, no 404s; page.json doubles as the directory; snapshot plus append-only log, validated against each other; a dynamic child is reached through its parent. → data: true or an inline object loads a page's data before it renders; pages without it load nothing. Tasks: `page-json-core`. [data: on a page; page.json as a page](/framework/core/Page/doc/data/) [a page.json with no page.js, open as a core page](/imagine/paging/made/notes/)
- **worktree-minions** (AI log) — Look into git worktrees for minions so parallel edits cannot collide; test a worktree workflow, including a server per worktree for screenshots. → Worktrees isolate perfectly but see only what is committed on main; with weeks of uncommitted work on michael/dev they are not usable yet. Fences stay the clash control. Tasks: `worktree-test`. [the decision, below](/framework/ai/2026-09-17/mastermind-layout-browser/)

## 2026-09-17 — 34 requests

### DONE (34)

- **ai-log-asks** (AI log) — The AI log page for this session shows everything you asked for: summarized, each linking to the original prompt in the session, listed as previews. Simple, visual, interactive. → This page lists what you asked, grouped, ranked, each linked to your words. Tasks: `ai-log-asks`. [the asks view](/framework/ai/2026-09-17/mastermind-layout-browser/) [the prompt, in the session](/framework/ai/2026-09-17/mastermind-layout-browser/?m=534b8723-d1a1-4eab-a4b6-c1b3a8138351)
- **ai-log-click-through** (AI log) — An item per ask (e.g. 'AI log revamp'); click through to what was discussed and which decisions were weighed and made. Iceberg UX: simple first, detail nested. → — Tasks: `ai-log-asks`. [the asks view](/framework/ai/2026-09-17/mastermind-layout-browser/) [how it works](/framework/ext/AITask/doc/asks/)
- **session-as-chat** (AI log) — The session log reads like a chat conversation with flow and rhythm, and can be clicked through, not a big text dump. → — Tasks: `ai-log-asks`. [the prompt, in the session](/framework/ai/2026-09-17/mastermind-layout-browser/?m=534b8723-d1a1-4eab-a4b6-c1b3a8138351) [how it works](/framework/ext/AITask/doc/asks/)
- **requirements-report** (AI log) — For every requirement, a report that shows exactly what was asked for and how closely it was adhered to. → — Tasks: `ai-log-asks`. [the asks view](/framework/ai/2026-09-17/mastermind-layout-browser/) [the task](/framework/ai/2026-09-17/ai-log-asks/)
- **layout-browser** (Layouts) — A layout browser: every layout on the site in one place, global page layouts kept apart from smaller templates like mobile card UI, so you can go layout by layout and approve or say what must change. → Every layout on the site is one wall you can approve or improve. Tasks: `layout-browser`. [the browser](/layouts/browse/) [the task](/framework/ai/2026-09-17/layout-browser/)
- **minion-skill** (AI log) — A minion skill: when a minion is spawned it is pointed at its specific task and the handoff rules. → Every minion loads one skill first and lands the same way. Tasks: `minion-skill`. [the task](/framework/ai/2026-09-17/minion-skill/)
- **lighten-darken** (Design system) — Lighten and darken colors: white and black at an alpha over shades of white and gray; watch the accent's saturation, because contrast ratios go out of whack fast. Experiment with it. → Lighten and darken are six aliases; saturation's effect on contrast depends on hue. Tasks: `color-lighten-darken`. [the study](/imagine/design/color/) [the task](/framework/ai/2026-09-17/color-lighten-darken/)
- **layout-skill-boxes** (Design system) — The layout skill analyzes contrast ratios and boxes: a thing with its own background needs padding; a thing without one probably needs none, or it floats in midair off the margins. → A box is a background different from its parent's; a box wants padding, no box wants none. Tasks: `color-lighten-darken`. [the task (skill section quoted)](/framework/ai/2026-09-17/color-lighten-darken/)
- **page-as-data** (Pages) — The page system is the core template data structure: pages are everything, they need not be real paths, and a dynamic item can pretend, with a directory at its path holding its data. → A page's children can come from data; Make and JSON pages run on it. Tasks: `core-data-children`. [the seam, applied](/framework/core/Page/doc/data-children/) [Make on it](/imagine/paging/make/)
- **ask-element** (Ask) — The Ask widget usable right in a main web page: select any element and ask about it; the answer knows that thing's readme and the design decisions behind it. → Pick any element and ask; the answer reads its readme and decisions. Tasks: `ask-element`. [the demo](/framework/ext/Ask/) [how it picks](/framework/ext/Ask/doc/picking/)
- **robust-layouts** (Layouts) — A small number of robust layouts (the huge objective): different sizes, small mobile layouts, maybe two padding levels like standard and section, more and better responsive layouts that span 3440. → Three practice layouts hold at 400 to 3440 with zero nav movement; the nav rule is in core. Tasks: `nav-stability`, `practice-layouts`. [the three practice layouts](/layouts/practice/) [what the critic found and fixed](/layouts/practice/doc/critique/)
- **stable-navigation** (Layouts) — In two-or-more-column layouts the persistent navigation never jumps: column widths must not reflow when another column opens, or the reader loses their place. → An open column never moves when another opens; measured 0 px at every width. Tasks: `nav-stability`. [the lab: all three rows read 0 / 0 now](/imagine/design/navigation/) [the freeze, in core](/framework/core/Page/doc/columns/)
- **decision-browsing** (AI log) — For anything created, browse the design decisions behind it: layout, navigation, structure, visual hierarchy, color, focus, interaction, purpose and outcome, nested iceberg-style; analyze the components before building, so each thing builds the system for the next. → — Tasks: `layout-browser`, `ask-element`, `color-lighten-darken`. [a browser item: the why, one click down](/layouts/browse/ui-card/) [a practice layout: nine answers in the fold](/layouts/practice/workbench/)
- **ai-cards-padding** (Design system) — Why do the AI cards use custom padding when the goal is a minimal design system? If a new tool is needed, make one. → The AI cards used the token but with eleven bespoke fractions; they use the rungs now. Tasks: `spacing-census`, `design-system`. [the design system](/framework/styles/system/) [the census](/framework/ai/2026-09-17/spacing-census/census/)
- **padding-where-to-look** (Design system) — One place to look to see our padding; the padding page shows several intervals, not the system. → Look at /framework/styles/system/; nowhere else. Tasks: `design-system`. [the design system](/framework/styles/system/)
- **design-system-minimal-tokens** (Design system) — A design system: minimal tokens used wherever possible; .ac("pad") maps to --pad; documented in one place. → The design system is one page with seven lengths and five rungs. Tasks: `spacing-census`, `design-system`. [the design system](/framework/styles/system/) [the census](/framework/ai/2026-09-17/spacing-census/census/)
- **decisions-feedback** (AI log) — A feedback system: every decision documented as a structured page tree the skills produce, each with approve or improve; a wrong one traces back to the rule that made it, so the system gets fixed. → Every decision is a claim tree you can approve or improve; an improve files itself into the skill. Tasks: `decisions-system`. [a Decisions tab, live](/framework/ai/2026-09-17/decisions-system/) [ui/decision](/framework/ui/decision/)
- **even-columns** (Layouts) — N even columns: N computed on resize from the column's recommended width and the available space (3k px and 1000 each = 3); fixed, non-janky navigation instead of continuous Miller columns; animate pages sliding left and right. → Columns can be N equal widths from the room, sliding one at a time; the Finder uses it. Tasks: `even-columns`. [the Finder, even](/framework/core/Page/overview/columns/finder/) [the lab: today / stable / even](/imagine/design/navigation/)
- **paging-demos-fix** (Pages) — Fix the paging demos; merge Playground, Panel and the pages CRUD into one editor with a selection scheme and everything in the right sidebar instead of a crowded toolbar. → The paging hub opens on six blocks; Make selects anything and shows only its rows. Tasks: `paging-fix`, `editor-select`. [the hub, six blocks](/imagine/paging/) [Make: select a thing, the sidebar follows](/imagine/paging/make/)
- **real-page-move** (Pages) — Pages may be real dirs with page.js or dynamic; keep file-system simplicity and portability; dragging a real page moves its dir on disk. Morning brief: an rpc:move on the dev server plus the parent's children line; made pages already move through Make's seam. → Dragging a real page moves its folder and rewrites both parents; live since your restart today. Tasks: `real-page-move`. [Make on a real subtree](/imagine/paging/make/?real=/framework/ux/) [why string-only, the tree.json alternative](/imagine/paging/make/doc/decisions/)
- **sqlite-status** (Pages) — SQLite half working: pages read from D1 locally, never deployed. Needs you: Cloudflare login and D1 create - 5 min. → SQLite works locally, never deployed; five minutes of yours unlock it. **Waits on the owner:** Cloudflare login and wrangler d1 create (5 min). Tasks: `sqlite-scout`, `pages-in-d1`. [SQLite half working - the report](/framework/ai/2026-09-17/sqlite-scout/report/) [three pages from D1, locally](/imagine/cms/d1/)
- **ranked-lists** (AI log) — Threaded ranked lists: prioritize the dashboard, the task lists and the decisions inside each task as rankable cards; owner items read like 'Cloudflare API setup - 5 mins'; make column paging work with them. → Drag to rank asks and decisions; owner items sit first with their minutes. Tasks: `ranked-lists`. [the front: needs you, first](/framework/ai/) [a ranked Asks tab, live](/framework/ai/2026-09-17/ranked-lists/)
- **imagine-integration** (Pages) — A minion studies how to integrate the imagine realms with the core framework: complexity, renames, restructuring to simplify architecture and UI. → All three moves are done: the rail is four groups, the shape labs live under /layouts/, the design studies live in the design system; /imagine/ is tools and worlds now. Tasks: `imagine-integration`, `imagine-rail`, `imagine-move-3`. [the studies, in the system](/framework/styles/system/studies/) [the shape labs](/layouts/labs/)
- **mastermind-decides** (AI log) — Written into the mastermind skill: no approval asked for non-dangerous work; architectural questions get the best decision plus the alternative; only credentials-type roadblocks stop. → —
- **decision-ui** (AI log) — A decision UI: a nested list of cards that reads as options or alternatives, each with its own background; the layout skill asks whether siblings are more similar or more different and leaves equals alone. → — Tasks: `decisions-system`. [ui/decision](/framework/ui/decision/) [a Decisions tab, live](/framework/ai/2026-09-17/decisions-system/)
- **tree-component** (Layouts) — One tree UI component, merged from whatever exists: expandable and collapsible navigation links that adapt as you drill down, and the basis of page drag-and-drop. → One tree component: fold, drag, adapt, built from any page. Tasks: `tree-component`. [the tree, five levels live](/framework/ux/Tree/) [the three compared](/framework/ux/Tree/compare/)
- **left-sidebar** (Layouts) — A new persistent left sidebar: stays fixed, any size, resizable, a deep tree visible; beside it a responsive viewport that renders different homepage layouts you can switch between, to drift toward the good ones and remix them. → A resizable sidebar that never moves, beside a tree of derived designs, exists. Tasks: `shell-lab`. [the shell: sidebar + eight derived designs](/layouts/shell/) [the pages doc: what is a real page](/layouts/shell/doc/pages/)
- **pages-as-views** (Pages) — Pages are the fundamental template: methods on a page render its parts (a home page, a section); buttons are not pages; decide what is a real page.js on disk and what is dynamic; document every paging question and potential. → A real page has a url and could carry notes; a button is never a page. Tasks: `shell-lab`. [the pages doc: what is a real page](/layouts/shell/doc/pages/) [the task](/framework/ai/2026-09-17/shell-lab/)
- **alternatives-noted** (AI log) — Written into the mastermind skill: equally viable alternatives are written down with their caveats and the case they would win, never dropped, even when one option clearly wins. → —
- **page-design-tree** (Layouts) — A tree of designs by inheritance: a sub page imports its parent page, clones it (Object.create or a subclass), overrides a few properties and renders a variant; each child adds, changes or subtracts one thing, so the tree reads as the logic. Note how imports flow. → A design derives from its parent by importing its config; never Object.create the page. Tasks: `shell-lab`. [the shell: sidebar + eight derived designs](/layouts/shell/) [the pages doc: what is a real page](/layouts/shell/doc/pages/)
- **color-meets-color** (Design system) — Explore every permutation of the color tokens as whole sections: white beside light gray, dark beside light, one nested in the other; the seam is a visual effect. Written into the layout skill. → Sixteen of thirty ground pairings read as two pages, fourteen as one. Tasks: `type-color-study`. [where two grounds meet](/imagine/design/color/sections/) [the task](/framework/ai/2026-09-17/type-color-study/)
- **typography-repetition** (Design system) — Typography is font, size, weight, color in relation to the text around it; repetition is the strongest tool: an eyebrow, an h1, a heavy h2 with a tiny colored line after it, repeated, is a visual anchor in the theme color. Written into the layout skill. → A repeated colored mark anchors a page; difference finds it, contrast reads it. Tasks: `type-color-study`. [the repeated anchor](/imagine/design/type/anchors/) [the task](/framework/ai/2026-09-17/type-color-study/)
- **skills-minimal-direction** (AI log) — Skills and briefs articulate ideas in a relatively minimal way without losing important detail, referencing the detail where needed; direction without over-direction; the owner's explanations summarized. Written into the mastermind skill. → —
- **asks-grouped** (AI log) — The asks report: item by item, similar items combined under the same parent topic like a tree, so tomorrow morning the owner clicks through, remembers what was asked, and sees what was delivered. → — Tasks: `decisions-system`. [the asks, in five topics](/framework/ai/2026-09-17/mastermind-layout-browser/)

