# shell-lab — a persistent, resizable left sidebar, a viewport beside it, and a tree of homepage designs derived one change at a time

Load the `minion` skill first. Then this brief.

**Three laws.** Less is more (one shell, one tree of designs, one derivation mechanism). Clear beats brief by far. Prioritize (the shell first, the design tree second, the pages doc third).
**Length budget:** `/layouts/shell/` IS the thing: sidebar left, a design in the viewport, nothing else on screen. The pages doc is one screen of questions and answers. Your landing report is one screen.
**The reader is the overwhelmed newcomer.** Shown, not told.

## The owner's words (2026-09-17, 23:20)

> I want to focus on building a left sidebar that stays relatively fixed. And that can be any size. That could be a small one or it could be a rather large one. A deeply nested tree, maybe you want to be able to just see the whole thing, five levels deep, and you can resize the left sidebar to however big you want. The big thing there is that then we can use that left sidebar as a persistent navigation, it doesn't jump around, it's actually the simplest and easiest way to do it, and then you get a responsive resizable viewport that you can then render anything you want. So this mock-up I'm looking at has a left sidebar and then has like a home page style web page to the right of it. And if we can switch between different homepage style layouts, that's the idea: have a lot of different designs and kind of try and drift towards the good ones and then reuse those and remix those.

> Pages are going to be like the fundamental, it's almost like a view. You can create methods on the page to just render different parts of that template — a home page, a section. Buttons probably shouldn't be pages, because they don't need navigation, comments, decisions and notes. We got to figure out exactly what becomes a real page in terms of a page.js on the file system and what can be a dynamic page. All this stuff about paging, we need to document this, all these questions and potentials.

> Each page could extend the page class; in sub pages you could extend that class even further. Or use Object.create to clone the parent page: import your parent page, Object.create it, override specific properties before rendering, and render a new one that is somewhat different. Using that system you could create logical trees: here's where we start, then each one adds something, changes something, maybe subtracts something. As you progress down the tree, each tree item has children that are logically organized. I forget how the importing resolves — top-down, the parent imports the children? Make a note of how that works in the report.

"On the Imagine page we have a list of left sidebar links, and that works pretty well. I can jump around." — reuse what works.

## Deliverables (each ticked against the sentences above at harvest)

1. **`/layouts/shell/`** — a new child of `/layouts/` (top level: the page grid; add `shell` to `/layouts/page.js` `children:` and one visible `md()` line in its `content()`, one line in `/layouts/readme.md`; count the anchor on `/layouts/`). The page renders its OWN shell: a left sidebar and a viewport, edge to edge (`bleed` is paint; the shell is the page's own render — read `core/Page/readme.md` Watch out on overriding `render()`: set `this.view`, carry `.page`, never nest a second `.page`; the `/resume/` solo page is the precedent). The site's own sidebar stays where it is; this is a second one inside the page, the practice one.
2. **The sidebar**: `ux/Tree` (its current API — a sibling is extending it tonight, additively; do not edit it) rendering the shell's own subtree (the design tree below) five levels deep, every row a real link; **resizable** by dragging its right edge (a pointer handler or `ext/Draggable`; min 12rem, max half the room), the width remembered with `this.store()` (`core/Page/api/store/`); **fixed**: `position: sticky` in a full-height track so it never moves when the viewport's content changes or scrolls — prove with the nav-stability measurement (switch designs, re-measure the sidebar's x, width and its first five links' y: 0px change, at 1280 / 1920 / 3440); at 400 it becomes a top strip or a drawer — say which and why. Small and large sizes both must read well: shoot it at 14rem and at 40rem.
3. **The viewport**: renders the selected design, responsive to whatever width the sidebar leaves it (the design must hold at that width — measure the three invariants inside the viewport, not the window). The designs are a **tree**: a base `Home` (a homepage layout — pick one of the approved shapes; the Catalog practice layout at `/layouts/practice/catalog/` is a starting point to derive from, not to copy) and, under it, children that each **add, change or subtract one thing** ("Home → taller hero", "→ wall of three", "→ no hero", "→ dark band", …, at least two levels deep, at least six designs). Selecting a row in the sidebar shows that design. Each design says, one click down, what it changed from its parent — one line — and the sidebar's tree IS the logic.
4. **The derivation mechanism — decide, and write the alternatives.** Research three: (a) a subclass per design (`class TallHero extends Home`), (b) `Object.create(parentPage)` + `Object.assign(overrides)` on the INSTANCE, (c) deriving the CONFIG: the parent exports its config object (`export const home = { … }; export default new Page(home)`), the child imports the config and does `new Page({ ...home, ...overrides, meta: import.meta })`. Test each against core: does a cloned instance re-run `declare()`? does it share the parent's `children` Map (a trap)? does `meta`/`url` come out right? Pick one, build the six designs with it, and write the decision with the two alternatives and their caveats and the case each would win (the owner's rule tonight: alternatives are written down, never dropped). If `ext/JSONL` has a `decision` verb by the time you land (a sibling is adding it), write it as one; otherwise a `log` line.
5. **How imports flow — the note the owner asked for.** Read `core/Page/Page.class.js` (`child()`, `load()`, `declare()`) and say in five plain sentences in the report and in the doc: a parent names its children by name in `children:`; core dynamic-imports a child's `page.js` when it is routed to (or when `load_all_children()` runs); a child never imports its parent (a parent↔child cycle breaks only on deep reload — CLAUDE.md); so for design inheritance the child imports the parent's CONFIG (a plain object), never the parent's Page instance — one direction, no cycle. Verify with a cold deep reload of the deepest design.
6. **`/layouts/shell/doc/pages.md`** — the owner's questions, each with the answer we can give now and the alternative: what is a real `page.js` on disk (it has a url, navigation, and could carry comments, decisions and notes) and what is dynamic (a row of data, a variation, a state); is a button a page (no); is a section (only when it has its own url); can a page's methods render its parts (yes — `content()`, and any method you add; show one); how pages derive (deliverable 4); how imports flow (deliverable 5); real pages moving on disk when dragged (open — a later task); pages in a database (open — the scout's report). One screen; the mastermind moves it into `core/Page/doc/` at harvest.

## Rules

- Load `code` (every method a seam; parts as static subclasses), `layout` (all of it — the shell is two regions; the viewport's designs answer the five questions each), `css`, `new-css-class` (`std-` is `/layouts/`'s prefix: `std-shell-*`), `new-page`; `new-task` before the first edit (your dir exists: `ai/2026-09-17/shell-lab/`); `documentation` then `finish-task`; `skill-improvement` for any skill that misled you.
- **Fence:** `public/layouts/shell/**` (new), one `children:` word + one `md()` line in `public/layouts/page.js`, one line in `public/layouts/readme.md`, your task dir. Nothing else — not `ux/Tree` (consume it; log what it lacks with lines), not core, not `/layouts/practice/`, not `/layouts/browse/`.
- Never kill or restart the dev server, never drive the owner's tabs, never `git stash`, never `find /`. The owner's server (port 80) is NOT running; start your own: `PORT=8108 node server.js` from the repo root, in the background; kill it by PID when you land. `ui-test` has the headless recipe; `fullPage` captures the viewport only — give the viewport the height.
- Demos never persist except the sidebar's width (an editor-like preference, the owner's rule: visible, a dot not an alert — a width is fine silently).
- Two numbers that must agree: designs in the tree and rows in the sidebar; the sidebar's x before and after a design switch (0).
- **Resolve, don't park.** Findings as `log` lines; timestamps from the clock.
- Landing: `outcome` = a headline, the link, one screenshot at 1920 with the sidebar at 14rem and one at 40rem, the derivation decision in one line with its alternative, the import-flow note in five sentences, what was left and why. One screen.
