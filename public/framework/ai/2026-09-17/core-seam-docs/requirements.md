# Requirements — verbatim

You are a minion in the lew42 monorepo (C:\Code\lew42\monorepo). Load the `minion` skill first. Two small fixes left behind by the core-data-children landing (read its report at `public/framework/ai/2026-09-17/core-data-children/task.jsonl`, the `assign.outcome` of its last line, and the applied proposal at `public/framework/core/Page/doc/data-children.md`):

1. `public/imagine/paging/doc/persistence.md` still describes Make's hand-written `child()` / `load_all_children()` overrides as current. They were removed today; Make now declares `children(){ … }` returning a promise (see `public/imagine/paging/make/page.js`). Rewrite only the sentences that are now false so they describe what the file does today, in plain sentences, and add one dated line (2026-09-17) saying the overrides moved into core as `source_children()` with a link to `/framework/core/Page/doc/data-children/`. Change nothing else in that doc.
2. `public/imagine/cms/page.js` declares a `welcome` child that has no `page.js`, so every `/imagine/cms/` url logs one 404. Read the `new-page` skill's rule on declaring only what exists (step 3) and fix it the cleanest way: if a `welcome.md` exists, declare it as the config form the skill shows; if nothing exists, remove the word and say so in your log. Verify headless on a private server (`PORT=8101 node server.js` from the repo root, background, killed by its own PID when done) that `/imagine/cms/` loads with zero console 404s and its nav still shows what it showed before (count the nav links before and after — two numbers that must agree, or explain the difference).

Open your task with `new-task` in a new dir `public/framework/ai/2026-09-17/core-seam-docs/` (create it; `requirements.md` = this message) before the first edit; `finish-task` at the end. Fence: those two files, your task dir. Never kill or restart the dev server, never drive the owner's tabs, never `git stash`, never `find /`. Final message: five plain sentences with the two links and the two nav counts.
