# Brief: group the /imagine/ rail into four kinds

**The owner's words (2026-09-04, of /imagine/):** "I'm sort of lost… it's not clear at all." And 2026-09-17: "recommend renaming or restructuring to simplify architecture, UI." The study `public/framework/ai/2026-09-17/imagine-integration/proposal.md` (read it, and `inventory.md`) found the cause: the rail gives 24 identical cards to four kinds of thing — 6 tools you use, 7 worlds you play with, 7 shapes that say how to arrange a screen, 4 finished studies. Its Move 1 (S): sort the rail into those four groups. Zero urls move, zero links break.

**Deliverable:** `public/imagine/page.js` (and `imagine.css` only if a group heading needs one rule — `imagine-` is its prefix) so the rail and Start's card wall show the four groups in the proposal's order and membership, each with a one-word heading (tools · worlds · shapes · studies) and the realms under it in the proposal's order; nothing else on the page changes; every existing url still answers. Read the page first — its `children` is an ARRAY that mixes real directories and inline pages, and the rail and the wall both draw from the same children (its own comments say how); the cleanest way is a group marker the rail and the wall both read, not two lists. Under `/imagine/` there is no page grid (a columns host): the rail is a `small` column; keep the headings inside its rhythm (`--gap` rungs, no raw numbers — `/framework/styles/system/`).

**Prove it** headless on your private server (`PORT=8111 node server.js` from the repo root, background, killed by its real Windows PID — `$!` is not one; read netstat): the rail shows four headings and 24 realm links (two numbers that must agree with the inventory's count), every one of the 24 urls answers with its own h1, zero console errors, at 400 / 1280 / 3440; one screenshot of the rail at 1280 before and after into your task dir.

**Fence:** `public/imagine/page.js`, `public/imagine/imagine.css`, your task dir. Nothing else — other minions are editing `/imagine/paging/`, `/imagine/design/color/sections/` and `/imagine/design/type/anchors/` right now. Never kill or restart the dev server on port 80, never drive the owner's tabs, never `git stash`, never `find /`; a `rg` pattern that starts with `/` returns nothing through this Bash tool — drop the slash. Final message: five plain sentences with the link, the two counts, and the before/after paths.
