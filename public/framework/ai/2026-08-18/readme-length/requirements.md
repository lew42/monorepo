# Readme length → a try, not a cap

## The ask, verbatim

> same thing about readme < 30 lines?  damn that's short... if it's justified, it's fine.  just say, "try to keep them as short and simple as possible (law #1)"

## Scope

- `.claude/skills/documentation/SKILL.md` — the instruction itself.
- The same sentence is stamped verbatim into **39** `doc/file/readme.md.md` files under
  `public/framework/`; softening only the skill would leave the old rule teaching in 39 places,
  so the stamped phrase is swapped too. Mechanical, one string.
- The `readme-shape-2026-08-17` memory note says "never re-grow" — now wrong, update it.

No readme is rewritten. This changes the rule, not the docs written under it.
