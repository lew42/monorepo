# panel-document — a workspace that is a scrolling document of sections, not one screen

Laws: less is more · clarity · prioritize. **Deliverable: one root word that turns the workspace from a fixed-height *fill* box into a *flow* — as tall as its rows, the page scrolls, a split below appends a section — proven headless on `/full/` with numbers and two pngs in this dir; docs current; final message ≤ 15 lines.** Opus. Additive; the default stays exactly today's behaviour.

The owner (2026-08-18), verbatim: *"how do we get a full-screen Panel? How do I add sections to it? It seems when I add a row, it splits the thing, and doesn't make the area grow. WE sort of need a... workspace? that goes full screen, and is scrollable?"* North star: *"a simple, easy to use Panel system that allows me to explore flex and grid responsive layouts quickly and easily."*

## Facts (read these files first)

- `/framework/ext/Panel/full/` is `full(this, () => div.c("flex v", () => scrubber(workspace())))` (`page.js:126`) inside `styles/layouts/full.js` — whole window, ✕ back; the column sizes the workspace.
- [`panel.css:11`](/framework/ext/Panel/panel.css) `.panel-workspace { height: var(--panel-height, 34em); --panel-hug: 16em; }` and `:21` `.panel-workspace, .panel-items { container-type: size; }`; `.panel { flex-grow: var(--panel-grow, 1); overflow: hidden }` (`:24`). Every panel is `w: fill, h: fill` by default (`Panel.defaults`, `Panel.js:141`), so a bottom-edge split divides the height the workspace already has.
- `size.js` / `size.css`: per-axis `fill | hug | fixed`; **`hug` declares `--panel-hug` (16em), it never measures** — `container-type: size` measures a box as if it were empty (readme trap; `doc/templates.md`). `.panel-items.v > .panel.panel-h-hug { flex: 0 0 auto }` + `min-block-size: min(var(--panel-hug), 100cqb)` (`size.css:35-38`); `panel-h-fixed` is `flex: 0 1 min(var(--panel-h-at, …), 100%)` (`:46`).
- `split.js` `commit` → `item.divide(dir, new Panel(), at)` — the new panel gets the defaults (fills).
- Yesterday's lesson, `ai/2026-08-18/figma-home/`: *"`fill` claims one screen and is wrong for any content-heavy page; a homepage is a document."* Same distinction here.
- Landed today, don't collide: `flow.js` (recorder — note the name clash: **do not call your word `flow`** if it confuses `flow.js`/`.panel-flow-*`; `document`, `scroll`, or `page` are free — pick one and say why), the WORDS table in `glyphs.js` read by bar + rail (add your word THERE, root-only), `structure(text)` presets.

## Build

1. **One root word** — e.g. `mode: fill | document` on the ROOT panel only (rail row + bar pop via the WORDS table; hidden on non-roots). `document` means: the workspace's block size is `auto` with `min-block-size: var(--panel-height, 34em)` (on `/full/`, `min-block-size: 100%` of the window) and it **scrolls** (`overflow: auto` on the workspace, or let the page scroll — measure which reads better inside `.layout-full`, which is `position: fixed`); root `dir` becomes `col` (a document is rows); the root's `container-type` must not pin the size — check `panel.css:21` and say what you changed.
2. **A split below appends a section.** In a `document` root, a bottom/top-edge split (or `divide("col")` on the root's column) gives the NEW row `h: fixed` at `--panel-hug` (16em) rather than `fill` — so the column grows by one section instead of dividing. Existing rows keep their words. Nested splits inside a section behave exactly as today (they divide the section). Say where that default is decided (`split.js` `commit`, or `Panel.divide` reading the root's mode) and why there.
3. **A section can hug its content.** Hug never measures — accept that; but a section whose body has a real template (`.panel-t`) already gets `block-size: min(var(--panel-hug), 100cqb)` (`panel.css:73`). Report what a section with `h: hug` and a long template does in `document` mode (grows / clips / scrolls) — do not redesign hug; log the finding.
4. **Presets**: does any of the nine `space` presets (`structure("document")`, `"landing"`) read better in `document` mode? Try two, log which; do not change presets.
5. **Prove it, headless** (Playwright via `file:///C:/Users/mike/AppData/Roaming/npm/node_modules/playwright/index.mjs`; scratchpad `C:/Users/mike/AppData/Local/Temp/claude/c--Code-lew42-monorepo/a14ec0db-4e8c-4ce1-a14c-378e52ac01a0/scratchpad/`; **block the dev socket with `page.routeWebSocket(/.*/, () => {})`** or your clicks rewrite the owner's `/data/panels.json`; `git status public/data Server/data` clean at the end). On `/full/` at 1280×800: reset to a single leaf (say how), set the root to `document`, split below **three times** → 4 sections; numbers: workspace `scrollHeight` > `clientHeight` (or the page's) and each section ≥ 16em tall (two numbers that must agree: 4 × the section height ≈ the scroll height); scroll to the bottom section (png); at 400 the sections stack, no horizontal overflow; then set the root back to `fill` → the four rows share the window again (regression: identical to today). Also prove the DEFAULT is unchanged: a fresh `/full/` with no word behaves as before (the recorder's step count for a split is still 1). Pngs: `document-1280.png`, `document-400.png`.
6. **Docs**: `readme.md` Use line + one Watch-out; `doc/words.md` (landed today) gets the root word; `doc/decisions.md` one entry (fill vs document, why a root word and not a second workspace door); `page.js` one line pointing at `/full/` for the document mode — a demo block only if it fits without restructuring.

## Fences

`ext/Panel/panel.css`, `size.js`, `size.css`, `split.js`, `Panel.js` (defaults/`divide` only), `glyphs.js` (the WORDS row), `toolbar.js`/`properties.js` ONLY if the WORDS table cannot express root-only (say so), `page.js` (the `/full/` route line + one line of prose), `readme.md`, `doc/words.md`, `doc/decisions.md`, this dir. NOT `flow.js`, `workspace.js` (unless the scroll container has to be there — then the smallest change and say why), `generate.js`, `templates.*`, `display.*`, `paint.js`. Another minion is in `core/Page` + `ext/markdown` — nothing of yours.

## Rules

- Load `code` and `css` once before editing; `new-css-class` if you add a class (`panel-` prefix). Run `new-task` first (dir + brief exist; write `task.jsonl` line 1 and the `day.jsonl` line; group `panels`); the ledger logs edits; `documentation` then `finish-task` (`"tokens": null`). A skill that misleads you gets one line in its `improvements.md` (`skill-improvement`).
- Timestamps from the clock; forward slashes; never Out-File a `.jsonl`; never a person's name — "the owner". Every CSS rule inside a layer; only `p()`/`h1`–`h6` read backticks; no DOM after an `await` outside a callback; container queries cannot restyle themselves. Wait in the foreground.
