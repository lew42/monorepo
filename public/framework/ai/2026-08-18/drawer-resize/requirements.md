# drawer-resize — the drawer resizes the way the DevBar does, from one shared grip

Laws: less is more · clarity · prioritize. **Deliverable: `ext/grip/` (one small module) used by BOTH `dev/DevBar` and `ext/drawer`; the drawer resizes, remembers its width, and never lingers when closed; proven headless with numbers and three pngs in this dir; docs current; final message ≤ 15 lines.** Opus — the DevBar grip carries a subtle 2026-08-16 fix that must survive the extraction.

The owner (2026-08-18): *"The ext/drawer needs resizing, use devbar's setup (it has to be offset to the right, so it doesn't linger when closed."* Design already decided: [`../element-provenance/proposal.md`](../element-provenance/proposal.md) §4 — read it, then `dev/DevBar/grip.js` (25 lines), `grip.css`, `settings.js` (`rail()`, `set()`), `ext/drawer/drawer.js`, `drawer.css`, `ext/drawer/readme.md`, and the record of the offscreen fix: [`/framework/ai/2026-08-16/devbar-grip-offscreen/`](../2026-08-16/devbar-grip-offscreen/task.jsonl) (its outcome).

## Build

1. **`public/framework/ext/grip/`** — `grip.js` + `grip.css` + `readme.md` (index shape: what · Use · Watch out · More; ≤ 20 lines) + `doc/decisions.md` (why extracted, the three facts). API: `grip({ write, done, side = "start" })` returns the strip view: pointer capture on down, `write(px)` on every move while sizing (px = the width the pointer implies — the caller decides what to do with it), the pill riding `--grip-y`, `done(width)` once on up. A class on `<html>` while sizing (`grip-sizing`, or keep `dev-sizing` if DevBar's CSS depends on it and renaming costs more — say which) so the rail's transition can be zeroed during the drag (`html.dev-sizing { --rail-ease: 0s }` must travel with the extraction). ⚠ `ext/` must not import `dev/` (dev is dev-only; `ext/drawer` ships) — the module lives in `ext/` and DevBar imports it, never the reverse. `new-css-class` for the `grip-` prefix; register the prefix in `styles/css-scopes.txt` if that file lists prefixes.
2. **DevBar uses it.** `dev/DevBar/grip.js` becomes `grip({ write: px => rail(innerWidth - px), done: width => set({ width }) })` (or is deleted and DevBar.js calls `ext/grip` directly — whichever leaves fewer files). The three facts to carry: the grip sits **wholly inside** the rail's box (`inset-inline-start: 0`, no straddle); the closed rail slides `calc(100% + var(--dev-grip))`; `--dev-grip: 2rem` lives on `:root` beside `--dev-rail`. Prove DevBar still resizes (drag 200px → `--dev-rail` changes by 200px) and that a CLOSED DevBar leaves 0px of grip on the page at 1280 and 3440 (`elementFromPoint` on the strip is not the grip).
3. **The drawer uses it.** In `ext/drawer/drawer.js`, mount the grip on the rail's inline-start edge; `write` sets the drawer's width; `done` persists it. ⚠ **`drawer.close()` clears `--drawer`** (`drawer.js:40`) — width and open/closed are ONE variable today, so a resized drawer springs back to 19rem on reopen. Persist the width as **`--drawer-w`** (on the same `$shell` element `--drawer` is written to; `localStorage` under one key so it survives reload — or `sessionStorage` if the DevBar's `set()` uses that; match it) and have `drawer()` write `--drawer: var(--drawer-w, 19rem)`. `framework.css` needs no change (the proposal checked). Keep the `26rem` sheet breakpoint and the `--devbar` offset as they are. The grip must not linger when the drawer is closed: the closed drawer is `translateX(100%)` — carry the DevBar's own answer (the extra `--grip` allowance in the slide, or the fact that a grip wholly inside the box goes with the box; measure, don't assume).
4. **Prove it, headless** (global playwright: `import { chromium } from "file:///C:/Users/mike/AppData/Roaming/npm/node_modules/playwright/index.mjs"`; scratchpad `C:/Users/mike/AppData/Local/Temp/claude/c--Code-lew42-monorepo/a14ec0db-4e8c-4ce1-a14c-378e52ac01a0/scratchpad/`; block the dev socket with `page.routeWebSocket(/.*/, () => {})`). On `http://localhost/framework/ext/drawer/` (its page opens the drawer): open, drag the grip 160px left → drawer `inline-size` grows by 160px AND `.app`'s reserved strip grows by the same 160px (two numbers that must agree — the push and the width are one number); close → `elementFromPoint` along the former grip column finds page content, not the grip, at 1280 and 3440; reopen → the width persists; reload → still persists. Pngs: `drawer-resized.png`, `drawer-closed.png`, `devbar-still-resizes.png`. Zero console errors on `/framework/ext/drawer/`, `/framework/dev/DevBar/`, `/framework/`.
5. **Docs**: `ext/drawer/readme.md` — one Use line (`--drawer-w`), one Watch-out (width vs open are two tokens now, and why); `dev/DevBar/readme.md` — the grip line points at `ext/grip`; `ext/grip/readme.md` new; decisions entries in both modules' `doc/decisions.md`. Both `page.js` files: nothing unless a demo slot needs one line.

## Fences

You own: `ext/grip/*` (new), `dev/DevBar/grip.js`, `grip.css`, `DevBar.js` (the import line only), `settings.js` (only if `set()` needs a key), `devbar.css` (only the sizing class if renamed), `dev/DevBar/readme.md` + `doc/`, `ext/drawer/*`, `styles/css-scopes.txt` (one line), this dir. Nothing else — `ext/Panel/*` (two minions are in it), `ext/layout/*`, `framework.css` are off limits.

## Rules

- Load `code` and `css` once before editing. Run `new-task` first (dir + brief exist; write `task.jsonl` line 1 and the `day.jsonl` line; group `web-ui`); the ledger logs edits; `documentation` then `finish-task` (`"tokens": null`). A skill that misleads you gets one line in its `improvements.md` (`skill-improvement`).
- Timestamps from the clock; forward slashes; never Out-File a `.jsonl`; never a person's name — "the owner". Only `p()`/`h1`–`h6` read backticks; every CSS rule inside a layer; `rem` not `em` for the rail width (drawer readme trap); no DOM after an `await` outside a callback; imports flow down. Wait in the foreground.
