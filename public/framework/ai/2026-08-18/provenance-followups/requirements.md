# provenance-followups — the four S items from the element-provenance proposal

Laws: less is more · clarity · prioritize. **Deliverable: four small edits, each proven headless (one png per item where it shows, in this dir), docs touched where a readme line changes; final message ≤ 15 lines.** Sonnet. Precise brief; no design left to do — [`../element-provenance/proposal.md`](../element-provenance/proposal.md) §2b, §5, §6, §3 and §7 rows 1–4 are the design. Read that file first, then `ext/layout/readme.md`, `panel.js`, `body.js`, `layout.js`.

## Do — in this order

1. **Inline styles as a row** (§2b). In `public/framework/ext/layout/body.js` `defined($el)` (line ~49), the css group lists every applied *rule*; the element's own `el.style` (`[...el.style]` → `prop: el.style.getPropertyValue(prop)`) is invisible to the CSSOM walk and stronger than every rule. Add ONE row at the top of the group, part label `inline — written by JS`, listing those declarations, only when there are any. Same markup as the rule rows.
2. **The two-up: both panes selectable** (§5). `public/framework/ext/demo/layout.js:71-72` steers and toggles only `$views[0]`; `public/framework/ext/layout/layout.js:57` `region($box)` is the only thing that grants selectability and `layout.bar()` is its only caller. Export it as `layout.selectable($el)` (two lines beside `layout.bar`), then in `demo/layout.js`: `$views.forEach(layout.selectable)` and `this.toggles($stage, redraw)` (`host_of()` walks up, so `$stage` covers both panes). Prove on `http://localhost/framework/styles/layouts/docs/`: `.layout-region` count 2 (was 1), and a click in the RIGHT pane's box selects it (`.layout-selected` inside pane 1) instead of deselecting.
3. **`layout.selected()`** (§6): a one-line accessor exported from `panel.js` (and surfaced on `layout`) returning the current `$sel` (or `null`) — the contract the DevBar AI context reads. Then in `public/framework/dev/DevBar/ask.js` `selection()` (line ~48) — which reads `.panel.focus, .panel-text-on` from the DOM — add `.layout-selected` to that selector list, so a selected element in any layout demo reaches the AI turn too. Do not import `ext/layout` into `dev/DevBar` (dev → ext is allowed once already; keep it DOM-read as it is).
4. **Lock glyph + part link** (§3.1–2): in `defined($el)`'s part label, when `part` is `framework` or `lew42`, prefix a small lock icon (`icon("lock")`, Material Icons — verify the glyph name renders as a glyph, ~19px, not as a word) and make the part label a link: `framework` → `/framework/styles/`, `lew42` → `/framework/styles/layers/`, a module part → `/framework/ext/<part>/` if that page exists (feature-test with a `HEAD` fetch? no — the parts map already knows the sheet's href; derive the module dir from the href and link it; the Router 404s honestly if it is not a page). No new page, no banner — the proposal rejected the separate "core component" page.

## Fences

`ext/layout/body.js`, `ext/layout/layout.js`, `ext/layout/panel.js`, `ext/layout/readme.md` (one Watch-out line for the two-up if you think it earns it; the proposal flagged it), `ext/demo/layout.js` (the two lines), `dev/DevBar/ask.js` (the selector list), this dir. Nothing else — `ext/CSSDoc`, `ext/drawer`, `ext/Panel`, `framework.css` are off limits.

## Rules

- Load `code` once. Run `new-task` first (dir + brief exist; write `task.jsonl` line 1 and the `day.jsonl` line; group `web-ui`); the ledger logs edits; `documentation` if a readme line changes; `finish-task` (`"tokens": null`). A skill that misleads you gets one line in its `improvements.md` (`skill-improvement`).
- Verify headless (global playwright: `import { chromium } from "file:///C:/Users/mike/AppData/Roaming/npm/node_modules/playwright/index.mjs"`; scratchpad `C:/Users/mike/AppData/Local/Temp/claude/c--Code-lew42-monorepo/a14ec0db-4e8c-4ce1-a14c-378e52ac01a0/scratchpad/`; block the dev socket with `page.routeWebSocket(/.*/, () => {})`): the docs two-up page, `/framework/ext/layout/`, `/framework/dev/DevBar/` load with zero console errors at 1280; pngs of the drawer showing the inline row and the lock.
- Timestamps from the clock; forward slashes; never Out-File a `.jsonl`; never a person's name — "the owner". Only `p()`/`h1`–`h6` read backticks; no DOM after an `await` outside a callback; every CSS rule in a layer (you should need no CSS). Wait in the foreground.
