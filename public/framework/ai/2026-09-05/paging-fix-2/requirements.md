# paging-fix-2 — the fix pass after audit 2 (Opus)

Read first: the repo's `CLAUDE.md` (law 2 and the Presentation section), `../mastermind-day/requirements.md` (decisions; the owner's brief verbatim), `../../2026-09-04/mastermind-platform/minion-rules.md`, then the two audits you are fixing — `../paging-audit-2/task.jsonl` (the newcomer: four breaks, a 14-item list) and `../paging-audit-2b/task.jsonl` (the designer: the url seam, two schemas, three vocabularies, a 10-item list) — and the rebuild's own log `../paging-v3/task.jsonl`. `../nav-stability/` landed the reserved-stage and persistent-rail rules in `public/imagine/paging/navigation/navigation.css`; lift them, do not re-derive. Skills: `new-task` (this dir, group `paging`), `code`, `layout`, `new-page`, `css`, `new-css-class` (prefix `paging-`), `ui-test`, `documentation`, `finish-task`. You own `public/imagine/paging/` (all of it, `navigation/` included now).

## The merged list, in order (do them in this order; stop where the budget stops and say so)

1. **The toolbar never covers the demo.** Reserve its height above the stage (or put it in the stage's gutter) so every element of the demo — the tab strip first — is clickable with a mouse. Prove: `elementFromPoint` on all four tab centres returns the tab; a click on Pricing lands.
2. **Takeover never hides the rail, at any width, and always has a way out.** At 1280 the takeover preset paints over the rail with no exit. Keep the rail above it; add the exit chip the mechanism already has to every takeover stage.
3. **The stage reserves its height, so captions are true.** "THE BOX DID NOT MOVE" beside "335px shorter" is the first demo disproving itself. Use nav-stability's reserved-stage rule (`.paging-nav-reserve`): the stage is as tall as its tallest panel; then the caption says what actually happened, measured.
4. **The four intro strings with literal `**`** (`/skin/` `/content/` `/room/` `/arrangement/`) go through `md()` — or become one sentence of plain text with no markup.
5. **Configuration lives in the url.** `stage()` reads the seven words off the query string on mount; `set()` writes them back with one `history.replaceState`; the hub's `route()` answers any configuration. A preset becomes a bookmark; `nest` becomes linkable; the drawer gains "copy this link" beside "make this a page". Prove: set three words, copy the url, open it cold in a new context, the stage matches. (The designer's fix 1; the night run's open item.)
6. **One schema, one vocabulary.** Six names — the rail's Stage · Navigation · Content · Room · Arrangement · Skin — everywhere: the toolbar, the drawer, `page.json`, Build, Make's chips. Delete the other navigation vocabularies (`words.js:11`, `build/words.js:44`, `make/tabs.js:28` → import one `NAVIGATION`; keep `MECHANISMS` as a four-line alias for `/imagine/codrops/`); the surface and arrangement duplicates the same way. One schema for a made page (Make's chips write the same seven words the drawer writes; the old keys migrate on read). Fix the seed (`make/made.js`) to real content ids. `navigation` and `arrangement` must not both offer "Left rail"/"Right rail" — rename one pair. Skin gets its three controls grouped (surface, background, type); `background` and `type` get a page or Skin gets three nav grids.
7. **The hub's wall is twelve live miniatures**, the way `/templates/` already does it (seven-up, using the whole 3440), not twelve text cards. And the hub's first screen at 3440 is not 52% grey: open the stage on a wide room word above 1920, or put the wall beside the stage. Measure the FOLD at 3440 (1400px tall), not the widest element of the scrolling pane; log before/after.
8. **The toolbar is the drawer's labelled chip groups**, centred over the stage — not five clipped native selects and unlabelled dots. "More" is a real button.
9. **Delete every blockquote that tells the reader what they are about to see.** The demo is beneath it. Where a page needs a sentence, it is an instruction ("Click a tab.").
10. **Small, all of them:** the two editors say which to start with (or Make's file list folds into Build); `critique/` and `inventory/` are linked from Docs or deleted; the rail fits a 1440 fold at 3440 (collapse or two-column the lower sections); a visible "print the page.js" control on the stage (Build's printer, surfaced) so the six words have a way out; the crossing named and railed ("Cross": the theming wall plus a navigation × arrangement twin); ~30 unreferenced classes out of `paging.css`; two pages named Stage become one.
11. **If budget remains:** `BuildStage` renders a `PagingStage` (one stage renderer, not seven); `/imagine/paging/compare/` — two stages, one toolbar row.

## Prove it

`ui-test` for 1, 2, 3, 5 (the plans are the evidence); screenshots at 1280 and 3440 of the hub (fold only), a block page, the takeover preset, the toolbar; zero console errors at 400/1280/1920/3440 on the hub, the six block pages and three presets; the `layout` skill's three invariants; the hub fold's grey percentage before and after.

## Fences and budget

Write only under `public/imagine/paging/`; `css-scopes.txt` via the skill; this task dir. Never `core/`, `ext/` — proposals with the diff (the nav-stability proposals for `Page.css` are queued for a core minion; do not apply them). Private server (kill by the pid you started); never `find /`; never spawn agents; never `git stash`/commit. Budget ~500k tokens. Report in ≤ 12 plain lines: which of the eleven landed, the four breaks' proofs, the fold's grey before/after at 3440, the url of a bookmarked configuration.
