# paging-audit-6 — the sixth audit (two critics, Opus)

Same shape as `../paging-audit-5/requirements.md` (read it; the rubric is `../paging-audit-2/requirements.md`), applied after `../paging-fix-5/task.jsonl` landed (all seven; read its landing lines). Name your exit condition first, then walk, then say whether it is true. The owner's bar: *simple, powerful, organized, intuitive, easy to browse, infinite potential.*

**Critic A — the newcomer, sixth visit** (this dir): cold walk at 1280 and 3440 with screenshots; the six words with the delta from visit five (5 / 5 / 5 / 4 / 5 / 4); the use test grows to seven steps — pick, send, open cold, save, nest, and now **point the stage at content you wrote** (a `.md` you write under your task dir served by your private server, and a page you made) and **change a saved page from its own bar and reload**; what still cannot be done; the ranked FIX LIST or an empty one with the reason. If `intuitive` is still 4, say exactly what would make it 5.

**Critic B — the systems designer, sixth visit** (`../paging-audit-6b/`, create with `new-task`): re-check the fifth list with file:line; renderer and vocabulary counts; the save path end to end (bar → file → reload → url clean); `content=<url>` and `nest=<url>` including the loop fuse; then the exit question in your own three conditions from visit five — are (2) and (3) true now — and the FIX LIST or an empty one.

## Rules

Fix nothing; change nothing under `public/` except a scratch `.md` in your own task dir. Private server (kill by the pid you started; never port 80); never `find /`; never spawn agents; never `git stash`/commit. Budget ~200k each. Report in ≤ 10 plain lines plus the list.
