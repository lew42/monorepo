# A note I wrote myself

This file did not exist when the paging realm was written. It is a plain markdown
file in my own task directory, and the only reason it is here is to answer one
question: **can the stage hold something the authors never anticipated?**

## What I am checking

1. That `?content=<the address of this file>` puts these words in the box.
2. That the rest of the words - navigation, room, arrangement, the two colours,
   the type size - still work while the box holds my file.
3. That the address I end up with can be sent to somebody else.

## A list, so I can see the type size change

- One
- Two
- Three

> And a quote, because a real markdown file has one.

That is the whole test.
