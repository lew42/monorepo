# paging-audit-7 — the seventh audit (two critics, Opus)

Same shape as `../paging-audit-6/requirements.md` (read it; the rubric is `../paging-audit-2/requirements.md`), applied after `../paging-fix-6/task.jsonl` landed (all six; read its landing lines). Name your exit condition first; walk; say whether it is true. The owner's bar: *simple, powerful, organized, intuitive, easy to browse, infinite potential.*

**Critic A — the newcomer, seventh visit** (this dir): cold walk at 1280 and 3440 with screenshots; the six words with the delta from visit six (5 / 5 / 5 / 4 / 5 / 5); the seven-step use test again with a new page you want, plus an eighth step: save a nested configuration and open the saved page cold. What still cannot be done. The ranked FIX LIST or an empty one with the reason — and if every remaining item is cosmetic, say "cosmetic only" as the first line.

**Critic B — the systems designer, seventh visit** (`../paging-audit-7b/`, create with `new-task`): re-check the sixth list with file:line; your three conditions — say plainly which are true now; then a short honest paragraph for the owner: what this realm IS now in three sentences a newcomer would accept, and what the one biggest thing left is. The ranked FIX LIST or an empty one.

## Rules

Fix nothing; change nothing under `public/` except a scratch `.md` in your own task dir. Private server (kill by the pid you started; never port 80); never `find /`; never spawn agents; never `git stash`/commit. Budget ~200k each. Report in ≤ 10 plain lines plus the list.
