# paging-audit-5 — the fifth audit (two critics, Opus)

Same shape as `../paging-audit-4/requirements.md` (read it; the rubric is `../paging-audit-2/requirements.md`), applied after `../paging-fix-4/task.jsonl` landed (all eleven; read its landing lines). The owner's bar: *simple, powerful, organized, intuitive, easy to browse, infinite potential* — and this round asks each critic to name the exit condition in their own words before walking: what would have to be true for you to return an empty list. Then walk, and say whether it is true.

**Critic A — the newcomer, fifth visit** (this dir): cold walk at 1280 and 3440 with screenshots; the six words with the delta from visit four (4 / 5 / 5 / 4 / 5 / 3); repeat the five-step use test with a DIFFERENT page you want (say which) and add a sixth step: nest a page you made inside a preset, send that link, open it cold; what a newcomer still cannot do; the ranked one-line FIX LIST or an empty one.

**Critic B — the systems designer, fifth visit** (`../paging-audit-5b/`, create with `new-task`): re-check the fourth list with file:line; count renderers and vocabularies (the merge landed — is it one now); the url seam with the new label-word keys and `?nest=<url>`; then the question the owner cares about most, in one paragraph with evidence: does "infinite potential" hold — what can a reader generate here that the authors did not anticipate, and what is the one thing still closed. The ranked FIX LIST or an empty one.

## Rules

Fix nothing; change nothing under `public/`. Private server (kill by the pid you started; never port 80); never `find /`; never spawn agents; never `git stash`/commit. Budget ~200k each. Report in ≤ 10 plain lines plus the list.
