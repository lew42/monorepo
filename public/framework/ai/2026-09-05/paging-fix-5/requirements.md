# paging-fix-5 — the fix pass after audit 5 (Opus)

Read first: the repo's `CLAUDE.md` (law 2, the Presentation section), `../mastermind-day/requirements.md`, `../../2026-09-04/mastermind-platform/minion-rules.md`, the two fifth audits — `../paging-audit-5/task.jsonl` (the newcomer: 5/5/5/4/5/4, six-step test passed, eight items) and `../paging-audit-5b/task.jsonl` (the designer: one renderer confirmed, the keeping side closed, seven items with file:line) — and `../paging-fix-4/task.jsonl`. Skills: `new-task` (this dir, group `paging`), `code`, `layout`, `css`, `ui-test`, `documentation`, `finish-task`. You own `public/imagine/paging/`.

## The merged list, in order

1. **A nested made page draws its own children and blocks.** `stage.js:263 inside()` / `:283 fetched()` throw away the node's children and blocks — `/library/blog-post/?nest=/imagine/paging/make/notes/` reads Overview/Pricing/Docs/Contact. Hand the fetched node `pages:` and `draw:` the way `make/page.js:103` does. This is the same defect fixed one level up last round; fix it at the seam so there is no third level.
2. **All seven words savable, not only sendable.** Make writes four, Build three; `room` and `type size` are saved by no control, and the bar on your own page writes only the address. Give the builder's middle the realm's own bar (`doc/builder.md:449` has the one line) and let the bar on a made page write the file (the page you stand on is yours; Make's own store is the writer) — then prove: change `room` on a made page, reload bare, it stayed.
3. **`content` takes a url.** Today it is eight canned renderers, so all ~100,000 configurations hold one of eight things. Let `content` take a url the way `nest` does — a page's url (its `page.json` fetched and drawn as the content) or a `.md` url (rendered as prose). The dropdown keeps its eight and gains "a page or file…" with a field. Prove: point the stage at `/imagine/paging/make/notes/` and at a `.md` under `/notes/`, cold, by url. This is the item both critics say moves `infinite potential` to 5.
4. **A block silently switches `content` off** (`make/page.js:105` + `stage.js:225`): with one block, `draw` wins and `content` draws nothing while both controls still cycle. Say so at the control, or compose (blocks above the content). Ship one made page with a block so the case exists.
5. **Build's step 7 shows a quarter of every line** — the `pre` in `.build-code` is 230px holding 701 at 1280; give it the card's width and its own scroll like the drawer's boxes. And "the same page, as code" gets a **Copy** button beside the code the way the link box has "Copy this link".
6. **Expand.** Three mechanisms are navigation values (tabs · columns · takeover); `expand` is not, so its page runs `navigation: rail` and nothing on the first screen expands. Add an `expand` navigation value (children as an accordion — `ui/accordion` exists) so the page called Expand expands; say on the page that it never routes.
7. **Small, all of them:** delete the dead renderer (`make/tabs.js:47`/`:73`, `paging.css:374-375`); the docs teaching the deleted schema (`doc/persistence.md:220/237/241/243/252`, `doc/builder.md:38/241/402`, `templates/readme.md:22`) rewritten to the seven words; Build's step 3 label read off `CONTROLS` (it says Surface; everything else says content colour); the three drawer action buttons get their own class, not `paging-chip on`; Cross at 1280 hints that it scrolls (two of six columns are hidden); Make's duplicated sentence said once; the nest band's page name links the page; a skip link to the bar (a keyboard reaches it after ~45 tabs today).

## Prove it

`ui-test`: a nested made page's tabs are its own; `room` changed on a made page survives a bare reload; `content` pointed at a made page and at a `.md`, cold; Expand expands; Build's step 7 line widths; the Copy button. Screenshots at 1280 and 3440. Zero console errors at 400/1280/1920/3440 across the realm (44 pages last time; match it), only core's `readme/page.js` probe 404 allowed.

## Fences and budget

Write only under `public/imagine/paging/`; this task dir. Never `core/`, `ext/` (`ui/accordion` is imported, not edited). Private server (kill by the pid you started); never `find /`; never spawn agents; never `git stash`/commit. Budget ~450k tokens. Report in ≤ 10 plain lines: which of the seven landed, the proofs for 1–3, what you left and why.
