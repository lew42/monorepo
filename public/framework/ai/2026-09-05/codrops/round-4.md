# codrops — round 4 (Sonnet)

Same rules as `requirements.md`, `round-2.md`, `round-3.md` in this dir (read them), plus `public/imagine/codrops/readme.md` and the nine rows in `codrops/page.js`. Task dir: `../codrops-4/` (create its `task.jsonl` with `new-task`, group `codrops`).

Port **three more**: a page-load or intro sequence (framed as the realm's `takeover` or `launch` if it fits, say which), a cursor or pointer-driven effect, and a card stack or carousel (framed as `swap`). Same licence discipline, substitutions, proofs; never `Write` a shared file; kill only the pid you started. Then, as the last thing: on the hub, group the nine-plus-three cards by the paging mechanism each one illustrates (`swap` · `expand` · `launch` · `takeover` · none), one plain sentence per group — so the realm reads as "Codrops effects, sorted by what a click does". Budget ~250k tokens. Report in ≤ 10 lines.
