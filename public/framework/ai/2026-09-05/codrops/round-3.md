# codrops — round 3 (Sonnet)

Same rules as `requirements.md` and `round-2.md` in this dir (read both), plus `public/imagine/codrops/readme.md` and the six rows already in `codrops/page.js`. Task dir: `../codrops-3/` (create its `task.jsonl` with `new-task`, group `codrops`).

Port **three more**: a menu or navigation effect (framed in the paging vocabulary — which mechanism is it), an image or grid transition, and a hover-driven layout change. Same licence discipline, same substitutions, same proofs. ⚠ Never `Write` a shared file (`usage.jsonl`, another task's files) — round 2 clobbered one; append only, and only to your own task dir. Log rejections. Budget ~250k tokens. Report in ≤ 10 lines.
