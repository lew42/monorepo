# sections — build brief (Opus): multi-column sections that fill 3440

Read first: the repo's `CLAUDE.md` (law 2 and the Presentation section — the page explains by being the thing), `../mastermind-day/requirements.md` (the decisions; the newcomer rule), `../../2026-09-04/mastermind-platform/minion-rules.md`, then what already exists and must be built on, not beside: `/imagine/layouts/` (`public/imagine/layouts/` — `system.js`, `LayoutsCard.js`, the `3.*` entries, `readme.md`, `doc/decisions.md`: the 3-column card technique IS a section), `/imagine/paging/navigation/` (the stable rules: persistent rail, reserved heights — `navigation.css`), the paging vocabulary (`public/imagine/paging/blocks.js`: room, arrangement, skin), the spacing decision (`/imagine/design/spacing/decision.md`: the three ramps and the three levels — use them, never a constant), `public/framework/styles/layouts/` (the arrangements), `public/imagine/shells/` (persistent rails), `public/blog/` (`Section` is a list of posts there — a different thing; do not collide on the name; `new-css-class` census, prefix `sections-`). Skills: `new-task` (this dir, group `layout`), `code`, `layout` (the five questions, per section kind), `new-page`, `css`, `new-css-class`, `ui-test`, `documentation`, `finish-task`.

## The owner's words (verbatim, 2026-09-05 15:30)

> I want you to build some multi column section UI, that fill 3440 well. you could try 2 column sections that would likely need columns nested within to utilize the space. however, 3 column layouts could be useful: the left column could have some prose, or anything. the main area could be a demo, or anything, and the right column some UI controls, or notes, or anything. experiment with different color schemes for the columns and content. maybe different distributions of columns. of the left and right sidebars match, or if there's a header and footer, and they all are flush and seamless, it's like the main area is framed, and gives a lot of space for buttons, UI, controls, etc.
> 4 column sections could be harder to have simple visual hierarchy, but you could try.
> for all the layout patterns, you could either stack these sections with 0 spacing, or with spacing, making them uniform and repeating. that could be good for a list of related templates, where each gets progressively different.
> you could alternate section styles, from 3 column to 2 or 4 column, for example, alternating the bg colors, is the section a card, or is the section flush/full bleed? and then scrolling:
> let's say you had the main column for each section with more than a screens height of content. you could try to make the column limited height and independently scroll, but I think making the left/right sidebars sticky, so they stay, but are confined to their section, the next section's sidebars do the same. then, the long main content causes the page to naturally be longer, however we get a nice ux with sidebars that remain on screen.
> for different screen sizes, maybe those sidebars themselves could have sticky footers and an inner scrolling area themselves, so that if a sticky sidebar had more than a screen of content, you could still have a footer and scrolling area that would work on any screen height. for mobile, they would simply stack, maybe a left sidebar nav could collapse into a menu or something.
> then, that's another interesting point: can these 3 column, tall sections, have nav? can we have left sidebar nav? try it, why not? can you have multiple sections on one page, with nav? I think it depends if they're launching a new column, switching the sections main content area, etc.
> figure out a way to organize and explain these concepts.
> I think an important distinction here, is that we might want to use a 3 column sticky sidebar system for both tall sections, and full screen layouts. for example, stacking 3 column sections creates scrolling ux. but you could have a single 3 column page as more of a full screen, center column scrolls, type of deal.

## Deliverables (numbered; each checked against his sentences)

0. **One class, parts as static subclasses**, in `public/imagine/sections/sections.js`: a section with `Side` (left) · `Main` · `Aside` (right) · `Head` · `Foot`, and words for: columns (2 · 3 · 4), distribution (the `styles/layouts/cols/` words: golden, main-aside, rail-main-aside, equal, thirds, a fixed + fluid), skin per column (the five paging surfaces, applied to any column independently, and to the section's own background), frame (`card` · `flush` — flush = full bleed; when side, aside, head and foot are flush and match, the main area reads as FRAMED), spacing between stacked sections (`0` · the gap ramp · airy), sticky (side and aside stick within their own section; `position: sticky` inside a grid row with `align-self: start`; the next section's own sidebars take over — measured, not assumed), the sidebar's inner scroll with a sticky footer (a sidebar taller than the screen: `max-height: 100vh`, a scrolling body, a footer that stays), and mobile (everything stacks; a left nav collapses to a menu). Every value a chip in the section's aside can change live (the paging hover toolbar's shape is fine; reuse its css if you can import it without dragging the realm in).
1. **`/imagine/sections/`** — the realm's page IS the explanation: a stack of sections, each one demonstrating one idea, its left column saying the idea in one or two plain sentences, its main area being the demo, its right column the chips that change it. In order: a 3-column framed section (flush matching sides + head + foot: "the main area is framed") · a 2-column section with columns nested inside the main to use the width · a 4-column section (the hierarchy problem, shown; your best answer) · distributions (one section, chips) · colour schemes (one section, chips for each column's skin and the section's own) · stacked with 0 spacing vs the gap ramp (two short stacks side by side or one after the other) · alternating styles (3 → 2 → 4, alternating background, card vs flush) · **sticky sidebars confined to their section** (a section whose main is two screens tall; scroll it: the sidebars stay, then the next section's take over) · a sidebar taller than the screen with an inner scroll and a sticky footer · a section with a left nav that SWITCHES its own main area (stable — nothing else moves) · a section with a left nav that LAUNCHES a column (dynamic — say so) · two sections on one page each with their own nav. Above the fold at 3440: the first framed section, filling the width; the fold at 1400px tall holds one section. Nothing telling; every section's left column is the whole text.
2. **`sections/full/`** — the same class as a full-screen page: side and aside fixed to the viewport, the centre scrolls; with a left nav switching the centre. The owner's distinction, shown: the same three columns as a tall section that scrolls the page vs a full screen that scrolls the centre.
3. **`sections/stack/`** — a list of related templates as uniform repeating sections, each progressively different (use the twelve paging presets or the eleven template families as the content: `/imagine/paging/templates/`, `/imagine/paging/library/` — import, do not copy).
4. **`sections/nav/`** — the nav question answered by doing it: a tall 3-column page with a left sidebar nav, four sections each with nav, the two behaviours (switch the main vs launch a column) side by side with one sentence each.
5. **Docs:** `readme.md` (what · use · watch out · more), `doc/decisions.md` (how this relates to `/imagine/layouts/` 3.* and the LayoutsCard — a section is that card made a first-class stackable thing — and to paging's arrangement/room/skin; the sticky measurements). Proposals in the log for: a "Sections" entry on the layouts hub and a "Next door" link on the paging rail (the mastermind wires realms).

## Prove it

`ui-test`: scroll the sticky section — read the sidebar's rect at three scroll positions inside the section (unchanged) and after leaving it (the next section's sidebar is now sticky); the inner-scroll sidebar's footer rect at two inner scroll positions; the left-nav switch (main changed, side and aside rects unchanged); mobile stacking at 400 (order, and the menu). Screenshots at 400 / 1280 / 1920 / 3440 of the hub's first fold and the sticky section mid-scroll. Zero console errors; the `layout` skill's three invariants; the spacing ramps, never a constant.

## Fences and budget

Write only `public/imagine/sections/` (new; a stub `page.js` is there — replace it), `css-scopes.txt` via the skill, this task dir. Never `core/`, `ext/`, `layouts/`, `paging/`, `styles/` — proposals with the diff. Private server (kill by the pid you started); never `find /`; never spawn agents; never `git stash`/commit. Budget ~500k tokens. Report in ≤ 12 plain lines: the realm url, the class's parts and words, the sticky proof numbers, what the 4-column answer was, the full-screen twin's url, what you left open and why.
